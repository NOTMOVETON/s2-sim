---
phase: 02-unified-entity-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_core/include/s2/entity.hpp
  - workspace/s2_core/include/s2/agent.hpp
  - workspace/s2_core/include/s2/actor.hpp
  - workspace/s2_core/include/s2/prop.hpp
  - workspace/s2_core/include/s2/world.hpp
  - workspace/s2_core/include/s2/types.hpp
  - workspace/s2_core/include/s2/effect_context.hpp
  - workspace/s2_core/include/s2/zone_system.hpp
  - workspace/s2_core/src/zone_system.cpp
  - workspace/s2_core/include/s2/plugin_base.hpp
  - workspace/s2_core/include/s2/world_snapshot.hpp
  - workspace/s2_core/include/s2/sim_engine.hpp
autonomous: true
---

<objective>
## Goal
Ввести EntityBase + AgentData/ActorData/PropData, перевести SimWorld на Variant D (типизированные векторы + индексные карты), обновить ZoneSystem/EffectContext/plugin_base/world_snapshot чтобы проект собирался.

## Purpose
Все последующие фазы (KernelCommands, WorldQuery, IActorBehavior, EffectPlugin, Transport) строятся поверх этой модели. Variant D даёт cache-friendly layout для тикового цикла и единый EntityId-namespace.

## Output
Новый entity.hpp. SimWorld с O(1) lookup. Все существующие плагины и тесты продолжают компилироваться и проходить. Сборка `tests` без ошибок.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-plugin-lifecycle/01-01-SUMMARY.md

## Phase Research
@.paul/phases/02-unified-entity-model/CONTEXT.md
@.paul/phases/02-unified-entity-model/RESEARCH.md

## Source Files
@workspace/s2_core/include/s2/types.hpp
@workspace/s2_core/include/s2/agent.hpp
@workspace/s2_core/include/s2/actor.hpp
@workspace/s2_core/include/s2/prop.hpp
@workspace/s2_core/include/s2/world.hpp
@workspace/s2_core/include/s2/sim_engine.hpp
@workspace/s2_core/include/s2/zone_system.hpp
@workspace/s2_core/src/zone_system.cpp
@workspace/s2_core/include/s2/effect_context.hpp
@workspace/s2_core/include/s2/plugin_base.hpp
@workspace/s2_core/include/s2/world_snapshot.hpp
</context>

<acceptance_criteria>

## AC-1: EntityBase hierarchy существует и корректна
```gherkin
Given entity.hpp включён
When код создаёт AgentData, ActorData, PropData
Then все три наследуют EntityBase; PropData не содержит поля SharedState
     (структурно — его нет в определении, не только runtime assertion)
```

## AC-2: SimWorld предоставляет O(1) lookup по EntityId
```gherkin
Given мир с добавленными агентами через add_agent()
When вызван get_agent(id)
Then агент возвращается через индексную карту (не линейный перебор)
```

## AC-3: ZoneSystem принимает SimWorld вместо vector<Agent>
```gherkin
Given ZoneSystem::tick() вызывается из SimEngine
When передаётся world_ вместо world_.agents()
Then зоны применяются ко всем агентам; тесты зон не падают
```

## AC-4: PropSnapshot использует правильный тип id
```gherkin
Given world_snapshot.hpp скомпилирован
When PropSnapshot строится
Then поле id имеет тип ObjectId (не ActorId — это был баг)
```

## AC-5: Все существующие тесты проходят
```gherkin
Given все изменения применены
When docker compose запускает tests
Then все тесты проходят; сборка без ошибок и warnings на новых типах
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: entity.hpp — EntityBase + AgentData + ActorData + PropData</name>
  <files>
    workspace/s2_core/include/s2/entity.hpp,
    workspace/s2_core/include/s2/types.hpp,
    workspace/s2_core/include/s2/agent.hpp,
    workspace/s2_core/include/s2/actor.hpp,
    workspace/s2_core/include/s2/prop.hpp
  </files>
  <action>
    1. В types.hpp добавить enum class EntityType { AGENT, ACTOR, PROP };
       Оставить AgentId, ActorId, ObjectId как есть — EntityId = uint32_t уже там.

    2. Создать workspace/s2_core/include/s2/entity.hpp:
       ```cpp
       struct EntityBase {
           EntityId id{0};
           EntityType type{EntityType::AGENT};
           std::string name;
           Pose3D world_pose;
           CollisionShape collision;
           VisualDesc visual;
           std::unordered_set<std::string> capabilities;
           std::map<std::string, std::string> tags;
           std::unordered_set<std::string> immune_to_effects;
           std::vector<std::string> signals;   // заглушка Phase 14
           bool enabled{true};
           std::vector<EntityId> owned_zones;  // заглушка Phase 12
       };

       struct AgentData : EntityBase {
           Velocity world_velocity;
           SharedState state;
           std::vector<std::unique_ptr<plugins::IAgentPlugin>> plugins;
           bool has_collision{false};
           double max_slope_rad{0.0};
           double max_step_height{0.0};
           CollisionShape bounding;
           std::unique_ptr<KinematicTree> kinematic_tree;
           // transport: хранится в tags["transport_type"] и tags["transport_domain_id"]
           // Phase 8 введёт TransportLink; пока — поля в tags для SceneLoader
       };

       struct ActorData : EntityBase {
           ActorState current_state;
           // BehaviorSlot — Phase 5
       };

       struct PropData : EntityBase {
           bool movable{false};
           std::map<std::string, std::string> properties;
           // NO SharedState — структурное ограничение, не runtime assertion
       };
       ```

    3. agent.hpp: заменить содержимое на `#include <s2/entity.hpp>` +
       `namespace s2 { using Agent = AgentData; }` — backward-compat alias,
       все существующие плагины (void update(Agent& a)) компилируются без изменений.

    4. actor.hpp: аналогично — `using Actor = ActorData;`

    5. prop.hpp: аналогично — `using Prop = PropData;`

    Не трогать: CollisionShape, VisualDesc, Pose3D, KinematicTree — они остаются как есть.
    Не менять: SharedState, plugin интерфейсы — они ссылаются на Agent через alias.
  </action>
  <verify>
    docker compose -f docker/docker-compose.yml run --rm build 2>&1 | grep -E "error:|warning:" | head -20
  </verify>
  <done>AC-1 satisfied: EntityBase/AgentData/ActorData/PropData компилируются; PropData без SharedState</done>
</task>

<task type="auto">
  <name>Task 2: world.hpp — Variant D storage с O(1) lookup</name>
  <files>
    workspace/s2_core/include/s2/world.hpp
  </files>
  <action>
    Переписать SimWorld (только storage и lookup — tick-логику не трогать):

    Private поля:
    ```cpp
    std::vector<AgentData> agents_;
    std::vector<ActorData> actors_;
    std::vector<PropData>  props_;
    std::unordered_map<EntityId, EntityType> entity_type_;
    std::unordered_map<EntityId, size_t>     agent_idx_;
    std::unordered_map<EntityId, size_t>     actor_idx_;
    std::unordered_map<EntityId, size_t>     prop_idx_;
    ```

    Public API:
    - `add_agent(AgentData agent)` — добавляет, обновляет все 4 map
    - `add_actor(ActorData actor)` — аналогично
    - `add_prop(PropData prop)` — аналогично
    - `get_agent(EntityId id) -> AgentData*` — O(1) через agent_idx_
    - `get_actor(EntityId id) -> ActorData*` — O(1) через actor_idx_
    - `get_prop(EntityId id) -> PropData*` — O(1) через prop_idx_
    - `get_entity_type(EntityId id) -> std::optional<EntityType>`
    - `remove_agent(EntityId id)` — swap-and-pop (обновляет индекс перемещённого)
    - `agents() -> std::vector<AgentData>&` (+ const overload)
    - `actors() -> std::vector<ActorData>&` (+ const overload)
    - `props() -> std::vector<PropData>&` (+ const overload)
    - Оставить get_agent(AgentId) как compat overload: static_cast<EntityId>(id)

    swap-and-pop для remove_agent:
    ```cpp
    void remove_agent(EntityId id) {
        auto it = agent_idx_.find(id);
        if (it == agent_idx_.end()) return;
        size_t idx = it->second;
        size_t last = agents_.size() - 1;
        if (idx != last) {
            std::swap(agents_[idx], agents_[last]);
            agent_idx_[agents_[idx].id] = idx;
        }
        agents_.pop_back();
        agent_idx_.erase(id);
        entity_type_.erase(id);
    }
    ```

    Не менять: check_sphere_collision(), static_geometry_, heightmap_, zones_.
    Не вводить: remove_actor/remove_prop (Phase 3, KernelCommands).
  </action>
  <verify>
    docker compose -f docker/docker-compose.yml run --rm build 2>&1 | grep -c "error:" ; echo "0 ошибок — OK"
  </verify>
  <done>AC-2 satisfied: get_agent через O(1) map lookup; сборка проходит</done>
</task>

<task type="auto">
  <name>Task 3: Consumer updates — ZoneSystem, EffectContext, plugin_base, world_snapshot, sim_engine</name>
  <files>
    workspace/s2_core/include/s2/zone_system.hpp,
    workspace/s2_core/src/zone_system.cpp,
    workspace/s2_core/include/s2/effect_context.hpp,
    workspace/s2_core/include/s2/plugin_base.hpp,
    workspace/s2_core/include/s2/world_snapshot.hpp,
    workspace/s2_core/include/s2/sim_engine.hpp
  </files>
  <action>
    1. effect_context.hpp:
       Добавить `EntityId entity_id{0};` рядом с `AgentId agent_id{0};`.
       Оставить agent_id для обратной совместимости (Phase 6 уберёт при миграции эффектов).
       При заполнении EffectContext в zone_system.cpp:
       `ctx.entity_id = agent.id; ctx.agent_id = agent.id;` — пока дублируем.

    2. zone_system.hpp — изменить сигнатуру tick():
       ```cpp
       // Было:
       void tick(std::vector<Agent>& agents, const std::vector<Actor>& actors,
                 SimBus& bus, double sim_time, double dt);
       // Стало:
       void tick(SimWorld& world, SimBus& bus, double sim_time, double dt);
       ```
       Добавить forward-declare или include SimWorld. 
       Обновить приватные методы: on_agent_enter/exit, apply_active_effects,
       detection_point, capabilities_match — принимают AgentData& (Agent = AgentData, ок).

    3. zone_system.cpp — обновить реализацию tick():
       `world.agents()` вместо переданного вектора агентов.
       `world.actors()` для attached-зон.
       Логика внутри не меняется — только источник данных.

    4. plugin_base.hpp — обновить on_spawn/on_despawn:
       ```cpp
       // Было: virtual void on_spawn(Agent& agent)
       // Стало: virtual void on_spawn(AgentData& agent)
       ```
       Agent = AgentData через alias, поэтому вызывающий код не ломается.
       Аналогично on_despawn. Остальные сигнатуры (update, initialize и т.д.)
       принимают Agent& — через alias это тот же AgentData&, ок.

    5. world_snapshot.hpp — исправить баг в PropSnapshot:
       ```cpp
       // Было: ActorId id;   ← БАГ (research/agent-class-hierarchy.md:34)
       // Стало: ObjectId id;
       ```
       Больше ничего в world_snapshot не трогать.

    6. sim_engine.hpp:
       - `initial_states_` тип не меняется (AgentId → AgentId, они же EntityId uint32_t)
       - tick() вызов ZoneSystem: заменить `zone_system_.tick(world_.agents(), world_.actors(), ...)` 
         на `zone_system_.tick(world_, ...)` 
       - Всё остальное в sim_engine не трогать.
  </action>
  <verify>
    docker compose -f docker/docker-compose.yml run --rm tests 2>&1 | tail -20
  </verify>
  <done>AC-3, AC-4, AC-5 satisfied: ZoneSystem принимает SimWorld; PropSnapshot.id = ObjectId; все тесты проходят</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `workspace/s2_core/include/s2/shared_state.hpp` — SharedState revision в Phase 7
- `workspace/s2_core/include/s2/scene_loader.hpp` — YAML-расширение в Phase 02-02
- Все файлы в `workspace/s2_plugins/` — плагины не трогаем (alias Agent = AgentData делает это ненужным)
- Порядок фаз тика в sim_engine.hpp — только tick() вызов ZoneSystem
- KinematicTree — переезжает в AgentData.kinematic_tree без изменений кода KinematicTree

## SCOPE LIMITS
- Не реализовывать BehaviorSlot логику (Phase 5)
- Не реализовывать TransportPool (Phase 8) — transport в tags как временное хранилище
- Не реализовывать OwnEffects (Phase 7)
- Signals и owned_zones — пустые заглушки
- Не добавлять новые YAML поля в SceneLoader (это Plan 02-02)
- Не трогать KinematicTree, CollisionSystem, RaycastEngine

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `docker compose -f docker/docker-compose.yml run --rm build` — 0 errors, 0 новых warnings
- [ ] `docker compose -f docker/docker-compose.yml run --rm tests` — все тесты green включая test_plugin_lifecycle
- [ ] entity.hpp существует с EntityBase, AgentData, ActorData, PropData
- [ ] world.hpp имеет agent_idx_ / actor_idx_ / prop_idx_ / entity_type_ maps
- [ ] PropSnapshot.id имеет тип ObjectId
- [ ] ZoneSystem::tick() принимает SimWorld& (не vector<Agent>&)
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Все задачи выполнены
- Сборка и тесты без ошибок
- PropData не содержит SharedState (структурно)
- SimWorld: get_agent(id) O(1)
- ZoneSystem: tick(SimWorld&, ...)
</success_criteria>

<output>
After completion, create `.paul/phases/02-unified-entity-model/02-01-SUMMARY.md`
</output>
