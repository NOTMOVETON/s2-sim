---
phase: "00-core-architecture-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_core/include/s2/types.hpp
  - workspace/s2_core/include/s2/event_bus.hpp
  - workspace/s2_core/include/s2/agent.hpp
  - workspace/s2_core/tests/test_event_bus.cpp
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - "struct Signal существует в namespace s2 в types.hpp с полями: signal_type, signal_id, local_pose, params, range, requires_los, enabled"
    - "class EventBus (бывший SimBus) существует в event_bus.hpp с теми же template-методами subscribe/publish"
    - "Agent::signals поле типа std::vector<Signal> присутствует в agent.hpp"
    - "Все новые event-типы присутствуют в namespace event внутри event_bus.hpp: EntitySpawned, EntityDespawned, SignalActivated, SignalDeactivated, ZoneEntered, ZoneExited, GrabAttempt, GrabSucceeded, GrabFailed, DamageDealt"
    - "sim_bus.hpp содержит using SimBus = EventBus и using AgentEnteredZone / AgentExitedZone для обратной совместимости"
    - "Тесты из test_sim_bus.cpp компилируются без изменений (backward compat aliases)"
    - "Docker: docker compose tests проходит"
  artifacts:
    - path: "workspace/s2_core/include/s2/event_bus.hpp"
      provides: "EventBus + все event:: типы"
      contains: "class EventBus"
    - path: "workspace/s2_core/include/s2/sim_bus.hpp"
      provides: "backward-compat alias"
      contains: "using SimBus = EventBus"
    - path: "workspace/s2_core/include/s2/types.hpp"
      provides: "Signal struct"
      contains: "struct Signal"
    - path: "workspace/s2_core/include/s2/agent.hpp"
      provides: "Agent::signals"
      contains: "signals"
    - path: "workspace/s2_core/tests/test_event_bus.cpp"
      provides: "тесты новых event-типов"
      contains: "EntitySpawned"
  key_links:
    - from: "workspace/s2_core/include/s2/sim_engine.hpp"
      to: "workspace/s2_core/include/s2/event_bus.hpp"
      via: "include sim_bus.hpp → alias EventBus"
      pattern: "sim_bus\\.hpp"
---

<objective>
Закладываем фундамент: Signal struct для системы сигналов и новый EventBus (переименование SimBus) с полным набором типизированных событий.

Purpose: Signal struct нужен для ARCH-05 WorldQuery (find_signals_of_type) и ARCH-03 wire-сигналов. EventBus с новыми event-типами нужен для всех последующих фаз (акторы, зоны, взаимодействия).

Output:
- types.hpp обновлён: Signal struct
- event_bus.hpp создан: class EventBus + все event:: типы
- sim_bus.hpp обновлён: backward-compat alias using SimBus = EventBus
- agent.hpp: добавлено поле signals
- test_event_bus.cpp: тесты новых типов
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/PROJECT.md
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/00-core-architecture-foundation/00-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/RESULT_DISCUSS.md
</context>

<interfaces>
<!-- Текущие файлы, которые executor ДОЛЖЕН прочитать перед реализацией. -->

Из workspace/s2_core/include/s2/types.hpp (добавить Signal struct в конец namespace s2):
```cpp
// Существующие типы: AgentId, ActorId, ObjectId, ZoneId, EntityId
// Vec3 = Eigen::Vector3d
// Pose3D { x,y,z,roll,pitch,yaw }
// Velocity, DesiredVelocity, CollisionShape, VisualDesc, ZoneShape, EffectType, ActorState
```

Из workspace/s2_core/include/s2/sim_bus.hpp (SimBus — шаблонная шина):
```cpp
class SimBus {
  template<typename EventT>
  void subscribe(std::function<void(const EventT&)> handler);
  template<typename EventT>
  void publish(const EventT& event);
  template<typename EventT>
  std::size_t subscriber_count() const;
  std::size_t event_type_count() const;
private:
  std::unordered_map<std::type_index,
                     std::vector<std::function<void(const std::any&)>>> handlers_;
};

namespace event {
  struct AgentEnteredZone { AgentId agent; ZoneId zone; };
  struct AgentExitedZone  { AgentId agent; ZoneId zone; };
  struct ObjectAttached   { ObjectId obj; AgentId agent; std::string link; };
  struct ObjectReleased   { ObjectId obj; AgentId agent; };
  struct ActorStateChanged{ ActorId actor; ActorState old_state; ActorState new_state; };
  struct AgentCollision   { AgentId agent; Vec3 point; };
  struct TeleportAgentCommand { AgentId agent_id; Vec3 destination; double yaw; };
  struct SetZoneTeleportTargetCommand { ZoneId zone_id; Vec3 destination; double yaw; };
}
```

Из workspace/s2_core/include/s2/agent.hpp (добавить signals):
```cpp
struct Agent {
  AgentId id{0};
  std::string name;
  int domain_id{0};
  Pose3D world_pose;
  Velocity world_velocity;
  SharedState state;
  std::unordered_set<std::string> capabilities;
  std::vector<std::unique_ptr<plugins::IAgentPlugin>> plugins;
  bool has_collision{false};
  double max_slope_rad{0.0};
  double max_step_height{0.0};
  CollisionShape bounding;
  VisualDesc visual;
  std::unique_ptr<KinematicTree> kinematic_tree;
  // ДОБАВИТЬ: std::vector<Signal> signals;
};
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signal struct в types.hpp + поле Agent::signals</name>
  <files>
    workspace/s2_core/include/s2/types.hpp
    workspace/s2_core/include/s2/agent.hpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/types.hpp — прочитать полностью, добавить Signal struct в конец namespace s2 (после ActorState)
    - workspace/s2_core/include/s2/agent.hpp — прочитать полностью, добавить поле signals
    - .planning/phases/00-core-architecture-foundation/00-CONTEXT.md §D-15, D-16, D-17 — точные определения
  </read_first>
  <behavior>
    - Signal struct содержит ровно поля из D-15: signal_type(string), signal_id(string), local_pose(Pose3D), params(nlohmann::json), range(double{0.0}), requires_los(bool{false}), enabled(bool{true})
    - Agent::signals — std::vector&lt;Signal&gt; signals (пустой по умолчанию), добавить после поля visual перед kinematic_tree
    - wire = Signal{range = std::numeric_limits&lt;double&gt;::infinity(), requires_los = false} — это конвенция использования, не отдельный тип, прокомментировать в types.hpp
  </behavior>
  <action>
В types.hpp добавить в конце namespace s2 (после ActorState, до закрывающей скобки):
```cpp
// ============================================================================
// Сигналы
// ============================================================================

/**
 * @brief Обнаруживаемый сигнал, прикреплённый к Entity.
 *
 * Используется детекторами (ArucoDetector, SignalListener) через WorldQuery::find_signals_of_type().
 *
 * wire-сигнал — это Signal с range = std::numeric_limits<double>::infinity()
 * и requires_los = false. Он "достигает" везде без трассировки луча.
 *
 * Пример YAML:
 *   signals:
 *     - signal_type: aruco
 *       signal_id: marker_42
 *       range: 5.0
 *       requires_los: true
 *       params: {size: 0.2}
 */
struct Signal
{
  std::string signal_type;      ///< Тип сигнала: "aruco", "wire", "rfid", ...
  std::string signal_id;        ///< Уникальный идентификатор экземпляра сигнала
  Pose3D      local_pose;       ///< Смещение от origin Entity (в локальных координатах)
  nlohmann::json params;        ///< Произвольные параметры (размер маркера, мощность и т.п.)
  double      range{0.0};       ///< Дальность обнаружения (0 = нет ограничений не по умолчанию)
  bool        requires_los{false}; ///< Требует ли прямой видимости (line-of-sight)
  bool        enabled{true};    ///< Активен ли сигнал
};
```

Добавить в types.hpp include перед остальными:
```cpp
#include <nlohmann/json.hpp>
```
(если его ещё нет в файле)

В agent.hpp добавить поле после поля `VisualDesc visual;` и перед `std::unique_ptr<KinematicTree> kinematic_tree;`:
```cpp
  std::vector<Signal> signals;  ///< Обнаруживаемые сигналы агента (ArUco, wire, RFID и т.п.)
```

Также добавить include в agent.hpp если Signal не виден через transitive includes:
```cpp
// Signal уже определён в types.hpp который уже включён через цепочку
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <done>
    - grep -n "struct Signal" workspace/s2_core/include/s2/types.hpp выдаёт строку с определением
    - grep -n "signal_type" workspace/s2_core/include/s2/types.hpp выдаёт строку поля
    - grep -n "std::vector&lt;Signal&gt; signals" workspace/s2_core/include/s2/agent.hpp выдаёт строку
    - Docker tests проходят без ошибок компиляции
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventBus (rename + новые event-типы) + backward-compat в sim_bus.hpp</name>
  <files>
    workspace/s2_core/include/s2/event_bus.hpp
    workspace/s2_core/include/s2/sim_bus.hpp
    workspace/s2_core/tests/test_event_bus.cpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/sim_bus.hpp — прочитать полностью, это источник для event_bus.hpp
    - workspace/s2_core/tests/test_sim_bus.cpp — прочитать для понимания стиля тестов
    - .planning/phases/00-core-architecture-foundation/00-CONTEXT.md §D-07, D-08 — точные требования
    - RESULT_DISCUSS.md §15.2 — полный список event-типов
  </read_first>
  <behavior>
    - event_bus.hpp создать как копию sim_bus.hpp с переименованием SimBus → EventBus
    - Добавить все новые event-типы из D-08 в namespace event
    - Старые AgentEnteredZone/AgentExitedZone остаются для backward compat (не удалять)
    - Добавить новые ZoneEntered/ZoneExited как алиасы или отдельные structs с EntityId
    - sim_bus.hpp переписать: включить event_bus.hpp и добавить using SimBus = EventBus
    - Тест test_event_bus.cpp проверяет все новые event-типы
  </behavior>
  <action>
Создать workspace/s2_core/include/s2/event_bus.hpp:

```cpp
#pragma once
/**
 * @file event_bus.hpp
 * EventBus — типизированная шина событий (переименован из SimBus).
 *
 * Содержит все стандартные event-типы S2.
 * Принцип работы идентичен SimBus — synchronous dispatch через std::any.
 */

#include <s2/types.hpp>

#include <any>
#include <functional>
#include <string>
#include <typeindex>
#include <unordered_map>
#include <vector>

namespace s2
{

// ============================================================================
// Стандартные события
// ============================================================================

namespace event
{

// ─── Зоны ────────────────────────────────────────────────────────────────────

/** @brief Агент вошёл в зону (legacy — backward compat). */
struct AgentEnteredZone { AgentId agent; ZoneId zone; };

/** @brief Агент вышел из зоны (legacy — backward compat). */
struct AgentExitedZone  { AgentId agent; ZoneId zone; };

/** @brief Entity вошла в зону. Публикуется ZoneSystem. */
struct ZoneEntered { ZoneId zone_id; EntityId entity_id; };

/** @brief Entity вышла из зоны. Публикуется ZoneSystem. */
struct ZoneExited  { ZoneId zone_id; EntityId entity_id; };

// ─── Сущности ─────────────────────────────────────────────────────────────────

/** @brief Entity создана. Публикуется ядром при SpawnEntity. */
struct EntitySpawned { EntityId id; std::string entity_type; };

/** @brief Entity удалена. Публикуется ядром при DespawnEntity. */
struct EntityDespawned { EntityId id; };

// ─── Акторы ──────────────────────────────────────────────────────────────────

/** @brief Состояние актора изменилось (FSM-переход). */
struct ActorStateChanged { ActorId actor; ActorState old_state; ActorState new_state; };

// ─── Сигналы ─────────────────────────────────────────────────────────────────

/** @brief Сигнал активирован (wire triggered, button pressed и т.п.). */
struct SignalActivated   { std::string signal_id; EntityId source_entity; };

/** @brief Сигнал деактивирован. */
struct SignalDeactivated { std::string signal_id; EntityId source_entity; };

// ─── Взаимодействия ──────────────────────────────────────────────────────────

/** @brief Попытка захватить объект. Публикуется GrabberPlugin перед командой. */
struct GrabAttempt   { EntityId agent; EntityId target; };

/** @brief Захват успешен. Публикуется ядром после AttachObject. */
struct GrabSucceeded { EntityId agent; EntityId target; };

/** @brief Захват не удался. Публикуется ядром при отказе AttachObject. */
struct GrabFailed    { EntityId agent; EntityId target; std::string reason; };

// ─── Урон ────────────────────────────────────────────────────────────────────

/** @brief Нанесён урон сущности. */
struct DamageDealt { EntityId source; EntityId target; double amount; std::string damage_type; };

// ─── Объекты (legacy) ────────────────────────────────────────────────────────

/** @brief Объект привязан к агенту (legacy). */
struct ObjectAttached  { ObjectId obj; AgentId agent; std::string link; };

/** @brief Объект отпущен (legacy). */
struct ObjectReleased  { ObjectId obj; AgentId agent; };

/** @brief Столкновение агента (legacy). */
struct AgentCollision  { AgentId agent; Vec3 point; };

/** @brief Команда телепортации (legacy). */
struct TeleportAgentCommand { AgentId agent_id; Vec3 destination; double yaw{0.0}; };

/** @brief Установка runtime-цели телепорт-зоны (legacy). */
struct SetZoneTeleportTargetCommand { ZoneId zone_id; Vec3 destination; double yaw{0.0}; };

}  // namespace event

// ============================================================================
// EventBus
// ============================================================================

/**
 * @brief Типизированная шина событий.
 *
 * Механизм publish/subscribe для синхронной коммуникации между модулями.
 * Dispatch синхронный — подписчики вызываются прямо в publish().
 *
 * Не потокобезопасен (только симуляционный поток).
 */
class EventBus
{
public:
  EventBus() = default;

  /**
   * @brief Подписаться на событие типа EventT.
   * @tparam EventT Тип события
   * @param handler void(const EventT&)
   */
  template <typename EventT>
  void subscribe(std::function<void(const EventT&)> handler)
  {
    auto wrapper = [handler](const std::any& event) {
      handler(std::any_cast<const EventT&>(event));
    };
    handlers_[typeid(EventT)].push_back(std::move(wrapper));
  }

  /**
   * @brief Опубликовать событие — синхронно вызвать всех подписчиков.
   * @tparam EventT Тип события (выводится автоматически)
   */
  template <typename EventT>
  void publish(const EventT& event)
  {
    auto it = handlers_.find(typeid(EventT));
    if (it == handlers_.end())
      return;
    for (const auto& handler : it->second)
      handler(event);
  }

  /** @brief Количество подписчиков на тип EventT. */
  template <typename EventT>
  std::size_t subscriber_count() const
  {
    auto it = handlers_.find(typeid(EventT));
    if (it == handlers_.end()) return 0;
    return it->second.size();
  }

  /** @brief Количество зарегистрированных типов событий. */
  std::size_t event_type_count() const { return handlers_.size(); }

private:
  std::unordered_map<std::type_index,
                     std::vector<std::function<void(const std::any&)>>>
      handlers_;
};

}  // namespace s2
```

Затем переписать sim_bus.hpp (сохранив только backward-compat содержимое):
```cpp
#pragma once
/**
 * @file sim_bus.hpp
 * Backward-compatibility header: SimBus = EventBus.
 *
 * Весь код, включающий sim_bus.hpp, продолжает работать без изменений.
 * Новый код должен использовать event_bus.hpp напрямую.
 */
#include <s2/event_bus.hpp>

namespace s2
{
  // Алиас для backward compatibility
  using SimBus = EventBus;
}  // namespace s2
```

Создать workspace/s2_core/tests/test_event_bus.cpp:
```cpp
/**
 * @file test_event_bus.cpp
 * Тесты для EventBus: новые event-типы (EntitySpawned, SignalActivated и т.п.).
 *
 * Тесты для старых event-типов (AgentEnteredZone и т.п.) остаются в test_sim_bus.cpp.
 */

#include <s2/event_bus.hpp>
#include <gtest/gtest.h>
#include <string>
#include <vector>

namespace s2
{

TEST(EventBus, EntitySpawnedDelivered)
{
  EventBus bus;
  EntityId received_id = 0;
  std::string received_type;

  bus.subscribe<event::EntitySpawned>(
      [&](const event::EntitySpawned& e) {
        received_id   = e.id;
        received_type = e.entity_type;
      });

  bus.publish(event::EntitySpawned{.id = 42, .entity_type = "agent"});
  EXPECT_EQ(received_id, 42u);
  EXPECT_EQ(received_type, "agent");
}

TEST(EventBus, EntityDespawnedDelivered)
{
  EventBus bus;
  EntityId received = 0;
  bus.subscribe<event::EntityDespawned>(
      [&](const event::EntityDespawned& e) { received = e.id; });
  bus.publish(event::EntityDespawned{.id = 7});
  EXPECT_EQ(received, 7u);
}

TEST(EventBus, SignalActivatedDelivered)
{
  EventBus bus;
  std::string received_id;
  EntityId received_source = 0;

  bus.subscribe<event::SignalActivated>(
      [&](const event::SignalActivated& e) {
        received_id     = e.signal_id;
        received_source = e.source_entity;
      });

  bus.publish(event::SignalActivated{.signal_id = "wire_01", .source_entity = 3});
  EXPECT_EQ(received_id, "wire_01");
  EXPECT_EQ(received_source, 3u);
}

TEST(EventBus, SignalDeactivatedDelivered)
{
  EventBus bus;
  bool called = false;
  bus.subscribe<event::SignalDeactivated>(
      [&](const event::SignalDeactivated&) { called = true; });
  bus.publish(event::SignalDeactivated{.signal_id = "wire_01", .source_entity = 1});
  EXPECT_TRUE(called);
}

TEST(EventBus, ZoneEnteredDelivered)
{
  EventBus bus;
  ZoneId received_zone;
  EntityId received_entity = 0;

  bus.subscribe<event::ZoneEntered>(
      [&](const event::ZoneEntered& e) {
        received_zone   = e.zone_id;
        received_entity = e.entity_id;
      });

  bus.publish(event::ZoneEntered{.zone_id = "ice_zone", .entity_id = 5});
  EXPECT_EQ(received_zone, "ice_zone");
  EXPECT_EQ(received_entity, 5u);
}

TEST(EventBus, ZoneExitedDelivered)
{
  EventBus bus;
  bool called = false;
  bus.subscribe<event::ZoneExited>(
      [&](const event::ZoneExited&) { called = true; });
  bus.publish(event::ZoneExited{.zone_id = "ice_zone", .entity_id = 5});
  EXPECT_TRUE(called);
}

TEST(EventBus, GrabAttemptSucceededFailed)
{
  EventBus bus;
  std::vector<std::string> log;

  bus.subscribe<event::GrabAttempt>(
      [&](const event::GrabAttempt&)   { log.push_back("attempt"); });
  bus.subscribe<event::GrabSucceeded>(
      [&](const event::GrabSucceeded&) { log.push_back("succeeded"); });
  bus.subscribe<event::GrabFailed>(
      [&](const event::GrabFailed& e)  { log.push_back("failed:" + e.reason); });

  bus.publish(event::GrabAttempt{.agent = 1, .target = 2});
  bus.publish(event::GrabSucceeded{.agent = 1, .target = 2});
  bus.publish(event::GrabFailed{.agent = 1, .target = 3, .reason = "too_far"});

  ASSERT_EQ(log.size(), 3u);
  EXPECT_EQ(log[0], "attempt");
  EXPECT_EQ(log[1], "succeeded");
  EXPECT_EQ(log[2], "failed:too_far");
}

TEST(EventBus, DamageDealtDelivered)
{
  EventBus bus;
  double received_amount = 0.0;
  std::string received_type;

  bus.subscribe<event::DamageDealt>(
      [&](const event::DamageDealt& e) {
        received_amount = e.amount;
        received_type   = e.damage_type;
      });

  bus.publish(event::DamageDealt{.source = 1, .target = 2, .amount = 42.5, .damage_type = "fire"});
  EXPECT_NEAR(received_amount, 42.5, 1e-9);
  EXPECT_EQ(received_type, "fire");
}

TEST(EventBus, SimBusAliasWorksAsEventBus)
{
  // Тест backward compat: SimBus через using-алиас
  SimBus bus;  // SimBus = EventBus
  bool called = false;

  bus.subscribe<event::EntitySpawned>(
      [&](const event::EntitySpawned&) { called = true; });
  bus.publish(event::EntitySpawned{.id = 1, .entity_type = "agent"});
  EXPECT_TRUE(called);
}

}  // namespace s2
```

Убедиться, что test_sim_bus.cpp не нужно менять — он включает sim_bus.hpp → backward compat.
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -40</automated>
  </verify>
  <done>
    - grep -rn "class EventBus" workspace/s2_core/include/s2/event_bus.hpp выдаёт строку
    - grep -n "using SimBus = EventBus" workspace/s2_core/include/s2/sim_bus.hpp выдаёт строку
    - grep -n "struct EntitySpawned" workspace/s2_core/include/s2/event_bus.hpp выдаёт строку
    - grep -n "struct ZoneEntered" workspace/s2_core/include/s2/event_bus.hpp выдаёт строку (не AgentEnteredZone)
    - grep -n "struct GrabAttempt" workspace/s2_core/include/s2/event_bus.hpp выдаёт строку
    - grep -n "struct DamageDealt" workspace/s2_core/include/s2/event_bus.hpp выдаёт строку
    - test_sim_bus.cpp компилируется без изменений (все старые AgentEnteredZone тесты зелёные)
    - test_event_bus.cpp все тесты зелёные
    - Docker tests: все 28+ тестов проходят
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → EventBus | Плагины публикуют события из симуляционного потока. Нет внешнего входа. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | EventBus handlers_ | accept | EventBus не потокобезопасен по дизайну — вызывается только в sim-потоке; явно задокументировано в комментарии |
| T-00-02 | Denial of Service | EventBus::publish | accept | Синхронный dispatch — если handler бросает исключение, оно пробрасывается; приемлемо для внутреннего API |
</threat_model>

<verification>
После выполнения обеих задач:

1. `grep -rn "struct Signal" workspace/s2_core/include/s2/types.hpp` — находит определение
2. `grep -rn "class EventBus" workspace/s2_core/include/s2/event_bus.hpp` — находит класс
3. `grep -rn "using SimBus = EventBus" workspace/s2_core/include/s2/sim_bus.hpp` — находит алиас
4. `grep -rn "std::vector<Signal> signals" workspace/s2_core/include/s2/agent.hpp` — находит поле
5. `docker compose --project-directory docker up --build tests` — все тесты зелёные
6. Файл test_sim_bus.cpp НЕ изменён — существующие тесты работают через backward compat
</verification>

<success_criteria>
- Signal struct присутствует в types.hpp с полями из D-15 (signal_type, signal_id, local_pose, params, range, requires_los, enabled)
- Agent::signals поле добавлено в agent.hpp
- event_bus.hpp создан с class EventBus и 11 новыми event-типами (EntitySpawned, EntityDespawned, ZoneEntered, ZoneExited, SignalActivated, SignalDeactivated, GrabAttempt, GrabSucceeded, GrabFailed, DamageDealt + ActorStateChanged уже был)
- sim_bus.hpp сохраняет все старые event-типы через backward compat, using SimBus = EventBus
- Все 28+ существующих тестов проходят без изменений
- Новые тесты test_event_bus.cpp проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/00-core-architecture-foundation/00-01-SUMMARY.md`
</output>
