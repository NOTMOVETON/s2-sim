---
phase: "00-core-architecture-foundation"
plan: "02"
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - workspace/s2_core/include/s2/plugin_base.hpp
  - workspace/s2_core/include/s2/world_query.hpp
  - workspace/s2_core/tests/test_plugin_lifecycle.cpp
  - workspace/s2_core/tests/test_plugin_roles.cpp
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-05

must_haves:
  truths:
    - "IAgentPlugin содержит виртуальные методы on_spawn(Agent&), on_despawn(Agent&), on_scene_load(const SimWorld&), on_reset(Agent&) с пустой default-реализацией"
    - "IAgentPlugin содержит виртуальный метод virtual PluginRole role() const = 0"
    - "enum class PluginRole { ACTUATION, SENSOR, INTERACTION, RESOURCE, UTILITY } определён в plugin_base.hpp"
    - "provided_capabilities() возвращает std::vector<std::string>"
    - "update() сигнатура изменена на update(double dt, Agent& agent, const PluginContext& ctx)"
    - "PluginContext struct определён: const WorldQuery& world; EventBus& bus; KernelCommandQueue& commands"
    - "class WorldQuery создан в world_query.hpp с методами из §15.3 RESULT_DISCUSS.md"
    - "using KernelCommandQueue = std::vector<KernelCommand> объявлен (forward или полный)"
    - "Docker: docker compose tests проходит"
  artifacts:
    - path: "workspace/s2_core/include/s2/plugin_base.hpp"
      provides: "IAgentPlugin расширенный интерфейс"
      contains: "on_spawn"
    - path: "workspace/s2_core/include/s2/plugin_base.hpp"
      provides: "PluginRole enum + PluginContext struct"
      contains: "PluginRole"
    - path: "workspace/s2_core/include/s2/world_query.hpp"
      provides: "WorldQuery read-only API"
      contains: "class WorldQuery"
    - path: "workspace/s2_core/tests/test_plugin_lifecycle.cpp"
      provides: "тесты новых lifecycle-методов"
      contains: "on_spawn"
    - path: "workspace/s2_core/tests/test_plugin_roles.cpp"
      provides: "тесты PluginRole"
      contains: "PluginRole::ACTUATION"
  key_links:
    - from: "workspace/s2_core/include/s2/plugin_base.hpp"
      to: "workspace/s2_core/include/s2/world_query.hpp"
      via: "PluginContext::world"
      pattern: "WorldQuery"
    - from: "workspace/s2_core/include/s2/plugin_base.hpp"
      to: "workspace/s2_core/include/s2/event_bus.hpp"
      via: "PluginContext::bus"
      pattern: "EventBus"
---

<objective>
Расширяем IAgentPlugin: новый lifecycle (on_spawn/on_despawn/on_scene_load/on_reset), система ролей (PluginRole), изменённая сигнатура update(). Создаём WorldQuery — read-only API для плагинов.

Это Wave 2 — зависит от Plan 01 (EventBus уже существует для PluginContext::bus).

Purpose:
- on_spawn/on_despawn нужны Phase 2+ (акторы, props)
- PluginRole нужен для валидации (ACTUATION = max 1 на агента)
- update(ctx) открывает путь к WorldQuery и EventBus из плагинов
- WorldQuery нужен всем Sensor/Interaction плагинам начиная с Phase 2

Output:
- plugin_base.hpp: полный расширенный интерфейс
- world_query.hpp: class WorldQuery (заглушки методов с корректными сигнатурами)
- тесты lifecycle и ролей
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/PROJECT.md
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/00-core-architecture-foundation/00-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/RESULT_DISCUSS.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/00-core-architecture-foundation/00-01-SUMMARY.md
</context>

<interfaces>
<!-- Текущие сигнатуры из plugin_base.hpp (executor ДОЛЖЕН прочитать файл перед изменением). -->

Существующий IAgentPlugin имеет:
```cpp
// СУЩЕСТВУЮЩИЕ методы (сохранить):
virtual std::string type() const = 0;
virtual void initialize(Agent& agent) {}
virtual void pre_resolve(double dt, Agent& agent) {}               // D-02: сигнатура НЕ меняется
virtual void set_collision_system(const CollisionSystem*) {}
virtual void set_raycast_engine(const RaycastEngine*) {}
virtual void update(double dt, Agent& agent) = 0;                  // ИЗМЕНИТЬ сигнатуру
virtual void from_config(const YAML::Node& node) = 0;
virtual std::string to_json() const = 0;
virtual void contribute_snapshot(nlohmann::json&, const Agent&) const {}
virtual std::string display_label() const { return type(); }
virtual std::string config_schema() const { return "[]"; }         // D-14: уже есть
virtual bool has_inputs() const { return false; }
virtual std::string inputs_schema() const { return ""; }
virtual void handle_input(const std::string& json_input) {}
virtual std::vector<std::string> command_topics() const { return {}; }
virtual std::vector<std::string> service_names() const { return {}; }
virtual std::string handle_service(...) { return "..."; }
virtual std::vector<s2::TransportEvent> poll_events() { return {}; }
virtual std::vector<std::string> subscribe_topics() const { return {}; }
virtual std::string subscription_msg_type() const { return "nav_msgs/Path"; }
virtual void handle_subscription(...) {}
void set_mount_pose(const Pose3D& pose) {}
std::optional<s2::FrameTransform> mount_frame() const {}
```

Существующий SimWorld (для on_scene_load):
```cpp
// workspace/s2_core/include/s2/world.hpp
class SimWorld {
  std::vector<Agent>& agents();
  std::vector<Actor>& actors();
  std::vector<Prop>& props();
  ...
};
```

Из RESULT_DISCUSS.md §15.3 — полный интерфейс WorldQuery:
```cpp
class WorldQuery {
public:
    std::vector<EntityId> find_in_radius(Vec3 center, double r, EntityFilter f);
    std::vector<EntityId> find_in_box(Box b, EntityFilter f);
    std::optional<EntityId> find_nearest(Vec3 pos, EntityFilter f);
    EntityId find_entity_below(Vec3 position);
    std::vector<Signal> find_signals_of_type(std::string type, Vec3 pos, DetectionVolume vol);
    bool has_line_of_sight(EntityId from, EntityId to);
    RaycastResult raycast(Vec3 origin, Vec3 dir, double max_range);
    std::vector<ZoneId> zones_at(Vec3 position);
    bool is_in_zone(EntityId id, ZoneId zone);
    std::optional<EntityId> find_deformable_in_box(Box b, EntityFilter f);
};
```

Из .planning/phases/00-core-architecture-foundation/00-CONTEXT.md:
- D-01: PluginContext { const WorldQuery& world; EventBus& bus; KernelCommandQueue& commands; }
- D-05: using KernelCommandQueue = std::vector&lt;KernelCommand&gt;
- D-09: enum class PluginRole { ACTUATION, SENSOR, INTERACTION, RESOURCE, UTILITY }
- D-10: ACTUATION = пишет в agent.velocity; JointVelPlugin = UTILITY
- D-12: on_spawn/on_despawn/on_scene_load/on_reset — все с пустой default-реализацией
- D-14: provided_capabilities() → std::vector&lt;std::string&gt;
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WorldQuery header — read-only API для плагинов</name>
  <files>
    workspace/s2_core/include/s2/world_query.hpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/types.hpp — все базовые типы (Vec3, Pose3D, EntityId, ZoneId, Signal)
    - workspace/s2_core/include/s2/raycast_engine.hpp — RaycastResult тип
    - workspace/s2_core/include/s2/zone_system.hpp — для zones_at
    - RESULT_DISCUSS.md §15.3 — точные сигнатуры методов WorldQuery
  </read_first>
  <behavior>
    - WorldQuery — интерфейсный класс (не абстрактный, конкретный с заглушками), реализация будет в Plan 05
    - В Phase 0 создаём только header с корректными сигнатурами и null-реализациями
    - EntityFilter, DetectionVolume, Box — вспомогательные struct'ы, определить в том же файле
    - RaycastResult взять из raycast_engine.hpp если уже есть, иначе определить
  </behavior>
  <action>
Прочитать workspace/s2_core/include/s2/raycast_engine.hpp чтобы найти RaycastResult.

Создать workspace/s2_core/include/s2/world_query.hpp:
```cpp
#pragma once
/**
 * @file world_query.hpp
 * WorldQuery — read-only API для плагинов и поведений.
 *
 * Позволяет плагинам запрашивать состояние мира без прямого доступа к SimWorld.
 * Передаётся через PluginContext в update() каждого плагина.
 *
 * Архитектурный принцип: никаких записей через WorldQuery. Только чтение.
 * Изменения мира — только через KernelCommand.
 *
 * Реализация: WorldQueryImpl в sim_engine.cpp (Plan 05).
 * В Phase 0 — заглушки (возвращают пустые результаты).
 */

#include <s2/types.hpp>

#include <optional>
#include <string>
#include <vector>

namespace s2
{

// ============================================================================
// Вспомогательные типы для WorldQuery
// ============================================================================

/**
 * @brief Фильтр Entity для поиска.
 * Позволяет ограничивать поиск по типу, тегам, capabilities.
 */
struct EntityFilter
{
  bool include_agents{true};   ///< Включать ли агентов
  bool include_actors{true};   ///< Включать ли акторов
  bool include_props{true};    ///< Включать ли пропы
  std::vector<std::string> required_capabilities; ///< Все перечисленные должны присутствовать
  std::vector<std::string> required_tags;         ///< Все теги должны присутствовать
  EntityId exclude_self{0};    ///< Исключить эту Entity из результата (0 = не исключать)

  // Фабричные методы для удобства
  static EntityFilter agents_only()
  {
    return EntityFilter{.include_agents = true, .include_actors = false, .include_props = false};
  }
  static EntityFilter actors_only()
  {
    return EntityFilter{.include_agents = false, .include_actors = true, .include_props = false};
  }
  static EntityFilter all() { return EntityFilter{}; }
};

/**
 * @brief Объём детекции для поиска сигналов.
 */
struct DetectionVolume
{
  enum class Shape { SPHERE, CONE, BOX };
  Shape shape{Shape::SPHERE};
  double range{10.0};          ///< Дальность (для SPHERE и CONE)
  double fov_rad{1.5708};      ///< Угол конуса (для CONE, ~90°)
  Vec3   direction{1, 0, 0};   ///< Направление конуса в мировых координатах
};

/**
 * @brief AABB-бокс для поиска.
 */
struct Box
{
  Vec3 center;
  Vec3 half_extents{1.0, 1.0, 1.0};
};

/**
 * @brief Результат raycast.
 * hit == true означает, что луч нашёл пересечение.
 */
struct RaycastQueryResult
{
  bool        hit{false};
  double      distance{0.0};   ///< Расстояние от origin до точки попадания
  Vec3        point;           ///< Точка пересечения в мировых координатах
  Vec3        normal;          ///< Нормаль поверхности в точке пересечения
  EntityId    entity_id{0};    ///< Entity, с которой произошло пересечение (0 = геометрия)
};

// ============================================================================
// WorldQuery — read-only API
// ============================================================================

/**
 * @brief Read-only API ядра для плагинов и поведений.
 *
 * Предоставляется плагинам через PluginContext::world.
 * Базовый класс: заглушки. Переопределяется WorldQueryImpl в SimEngine (Plan 05).
 *
 * Использование:
 * @code
 * void MyPlugin::update(double dt, Agent& agent, const PluginContext& ctx) {
 *     auto nearby = ctx.world.find_in_radius(agent.world_pose.position(), 5.0,
 *                                            EntityFilter::agents_only());
 * }
 * @endcode
 */
class WorldQuery
{
public:
  virtual ~WorldQuery() = default;

  // ─── Поиск Entity ──────────────────────────────────────────────────────────

  /**
   * @brief Найти все Entity в радиусе от точки.
   * @param center Центр поиска (мировые координаты)
   * @param r      Радиус поиска (метры)
   * @param filter Фильтр по типу/тегам/capabilities
   * @return Список EntityId в радиусе (не отсортирован)
   */
  virtual std::vector<EntityId> find_in_radius(Vec3 center, double r, EntityFilter filter) const
  {
    (void)center; (void)r; (void)filter;
    return {};
  }

  /**
   * @brief Найти все Entity в AABB-боксе.
   */
  virtual std::vector<EntityId> find_in_box(Box b, EntityFilter filter) const
  {
    (void)b; (void)filter;
    return {};
  }

  /**
   * @brief Найти ближайшую Entity к точке.
   * @return EntityId или nullopt если ничего не найдено
   */
  virtual std::optional<EntityId> find_nearest(Vec3 pos, EntityFilter filter) const
  {
    (void)pos; (void)filter;
    return std::nullopt;
  }

  /**
   * @brief Найти Entity непосредственно под точкой (вертикальный raycast вниз).
   * @return EntityId или 0 если ничего не найдено
   */
  virtual EntityId find_entity_below(Vec3 position) const
  {
    (void)position;
    return 0;
  }

  // ─── Сигналы ───────────────────────────────────────────────────────────────

  /**
   * @brief Найти все сигналы заданного типа в объёме детекции.
   * @param signal_type Тип сигнала: "aruco", "wire", "rfid", ...
   * @param pos         Позиция детектора (мировые координаты)
   * @param volume      Параметры объёма детекции
   * @return Список сигналов (с учётом requires_los если DetectionVolume поддерживает)
   */
  virtual std::vector<Signal> find_signals_of_type(
      const std::string& signal_type, Vec3 pos, const DetectionVolume& volume) const
  {
    (void)signal_type; (void)pos; (void)volume;
    return {};
  }

  // ─── Геометрия ─────────────────────────────────────────────────────────────

  /**
   * @brief Проверить наличие прямой видимости между двумя Entity.
   * Луч из центра from к центру to. Возвращает true если нет препятствий.
   */
  virtual bool has_line_of_sight(EntityId from, EntityId to) const
  {
    (void)from; (void)to;
    return true;  // Заглушка: всегда видимость есть
  }

  /**
   * @brief Бросить луч из точки в направлении.
   * @param origin    Начало луча (мировые координаты)
   * @param dir       Направление луча (единичный вектор)
   * @param max_range Максимальная дальность
   * @return Результат: hit=false если ничего не нашёл
   */
  virtual RaycastQueryResult raycast(Vec3 origin, Vec3 dir, double max_range) const
  {
    (void)origin; (void)dir; (void)max_range;
    return RaycastQueryResult{};
  }

  // ─── Зоны ──────────────────────────────────────────────────────────────────

  /**
   * @brief Получить список зон, содержащих точку.
   * @param position Точка в мировых координатах
   * @return Список ZoneId зон, которые содержат position
   */
  virtual std::vector<ZoneId> zones_at(Vec3 position) const
  {
    (void)position;
    return {};
  }

  /**
   * @brief Проверить, находится ли Entity в конкретной зоне.
   */
  virtual bool is_in_zone(EntityId entity_id, const ZoneId& zone_id) const
  {
    (void)entity_id; (void)zone_id;
    return false;
  }

  // ─── Деформируемые объекты ─────────────────────────────────────────────────

  /**
   * @brief Найти деформируемую Entity в боксе.
   * Используется MaterialTransfer / DeformEntity KernelCommand.
   */
  virtual std::optional<EntityId> find_deformable_in_box(Box b, EntityFilter filter) const
  {
    (void)b; (void)filter;
    return std::nullopt;
  }
};

}  // namespace s2
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - grep -n "class WorldQuery" workspace/s2_core/include/s2/world_query.hpp выдаёт строку
    - grep -n "find_in_radius" workspace/s2_core/include/s2/world_query.hpp выдаёт строку
    - grep -n "find_signals_of_type" workspace/s2_core/include/s2/world_query.hpp выдаёт строку
    - grep -n "has_line_of_sight" workspace/s2_core/include/s2/world_query.hpp выдаёт строку
    - Docker tests компилируются без ошибок
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Расширить IAgentPlugin — lifecycle, PluginRole, PluginContext, update() сигнатура</name>
  <files>
    workspace/s2_core/include/s2/plugin_base.hpp
    workspace/s2_core/tests/test_plugin_lifecycle.cpp
    workspace/s2_core/tests/test_plugin_roles.cpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/plugin_base.hpp — прочитать полностью. Это файл, который изменяем.
    - workspace/s2_core/include/s2/world_query.hpp — только что создан в Task 1
    - workspace/s2_core/include/s2/event_bus.hpp — создан в Plan 01
    - workspace/s2_core/include/s2/world.hpp — нужен для on_scene_load(const SimWorld&)
    - .planning/phases/00-core-architecture-foundation/00-CONTEXT.md §D-01 до D-14
    - workspace/s2_core/tests/test_sim_engine.cpp — прочитать для понимания стиля тестов
  </read_first>
  <behavior>
    - update() сигнатура меняется: virtual void update(double dt, Agent& agent, const PluginContext& ctx) = 0
    - КритичнО: все существующие плагины переопределяют update(dt, agent) — они сломаются. Решение: в base class добавить невиртуальный dispatcher, который вызывает новую виртуальную форму с NullContext для обратной совместимости — НЕТ, это усложнение. Правильное решение: Plan 06 обновит все плагины. В Plan 02 мы объявляем новый интерфейс и создаём NullWorldQuery-заглушку для SimEngine пока не готов Plan 05.
    - Стратегия backward compat: поменять virtual update() = 0 на новую сигнатуру. Плагины получат compile error — это нормально, они обновятся в Plan 06.
    - PluginContext определяется в plugin_base.hpp, не в отдельном файле
    - KernelCommandQueue: пока forward-объявлена как using KernelCommandQueue = std::vector&lt;KernelCommand&gt;. KernelCommand будет определён в Plan 04. Для сейчас: использовать nlohmann::json как placeholder или объявить using KernelCommandQueue = std::vector&lt;nlohmann::json&gt; с TODO. Нет — правильнее объявить forward struct KernelCommand и using прямо здесь.
    - Решение для KernelCommandQueue: определить пустую struct KernelCommand forward в plugin_base.hpp, а полное определение добавим в Plan 04 в kernel_command.hpp. PluginContext использует std::vector&lt;KernelCommand&gt;&.
  </behavior>
  <action>
ВАЖНО: Прочитать workspace/s2_core/include/s2/plugin_base.hpp полностью перед редактированием.

Изменить plugin_base.hpp следующим образом:

1. Добавить includes в начало файла:
```cpp
#include <s2/event_bus.hpp>
#include <s2/world_query.hpp>
```

2. Добавить forward-объявление KernelCommand и type alias перед классом IAgentPlugin:
```cpp
// Forward declaration — полное определение в kernel_command.hpp (Plan 04)
struct KernelCommand;
/// Очередь команд ядра — однотиковый локальный буфер плагина.
/// Команды применяются в Phase 0 следующего тика SimEngine.
using KernelCommandQueue = std::vector<KernelCommand>;
```

3. Добавить PluginRole enum и PluginContext struct перед классом IAgentPlugin:
```cpp
/**
 * @brief Роль плагина агента в системе.
 *
 * Определяет к каким ресурсам плагин имеет право доступа:
 *  ACTUATION   — читает SharedState, пишет agent.velocity (движение тела)
 *  SENSOR      — читает WorldQuery, публикует данные на транспорт
 *  INTERACTION — читает WorldQuery, публикует KernelCommand и EventBus
 *  RESOURCE    — пишет в SharedState (battery, payload)
 *  UTILITY     — вспомогательные действия (joints, trajectory recorder)
 *
 * Ограничение: у агента максимум один ACTUATION-плагин (валидируется при загрузке).
 */
enum class PluginRole
{
  ACTUATION,    ///< Пишет в agent.velocity (DiffDrive, GravityPlugin)
  SENSOR,       ///< Читает WorldQuery, публикует данные (Lidar, GNSS, IMU)
  INTERACTION,  ///< Выдаёт KernelCommand / EventBus (Grabber, DoorOpener)
  RESOURCE,     ///< Пишет в SharedState (Battery, Payload)
  UTILITY       ///< Вспомогательные (JointVel, TrajectoryRecorder, Color)
};

/**
 * @brief Контекст плагина — доступ к инфраструктуре ядра.
 *
 * Передаётся в update() каждого плагина каждый тик.
 * Позволяет плагинам взаимодействовать с ядром без прямой зависимости.
 *
 * world    — read-only доступ к миру (поиск Entity, сигналы, raycast, зоны)
 * bus      — публикация событий (только publish, не subscribe)
 * commands — очередь команд ядра (SpawnEntity, Interact, AttachObject и т.п.)
 *
 * KernelCommandQueue — однотиковый локальный буфер. Плагин добавляет команды,
 * SimEngine применяет их в Phase 0 следующего тика.
 */
struct PluginContext
{
  const WorldQuery&      world;     ///< Read-only доступ к миру
  EventBus&              bus;       ///< Шина событий (publish)
  KernelCommandQueue&    commands;  ///< Очередь команд ядра
};
```

4. В классе IAgentPlugin:
   - Изменить сигнатуру update: `virtual void update(double dt, Agent& agent, const PluginContext& ctx) = 0;`
   - Добавить чисто виртуальный метод role: `virtual PluginRole role() const = 0;`
   - Изменить config_schema: `virtual nlohmann::json config_schema() const { return nlohmann::json::array(); }` (вместо std::string)
   - Добавить provided_capabilities: `virtual std::vector<std::string> provided_capabilities() const { return {}; }`
   - Добавить после метода initialize() новые lifecycle-методы:

```cpp
    // ─── Lifecycle-методы плагина ──────────────────────────────────────────

    /**
     * @brief Вызывается при добавлении плагина к Entity (spawn или AddPlugin команда).
     * Используется для инициализации, которая требует доступа к Entity-контексту.
     * По умолчанию — no-op.
     */
    virtual void on_spawn(Agent& agent) { (void)agent; }

    /**
     * @brief Вызывается при удалении плагина (DespawnEntity или RemovePlugin команда).
     * Используется для освобождения ресурсов, публикации финальных событий.
     * По умолчанию — no-op.
     */
    virtual void on_despawn(Agent& agent) { (void)agent; }

    /**
     * @brief Вызывается при загрузке новой сцены (LoadScene или NewScene команда).
     * Позволяет плагину прочитать начальное состояние мира.
     * По умолчанию — no-op.
     */
    virtual void on_scene_load(const SimWorld& world) { (void)world; }

    /**
     * @brief Вызывается при сбросе симуляции (ResetSim команда).
     * Плагин должен сбросить своё внутреннее состояние к начальному.
     * По умолчанию — no-op.
     */
    virtual void on_reset(Agent& agent) { (void)agent; }
```

Нужен forward-declare SimWorld в plugin_base.hpp:
```cpp
// Forward-declare
class SimWorld;
```
(уже есть Agent и CollisionSystem forward-declares — добавить рядом)

Создать workspace/s2_core/tests/test_plugin_lifecycle.cpp:
```cpp
/**
 * @file test_plugin_lifecycle.cpp
 * Тесты lifecycle-методов IAgentPlugin: on_spawn, on_despawn, on_reset, on_scene_load.
 */

#include <s2/plugin_base.hpp>
#include <s2/agent.hpp>
#include <s2/world_query.hpp>
#include <s2/event_bus.hpp>
#include <gtest/gtest.h>
#include <string>
#include <vector>

namespace s2
{
namespace plugins
{

// Тестовый плагин, отслеживающий вызовы lifecycle-методов
class TestLifecyclePlugin : public IAgentPlugin
{
public:
  std::string type() const override { return "test_lifecycle"; }
  PluginRole  role() const override { return PluginRole::UTILITY; }

  void update(double, Agent&, const PluginContext&) override {}
  void from_config(const YAML::Node&) override {}
  std::string to_json() const override { return "{}"; }

  void on_spawn(Agent&)              override { calls_.push_back("spawn"); }
  void on_despawn(Agent&)            override { calls_.push_back("despawn"); }
  void on_scene_load(const SimWorld&) override { calls_.push_back("scene_load"); }
  void on_reset(Agent&)              override { calls_.push_back("reset"); }

  const std::vector<std::string>& calls() const { return calls_; }
private:
  std::vector<std::string> calls_;
};

TEST(PluginLifecycle, OnSpawnCalled)
{
  TestLifecyclePlugin plugin;
  Agent agent;
  plugin.on_spawn(agent);
  ASSERT_EQ(plugin.calls().size(), 1u);
  EXPECT_EQ(plugin.calls()[0], "spawn");
}

TEST(PluginLifecycle, OnDespawnCalled)
{
  TestLifecyclePlugin plugin;
  Agent agent;
  plugin.on_despawn(agent);
  ASSERT_EQ(plugin.calls().size(), 1u);
  EXPECT_EQ(plugin.calls()[0], "despawn");
}

TEST(PluginLifecycle, OnResetCalled)
{
  TestLifecyclePlugin plugin;
  Agent agent;
  plugin.on_reset(agent);
  ASSERT_EQ(plugin.calls().size(), 1u);
  EXPECT_EQ(plugin.calls()[0], "reset");
}

TEST(PluginLifecycle, DefaultImplNoOp)
{
  // Базовая реализация не падает и ничего не делает
  // (используем реальный плагин, который НЕ переопределяет lifecycle)
  // Проверяем через базовый вызов — не должен упасть
  // TestLifecyclePlugin переопределяет, поэтому используем отдельный минимальный плагин
  class MinimalPlugin : public IAgentPlugin
  {
  public:
    std::string type() const override { return "minimal"; }
    PluginRole  role() const override { return PluginRole::UTILITY; }
    void update(double, Agent&, const PluginContext&) override {}
    void from_config(const YAML::Node&) override {}
    std::string to_json() const override { return "{}"; }
    // on_spawn и т.п. — используют default-реализацию из base
  };

  MinimalPlugin plugin;
  Agent agent;
  EXPECT_NO_THROW(plugin.on_spawn(agent));
  EXPECT_NO_THROW(plugin.on_despawn(agent));
  EXPECT_NO_THROW(plugin.on_reset(agent));
}

TEST(PluginLifecycle, ProvidedCapabilitiesDefaultEmpty)
{
  TestLifecyclePlugin plugin;
  EXPECT_TRUE(plugin.provided_capabilities().empty());
}

TEST(PluginLifecycle, PluginContextHoldsReferences)
{
  WorldQuery world;
  EventBus bus;
  KernelCommandQueue cmds;
  PluginContext ctx{world, bus, cmds};

  // PluginContext корректно создаётся и хранит ссылки
  EXPECT_EQ(&ctx.world, &world);
  EXPECT_EQ(&ctx.bus, &bus);
  EXPECT_EQ(&ctx.commands, &cmds);
}

}  // namespace plugins
}  // namespace s2
```

Создать workspace/s2_core/tests/test_plugin_roles.cpp:
```cpp
/**
 * @file test_plugin_roles.cpp
 * Тесты системы ролей плагинов: PluginRole enum, матрица доступа.
 */

#include <s2/plugin_base.hpp>
#include <s2/agent.hpp>
#include <gtest/gtest.h>

namespace s2
{
namespace plugins
{

// Плагин с ролью ACTUATION
class FakeActuationPlugin : public IAgentPlugin
{
public:
  std::string type() const override { return "fake_actuation"; }
  PluginRole  role() const override { return PluginRole::ACTUATION; }
  void update(double, Agent&, const PluginContext&) override {}
  void from_config(const YAML::Node&) override {}
  std::string to_json() const override { return "{}"; }
};

// Плагин с ролью SENSOR
class FakeSensorPlugin : public IAgentPlugin
{
public:
  std::string type() const override { return "fake_sensor"; }
  PluginRole  role() const override { return PluginRole::SENSOR; }
  std::vector<std::string> provided_capabilities() const override { return {"optical_sensor"}; }
  void update(double, Agent&, const PluginContext&) override {}
  void from_config(const YAML::Node&) override {}
  std::string to_json() const override { return "{}"; }
};

TEST(PluginRole, ActuationRoleReturned)
{
  FakeActuationPlugin p;
  EXPECT_EQ(p.role(), PluginRole::ACTUATION);
}

TEST(PluginRole, SensorRoleReturned)
{
  FakeSensorPlugin p;
  EXPECT_EQ(p.role(), PluginRole::SENSOR);
}

TEST(PluginRole, AllRolesDistinct)
{
  // enum class PluginRole содержит 5 различных значений
  EXPECT_NE(PluginRole::ACTUATION,   PluginRole::SENSOR);
  EXPECT_NE(PluginRole::ACTUATION,   PluginRole::INTERACTION);
  EXPECT_NE(PluginRole::ACTUATION,   PluginRole::RESOURCE);
  EXPECT_NE(PluginRole::ACTUATION,   PluginRole::UTILITY);
  EXPECT_NE(PluginRole::SENSOR,      PluginRole::INTERACTION);
  EXPECT_NE(PluginRole::SENSOR,      PluginRole::RESOURCE);
  EXPECT_NE(PluginRole::SENSOR,      PluginRole::UTILITY);
  EXPECT_NE(PluginRole::INTERACTION, PluginRole::RESOURCE);
  EXPECT_NE(PluginRole::INTERACTION, PluginRole::UTILITY);
  EXPECT_NE(PluginRole::RESOURCE,    PluginRole::UTILITY);
}

TEST(PluginRole, ProvidedCapabilitiesFromSensor)
{
  FakeSensorPlugin p;
  auto caps = p.provided_capabilities();
  ASSERT_EQ(caps.size(), 1u);
  EXPECT_EQ(caps[0], "optical_sensor");
}

TEST(PluginRole, ValidationActuationCountHelper)
{
  // Вспомогательная функция: count actuation plugins
  // (Реальная валидация будет в SceneLoader — Plan 06)
  std::vector<std::unique_ptr<IAgentPlugin>> plugins;
  plugins.push_back(std::make_unique<FakeActuationPlugin>());
  plugins.push_back(std::make_unique<FakeSensorPlugin>());

  int actuation_count = 0;
  for (const auto& p : plugins)
    if (p->role() == PluginRole::ACTUATION)
      actuation_count++;

  EXPECT_EQ(actuation_count, 1);
}

}  // namespace plugins
}  // namespace s2
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -40</automated>
  </verify>
  <done>
    - grep -n "enum class PluginRole" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку
    - grep -n "struct PluginContext" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку
    - grep -n "virtual void on_spawn" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку
    - grep -n "virtual PluginRole role" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку
    - grep -n "const PluginContext" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку в update()
    - grep -n "provided_capabilities" workspace/s2_core/include/s2/plugin_base.hpp выдаёт строку
    - test_plugin_lifecycle.cpp все тесты зелёные
    - test_plugin_roles.cpp все тесты зелёные
    - Docker: все тесты компилируются и проходят (возможны ошибки компиляции в плагинах — они исправятся в Plan 06)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → PluginContext | Плагин получает доступ к WorldQuery/EventBus/KernelCommandQueue |
| PluginContext::commands | Команды из плагина попадают в очередь ядра |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Tampering | PluginContext::world (WorldQuery) | mitigate | WorldQuery только read-only (const refs); нет методов записи по дизайну |
| T-00-04 | Elevation of Privilege | PluginRole::ACTUATION единственность | mitigate | Валидация в SceneLoader::load() и AddPlugin обработчике (Plan 06): if actuation_count > 1 throw runtime_error |
| T-00-05 | Denial of Service | KernelCommandQueue unbounded | accept | Внутренний API, только один тик, очередь очищается в phase0_kernel_commands() |
</threat_model>

<verification>
После выполнения обеих задач:

1. `grep -n "class WorldQuery" workspace/s2_core/include/s2/world_query.hpp` — находит
2. `grep -n "find_signals_of_type" workspace/s2_core/include/s2/world_query.hpp` — находит
3. `grep -n "enum class PluginRole" workspace/s2_core/include/s2/plugin_base.hpp` — находит
4. `grep -n "on_spawn" workspace/s2_core/include/s2/plugin_base.hpp` — находит
5. `grep -n "PluginContext" workspace/s2_core/include/s2/plugin_base.hpp` — находит
6. `docker compose --project-directory docker up --build tests` — новые тесты зелёные
</verification>

<success_criteria>
- WorldQuery header создан со всеми 10 методами из §15.3 RESULT_DISCUSS.md
- IAgentPlugin содержит on_spawn/on_despawn/on_scene_load/on_reset с пустыми default-реализациями
- PluginRole enum с 5 значениями (ACTUATION, SENSOR, INTERACTION, RESOURCE, UTILITY) в plugin_base.hpp
- PluginContext struct с world/bus/commands полями в plugin_base.hpp
- update() сигнатура изменена на update(double, Agent&, const PluginContext&)
- provided_capabilities() возвращает std::vector&lt;std::string&gt;
- test_plugin_lifecycle.cpp и test_plugin_roles.cpp проходят
</success_criteria>

<output>
После завершения создать `.planning/phases/00-core-architecture-foundation/00-02-SUMMARY.md`
</output>
