---
phase: "00-core-architecture-foundation"
plan: "05"
type: execute
wave: 5
depends_on:
  - "00-04"
files_modified:
  - workspace/s2_plugins/include/s2/plugins/diff_drive.hpp
  - workspace/s2_plugins/include/s2/plugins/battery.hpp
  - workspace/s2_plugins/include/s2/plugins/gnss.hpp
  - workspace/s2_plugins/include/s2/plugins/imu.hpp
  - workspace/s2_plugins/include/s2/plugins/lidar.hpp
  - workspace/s2_plugins/include/s2/plugins/gravity.hpp
  - workspace/s2_plugins/include/s2/plugins/color.hpp
  - workspace/s2_plugins/include/s2/plugins/joint_vel.hpp
  - workspace/s2_plugins/include/s2/plugins/trajectory_recorder.hpp
  - workspace/s2_plugins/include/s2/plugins/path_display.hpp
  - workspace/s2_plugins/include/s2/plugins/topic_display.hpp
  - workspace/s2_core/include/s2/scene_loader.hpp
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "Все 11 плагинов компилируются с новой сигнатурой update(double, Agent&, const PluginContext&)"
    - "Каждый плагин реализует virtual PluginRole role() const override"
    - "DiffDrivePlugin имеет on_reset(Agent& agent) который сбрасывает external_linear_velocity_ и external_angular_velocity_"
    - "BatteryPlugin имеет on_reset(Agent& agent) который сбрасывает заряд до initial_level_"
    - "SceneLoader валидирует: не более одного ACTUATION плагина на агента, бросает std::runtime_error"
    - "Docker: docker compose tests проходит, все существующие тесты зелёные"
  artifacts:
    - path: "workspace/s2_plugins/include/s2/plugins/diff_drive.hpp"
      provides: "DiffDrivePlugin с новым update(), role(), on_reset()"
      contains: "PluginRole::ACTUATION"
    - path: "workspace/s2_plugins/include/s2/plugins/battery.hpp"
      provides: "BatteryPlugin с новым update(), role(), on_reset()"
      contains: "PluginRole::RESOURCE"
    - path: "workspace/s2_core/include/s2/scene_loader.hpp"
      provides: "валидация ACTUATION плагинов"
      contains: "runtime_error"
  key_links:
    - from: "workspace/s2_plugins/include/s2/plugins/diff_drive.hpp"
      to: "workspace/s2_core/include/s2/plugin_base.hpp"
      via: "update() сигнатура + role()"
      pattern: "PluginRole"
    - from: "workspace/s2_core/include/s2/scene_loader.hpp"
      to: "workspace/s2_core/include/s2/plugin_base.hpp"
      via: "валидация PluginRole::ACTUATION"
      pattern: "ACTUATION"
---

<objective>
Финальный шаг Phase 0: обновляем все 11 существующих плагинов под новый интерфейс IAgentPlugin (update с PluginContext, role(), on_reset баги), добавляем валидацию ACTUATION в SceneLoader.

Purpose: После Plans 01–04 все интерфейсы готовы, но плагины ещё компилируются со старой сигнатурой update(dt, agent). Этот план синхронизирует всю codebase.

Output:
- 11 плагинов: новая сигнатура update() + role() + on_reset() где нужно
- DiffDrive on_reset: сбрасывает external_linear_velocity_ (баг из D-13)
- Battery on_reset: сбрасывает заряд до initial_level_ (баг из D-13)
- SceneLoader: валидирует ACTUATION плагины (D-11)
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/PROJECT.md
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/00-core-architecture-foundation/00-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/00-core-architecture-foundation/00-04-SUMMARY.md
</context>

<interfaces>
<!-- Текущие сигнатуры всех плагинов — executor ДОЛЖЕН прочитать каждый файл. -->

Роль каждого плагина (из D-09, D-10):
- DiffDrivePlugin:           role() → ACTUATION  (пишет agent.velocity)
- GravityPlugin:             role() → ACTUATION  (управляет Z-позицией)
- GnssPlugin:                role() → SENSOR
- ImuPlugin:                 role() → SENSOR
- LidarPlugin:               role() → SENSOR
- BatteryPlugin:             role() → RESOURCE   (пишет в SharedState)
- ColorPlugin:               role() → UTILITY
- JointVelPlugin:            role() → UTILITY    (пишет в KinematicTree, не velocity)
- TrajectoryRecorderPlugin:  role() → UTILITY
- PathDisplayPlugin:         role() → UTILITY
- TopicDisplayPlugin:        role() → UTILITY

Старая сигнатура update (во всех 11 файлах):
  void update(double dt, Agent& agent) override

Новая сигнатура (D-03):
  void update(double dt, Agent& agent, const PluginContext& ctx) override
  ctx = PluginContext{ const WorldQuery& world, EventBus& bus, KernelCommandQueue& commands }
  По умолчанию ctx игнорируется (достаточно добавить параметр и (void)ctx)

Известные on_reset баги (D-13):
- DiffDrivePlugin::on_reset: нужно сбросить external_linear_velocity_ = 0.0, external_angular_velocity_ = 0.0, has_external_input_ = false
- BatteryPlugin::on_reset: нужно сбросить BatteryComponent до initial_level_

Из scene_loader.hpp — место для валидации:
  SceneLoader::load() создаёт агентов и их плагины. После создания всех плагинов —
  проверить что ACTUATION плагинов не более одного на агента.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Обновить все 11 плагинов — новая сигнатура update() + role()</name>
  <files>
    workspace/s2_plugins/include/s2/plugins/diff_drive.hpp
    workspace/s2_plugins/include/s2/plugins/battery.hpp
    workspace/s2_plugins/include/s2/plugins/gnss.hpp
    workspace/s2_plugins/include/s2/plugins/imu.hpp
    workspace/s2_plugins/include/s2/plugins/lidar.hpp
    workspace/s2_plugins/include/s2/plugins/gravity.hpp
    workspace/s2_plugins/include/s2/plugins/color.hpp
    workspace/s2_plugins/include/s2/plugins/joint_vel.hpp
    workspace/s2_plugins/include/s2/plugins/trajectory_recorder.hpp
    workspace/s2_plugins/include/s2/plugins/path_display.hpp
    workspace/s2_plugins/include/s2/plugins/topic_display.hpp
  </read_first>
  <read_first>
    - workspace/s2_plugins/include/s2/plugins/diff_drive.hpp — прочитать полностью
    - workspace/s2_plugins/include/s2/plugins/battery.hpp — прочитать полностью
    - workspace/s2_plugins/include/s2/plugins/gnss.hpp — прочитать (понять update pattern)
    - workspace/s2_plugins/include/s2/plugins/lidar.hpp — прочитать (это SENSOR)
    - workspace/s2_plugins/include/s2/plugins/gravity.hpp — прочитать (это ACTUATION)
    - workspace/s2_core/include/s2/plugin_base.hpp — новый интерфейс (Plans 02)
  </read_first>
  <action>
Для КАЖДОГО из 11 плагинов выполнить следующие изменения:

**Изменение 1 — update() сигнатура:**
Найти строку вида:
  `void update(double dt, Agent& agent) override`
Заменить на:
  `void update(double dt, Agent& agent, const plugins::PluginContext& ctx) override`
Добавить в начало тела функции:
  `(void)ctx;  // Плагин не использует WorldQuery/EventBus в Phase 0`

Для плагинов в отдельных .cpp файлах (color.cpp, joint_vel.cpp) — изменить только .hpp объявление,
.cpp определение также должно быть обновлено.

**Изменение 2 — role() метод:**
Добавить после строки `std::string type() const override`:
```cpp
    plugins::PluginRole role() const override { return plugins::PluginRole::XXXX; }
```
Где XXXX — согласно таблице ролей выше.

**Изменение 3 — on_reset для DiffDrive:**
В DiffDrivePlugin добавить метод:
```cpp
    void on_reset(Agent& agent) override
    {
      (void)agent;
      // Сброс external команд — агент не должен продолжать движение после reset
      external_linear_velocity_  = 0.0;
      external_angular_velocity_ = 0.0;
      has_external_input_        = false;
      time_acc_                  = 0.0;
    }
```

**Изменение 4 — on_reset для Battery:**
В BatteryPlugin добавить метод:
```cpp
    void on_reset(Agent& agent) override
    {
      // Сбросить заряд батареи до начального значения
      auto* bat = agent.state.get<BatteryComponent>();
      if (bat) {
        bat->level    = initial_level_;
        bat->charging = false;
      }
    }
```

**Дополнительно — color.cpp и joint_vel.cpp:**
Прочитать workspace/s2_plugins/src/color.cpp и workspace/s2_plugins/src/joint_vel.cpp,
обновить сигнатуры update() там тоже (добавить const PluginContext& ctx параметр).

Полный список файлов для изменения и что именно менять:

| Файл | update() | role() | on_reset() |
|------|---------|--------|-----------|
| diff_drive.hpp | добавить ctx | ACTUATION | исправить баг |
| battery.hpp | добавить ctx | RESOURCE | исправить баг |
| gnss.hpp | добавить ctx | SENSOR | — |
| imu.hpp | добавить ctx | SENSOR | — |
| lidar.hpp | добавить ctx | SENSOR | — |
| gravity.hpp | добавить ctx | ACTUATION | — |
| color.hpp + color.cpp | добавить ctx | UTILITY | — |
| joint_vel.hpp + joint_vel.cpp | добавить ctx | UTILITY | — |
| trajectory_recorder.hpp | добавить ctx | UTILITY | — |
| path_display.hpp | добавить ctx | UTILITY | — |
| topic_display.hpp | добавить ctx | UTILITY | — |
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <done>
    - grep -rn "PluginRole::ACTUATION" workspace/s2_plugins/include/ выдаёт 2 строки (DiffDrive + Gravity)
    - grep -rn "PluginRole::SENSOR" workspace/s2_plugins/include/ выдаёт 3 строки (Gnss + Imu + Lidar)
    - grep -rn "PluginRole::RESOURCE" workspace/s2_plugins/include/ выдаёт 1 строку (Battery)
    - grep -rn "PluginRole::UTILITY" workspace/s2_plugins/include/ выдаёт 5+ строк
    - grep -rn "void update(double dt, Agent& agent)" workspace/s2_plugins/ выдаёт 0 строк (все обновлены)
    - grep -rn "const PluginContext& ctx" workspace/s2_plugins/ выдаёт 11+ строк
    - Docker: все тесты компилируются и проходят
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Валидация ACTUATION в SceneLoader + тест on_reset багов DiffDrive/Battery</name>
  <files>
    workspace/s2_core/include/s2/scene_loader.hpp
    workspace/s2_core/tests/test_plugin_on_reset.cpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/scene_loader.hpp — прочитать полностью (место для валидации)
    - workspace/s2_plugins/include/s2/plugins/diff_drive.hpp — прочитать on_reset после Task 1
    - workspace/s2_plugins/include/s2/plugins/battery.hpp — прочитать on_reset после Task 1
    - workspace/s2_core/tests/test_battery_plugin.cpp — прочитать для понимания стиля тестов
    - workspace/s2_core/tests/test_sim_engine.cpp — прочитать паттерн load_world/step
    - .planning/phases/00-core-architecture-foundation/00-CONTEXT.md §D-11, D-13
  </read_first>
  <behavior>
    - В SceneLoader после создания всех плагинов агента считать ACTUATION плагины
    - Если больше одного — throw std::runtime_error("Agent [name] имеет несколько ACTUATION плагинов")
    - Тесты проверяют: DiffDrive on_reset сбрасывает состояние, Battery on_reset сбрасывает заряд
    - Тест: engine.reset() вызывает on_reset() для всех плагинов
  </behavior>
  <action>
Обновить workspace/s2_core/include/s2/scene_loader.hpp:

Найти место где завершается загрузка плагинов агента (цикл по плагинам агента).
После создания всех плагинов добавить проверку (per D-11):

```cpp
  // Валидация: не более одного ACTUATION-плагина на агента (D-11)
  {
    int actuation_count = 0;
    for (const auto& plugin : agent.plugins)
      if (plugin->role() == s2::plugins::PluginRole::ACTUATION)
        actuation_count++;
    if (actuation_count > 1) {
      throw std::runtime_error(
          "Агент '" + agent.name + "' имеет " + std::to_string(actuation_count) +
          " ACTUATION-плагина. Допустим только один.");
    }
  }
```

Обновить SimEngine::reset() в sim_engine.hpp — добавить вызов on_reset() для всех плагинов:
```cpp
  void reset()
  {
    restore_initial_states();
    // Вызвать on_reset() для всех плагинов всех агентов (D-12)
    for (auto& agent : world_.agents())
      for (auto& plugin : agent.plugins)
        plugin->on_reset(agent);
    sim_time_ = 0.0;
    paused_   = true;
  }
```

Создать workspace/s2_core/tests/test_plugin_on_reset.cpp:
```cpp
/**
 * @file test_plugin_on_reset.cpp
 * Тесты on_reset() для DiffDrive и Battery плагинов.
 *
 * Проверяет баги из D-13:
 *  - DiffDrive: external_linear_velocity_ сбрасывается при reset
 *  - Battery: заряд сбрасывается до initial_level_ при reset
 */

#include <s2/sim_engine.hpp>
#include <s2/agent.hpp>
#include <s2/plugins/diff_drive.hpp>
#include <s2/plugins/battery.hpp>
#include <s2/components/battery_component.hpp>
#include <gtest/gtest.h>

namespace s2
{

// Вспомогательная: создать SimEngine с одним агентом и заданным плагином
template<typename PluginT, typename... Args>
SimEngine make_engine_with_plugin(Args&&... args)
{
  SimEngine::Config cfg;
  cfg.update_rate = 100.0;
  cfg.viz_rate    = 0.0;
  cfg.transport_rate = 0.0;
  SimEngine engine(cfg);

  Agent agent;
  agent.id = 1;
  agent.plugins.push_back(std::make_unique<PluginT>(std::forward<Args>(args)...));

  SimWorld world;
  world.agents().push_back(std::move(agent));
  engine.load_world(std::move(world));
  return engine;
}

// ─── DiffDrive on_reset ────────────────────────────────────────────────────────

TEST(PluginOnReset, DiffDriveExternalVelocityResetAfterReset)
{
  SimEngine::Config cfg;
  cfg.update_rate = 100.0;
  cfg.viz_rate = 0.0;
  cfg.transport_rate = 0.0;
  SimEngine engine(cfg);

  Agent agent;
  agent.id = 1;
  auto* plugin_ptr = new plugins::DiffDrivePlugin();
  agent.plugins.push_back(std::unique_ptr<plugins::DiffDrivePlugin>(plugin_ptr));

  SimWorld world;
  world.agents().push_back(std::move(agent));
  engine.load_world(std::move(world));

  // Отправить внешнюю команду скорости
  plugin_ptr->handle_input("{\"linear_velocity\": 1.5, \"angular_velocity\": 0.5}");

  // Прогнать один тик — агент должен двигаться
  engine.step(1);
  const auto& a_before = engine.world().agents()[0];
  double speed_before = a_before.world_velocity.linear.norm();
  EXPECT_GT(speed_before, 0.0) << "После external input агент должен двигаться";

  // Reset — on_reset() должен сбросить external_linear_velocity_
  engine.reset();
  engine.step(1);  // ещё один тик без команд

  const auto& a_after = engine.world().agents()[0];
  // После reset без новых команд скорость должна быть нулевой
  EXPECT_NEAR(a_after.world_velocity.linear.norm(), 0.0, 1e-6)
      << "После reset агент не должен двигаться (external_linear_velocity_ сброшен)";
}

// ─── Battery on_reset ─────────────────────────────────────────────────────────

TEST(PluginOnReset, BatteryLevelRestoredAfterReset)
{
  SimEngine::Config cfg;
  cfg.update_rate = 100.0;
  cfg.viz_rate = 0.0;
  cfg.transport_rate = 0.0;
  SimEngine engine(cfg);

  Agent agent;
  agent.id = 1;
  auto battery = std::make_unique<plugins::BatteryPlugin>();

  // Установить начальный заряд через конфигурацию
  YAML::Node config;
  config["initial_level"] = 0.8;
  battery->from_config(config);
  battery->initialize(agent);

  agent.plugins.push_back(std::move(battery));

  SimWorld world;
  world.agents().push_back(std::move(agent));
  engine.load_world(std::move(world));

  // Симулировать разряд: напрямую снизить уровень в BatteryComponent
  {
    auto& a = engine.world().agents()[0];
    auto* bat = a.state.get<BatteryComponent>();
    ASSERT_NE(bat, nullptr);
    bat->level = 0.1;  // имитируем разряд
  }

  // Проверяем что заряд снижен
  {
    const auto& a = engine.world().agents()[0];
    const auto* bat = a.state.get<BatteryComponent>();
    EXPECT_NEAR(bat->level, 0.1, 1e-6);
  }

  // Reset — on_reset() должен восстановить initial_level_ = 0.8
  engine.reset();

  {
    const auto& a = engine.world().agents()[0];
    const auto* bat = a.state.get<BatteryComponent>();
    ASSERT_NE(bat, nullptr);
    EXPECT_NEAR(bat->level, 0.8, 1e-6)
        << "После reset заряд батареи должен восстановиться до initial_level_ = 0.8";
  }
}

// ─── SceneLoader валидация ACTUATION ──────────────────────────────────────────

TEST(SceneLoaderValidation, SingleActuationPluginAllowed)
{
  // Один DiffDrive — должно работать без исключений
  // Тест через прямое создание агента (без YAML):
  // SceneLoader::load() с двумя ACTUATION плагинами должен бросить

  // Прямой тест валидации: вручную создать агента с двумя ACTUATION плагинами
  // и вызвать функцию валидации
  std::vector<std::unique_ptr<plugins::IAgentPlugin>> plugins_list;
  plugins_list.push_back(std::make_unique<plugins::DiffDrivePlugin>());
  plugins_list.push_back(std::make_unique<plugins::DiffDrivePlugin>());

  int actuation_count = 0;
  for (const auto& p : plugins_list)
    if (p->role() == plugins::PluginRole::ACTUATION)
      actuation_count++;

  EXPECT_EQ(actuation_count, 2);
  // Код SceneLoader бросит runtime_error при таком агенте
  // (полный тест через YAML файл — TODO в интеграционных тестах)
}

}  // namespace s2
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -50</automated>
  </verify>
  <done>
    - grep -n "on_reset" workspace/s2_plugins/include/s2/plugins/diff_drive.hpp выдаёт строку метода
    - grep -n "on_reset" workspace/s2_plugins/include/s2/plugins/battery.hpp выдаёт строку метода
    - grep -n "on_reset" workspace/s2_core/include/s2/sim_engine.hpp выдаёт вызов в reset()
    - grep -n "runtime_error" workspace/s2_core/include/s2/scene_loader.hpp выдаёт строку валидации
    - test_plugin_on_reset.cpp: DiffDriveExternalVelocityResetAfterReset зелёный
    - test_plugin_on_reset.cpp: BatteryLevelRestoredAfterReset зелёный
    - Docker: все тесты зелёные (включая все существующие 28+)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config → SceneLoader | YAML из файловой системы парсится SceneLoader |
| Plugin → shared state | RESOURCE плагины пишут в SharedState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Tampering | DiffDrive handle_input external_velocity | accept | Внутренний API; внешний input через handle_input уже проходит JSON парсинг |
| T-00-13 | Elevation of Privilege | SceneLoader: два ACTUATION плагина | mitigate | Валидация в SceneLoader::load(): счётчик ACTUATION плагинов, throw runtime_error если > 1 |
| T-00-14 | Denial of Service | Battery on_reset: level из YAML | accept | initial_level_ зажат в [0.0, 1.0] при from_config() |
</threat_model>

<verification>
После выполнения обеих задач:

1. `grep -rn "void update(double dt, Agent& agent)" workspace/s2_plugins/` — 0 результатов (все обновлены)
2. `grep -rn "const PluginContext& ctx" workspace/s2_plugins/` — 11+ результатов
3. `grep -rn "PluginRole::" workspace/s2_plugins/` — 11 результатов (все плагины имеют role())
4. `grep -n "on_reset" workspace/s2_plugins/include/s2/plugins/diff_drive.hpp` — находит
5. `grep -n "external_linear_velocity_" workspace/s2_plugins/include/s2/plugins/diff_drive.hpp` — в on_reset
6. `grep -n "on_reset" workspace/s2_plugins/include/s2/plugins/battery.hpp` — находит
7. `grep -n "initial_level_" workspace/s2_plugins/include/s2/plugins/battery.hpp` — в on_reset
8. `grep -n "on_reset" workspace/s2_core/include/s2/sim_engine.hpp` — в reset() методе
9. `grep -n "runtime_error" workspace/s2_core/include/s2/scene_loader.hpp` — находит валидацию
10. `docker compose --project-directory docker up --build tests` — все тесты зелёные
</verification>

<success_criteria>
- Все 11 плагинов скомпилированы с update(double, Agent&, const PluginContext&)
- Каждый плагин реализует role() с правильным PluginRole
- DiffDrive on_reset() сбрасывает external_linear_velocity_ = 0, external_angular_velocity_ = 0, has_external_input_ = false
- Battery on_reset() устанавливает BatteryComponent::level = initial_level_
- SimEngine::reset() вызывает on_reset(agent) для всех плагинов всех агентов
- SceneLoader бросает std::runtime_error если у агента > 1 ACTUATION плагина
- test_plugin_on_reset.cpp: 3 теста зелёные
- Все 28+ существующих тестов проходят без изменений
</success_criteria>

<output>
После завершения создать `.planning/phases/00-core-architecture-foundation/00-05-SUMMARY.md`
</output>
