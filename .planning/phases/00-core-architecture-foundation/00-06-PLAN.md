---
phase: 00-core-architecture-foundation
plan: 06
type: execute
wave: 1
depends_on: ["00-05"]
files_modified:
  - workspace/s2_core/src/zone_system.cpp
  - workspace/s2_core/tests/test_zone_system.cpp
autonomous: true
requirements: [ARCH-04]
gap_closure: true

must_haves:
  truths:
    - "ZoneSystem публикует event::ZoneEntered при входе агента в зону"
    - "ZoneSystem публикует event::ZoneExited при выходе агента из зоны"
    - "Поля zone_id и entity_id события содержат корректные значения"
    - "Устаревшие события AgentEnteredZone / AgentExitedZone продолжают публиковаться (backward compat)"
  artifacts:
    - path: "workspace/s2_core/src/zone_system.cpp"
      provides: "публикация event::ZoneEntered и event::ZoneExited в on_agent_enter / on_agent_exit"
      contains: "bus.publish(event::ZoneEntered"
    - path: "workspace/s2_core/tests/test_zone_system.cpp"
      provides: "интеграционные тесты ZoneEntered / ZoneExited через ZoneSystem"
      contains: "ZoneSystem_ZoneEnteredEvent"
  key_links:
    - from: "ZoneSystem::on_agent_enter()"
      to: "event::ZoneEntered"
      via: "bus.publish()"
      pattern: "bus\\.publish\\(event::ZoneEntered"
    - from: "ZoneSystem::on_agent_exit()"
      to: "event::ZoneExited"
      via: "bus.publish()"
      pattern: "bus\\.publish\\(event::ZoneExited"
---

<objective>
Закрыть gap ARCH-04: ZoneSystem не публиковала event::ZoneEntered / event::ZoneExited,
из-за чего подписчики (Phase 1+) никогда не получали эти события.

Цель: добавить публикацию новых событий в ZoneSystem::on_agent_enter() и
ZoneSystem::on_agent_exit(), не сломав backward-compat с legacy-событиями.

Purpose: Phase 1 (ToggleZone ON_ENTER/ON_EXIT) и Phase 2 (EventReactor) зависят от event::ZoneEntered.
Output: исправленный zone_system.cpp + тесты в test_zone_system.cpp.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Добавить публикацию event::ZoneEntered и event::ZoneExited в ZoneSystem</name>
  <files>workspace/s2_core/src/zone_system.cpp</files>

  <read_first>
    - workspace/s2_core/src/zone_system.cpp — строки 199-245 (on_agent_enter, on_agent_exit)
    - workspace/s2_core/include/s2/event_bus.hpp — строки 57-60 (struct ZoneEntered, ZoneExited)
  </read_first>

  <action>
В файле workspace/s2_core/src/zone_system.cpp:

1. В методе ZoneSystem::on_agent_enter() (строка ~202), СРАЗУ ПОСЛЕ строки:
     bus.publish(event::AgentEnteredZone{.agent = agent.id, .zone = zone.id});
   добавить:
     bus.publish(event::ZoneEntered{.zone_id = zone.id, .entity_id = agent.id});

2. В методе ZoneSystem::on_agent_exit() (строка ~225), СРАЗУ ПОСЛЕ строки:
     bus.publish(event::AgentExitedZone{.agent = agent.id, .zone = zone.id});
   добавить:
     bus.publish(event::ZoneExited{.zone_id = zone.id, .entity_id = agent.id});

Комментарий (рядом с каждой новой строкой, по-русски):
  // Новый event — entity-уровень (для Phase 1+ подписчиков)

Типы совместимы: AgentId и EntityId оба являются using ... = uint32_t,
поэтому agent.id используется напрямую как entity_id.

Ничего больше не изменять: логика эффектов, legacy-события — без изменений.
  </action>

  <verify>
    <automated>grep -n "bus.publish(event::ZoneEntered" /home/dmitriy/study/programming/s2-sim/workspace/s2_core/src/zone_system.cpp</automated>
    <automated>grep -n "bus.publish(event::ZoneExited" /home/dmitriy/study/programming/s2-sim/workspace/s2_core/src/zone_system.cpp</automated>
  </verify>

  <acceptance_criteria>
    - grep выводит ровно одну строку для ZoneEntered и ровно одну для ZoneExited
    - строка ZoneEntered содержит `.zone_id = zone.id` и `.entity_id = agent.id`
    - строка ZoneExited содержит `.zone_id = zone.id` и `.entity_id = agent.id`
    - legacy-строки AgentEnteredZone и AgentExitedZone остаются нетронутыми (grep возвращает их на прежних номерах строк ± 1 от вставки)
    - docker compose --project-directory docker up --build tests завершается без ошибок компиляции
  </acceptance_criteria>

  <done>ZoneSystem::on_agent_enter() и on_agent_exit() публикуют оба поколения событий. Сборка чистая.</done>
</task>

<task type="auto">
  <name>Задача 2: Добавить интеграционные тесты ZoneEntered / ZoneExited в test_zone_system.cpp</name>
  <files>workspace/s2_core/tests/test_zone_system.cpp</files>

  <read_first>
    - workspace/s2_core/tests/test_zone_system.cpp — полностью (паттерны make_agent, make_sphere_zone, SimBus, тест ZoneSystem_AgentEnterSphere)
    - workspace/s2_core/include/s2/event_bus.hpp — строки 57-60 (struct ZoneEntered, ZoneExited)
  </read_first>

  <action>
В конец файла workspace/s2_core/tests/test_zone_system.cpp добавить два теста
(перед закрывающей `}  // namespace s2`):

--- начало вставки ---

// ─── Тест N: ZoneSystem публикует event::ZoneEntered при входе агента ────────

TEST(ZoneSystem_ZoneEnteredEvent, PublishedOnAgentEnter)
{
    ZoneSystem zs;
    zs.add_zone(make_sphere_zone("charging_zone", Vec3{0.0, 0.0, 0.0}, 2.0));

    SimBus bus;
    ZoneId   received_zone;
    EntityId received_entity = 0;

    bus.subscribe<event::ZoneEntered>([&](const event::ZoneEntered& e) {
        received_zone   = e.zone_id;
        received_entity = e.entity_id;
    });

    std::vector<Agent> agents;
    agents.push_back(make_agent(7, 1.0, 0.0, 0.0));  // внутри зоны
    std::vector<Actor> actors;
    zs.tick(agents, actors, bus, 0.0, 0.01);

    EXPECT_EQ(received_zone,   "charging_zone");
    EXPECT_EQ(received_entity, 7u);
}

// ─── Тест N+1: ZoneSystem публикует event::ZoneExited при выходе агента ──────

TEST(ZoneSystem_ZoneExitedEvent, PublishedOnAgentExit)
{
    ZoneSystem zs;
    zs.add_zone(make_sphere_zone("charging_zone", Vec3{0.0, 0.0, 0.0}, 2.0));

    SimBus bus;

    // Сначала ввести агента в зону
    std::vector<Agent> agents;
    agents.push_back(make_agent(7, 1.0, 0.0, 0.0));
    std::vector<Actor> actors;
    zs.tick(agents, actors, bus, 0.0, 0.01);

    ZoneId   received_zone;
    EntityId received_entity = 0;

    bus.subscribe<event::ZoneExited>([&](const event::ZoneExited& e) {
        received_zone   = e.zone_id;
        received_entity = e.entity_id;
    });

    // Вывести агента за пределы зоны
    agents[0].world_pose.x = 5.0;
    zs.tick(agents, actors, bus, 0.01, 0.01);

    EXPECT_EQ(received_zone,   "charging_zone");
    EXPECT_EQ(received_entity, 7u);
}

--- конец вставки ---

Шаблон строго соответствует существующим тестам ZoneSystem_AgentEnterSphere /
ZoneSystem_AgentExitSphere — те же хелперы make_agent, make_sphere_zone, SimBus,
тот же стиль именования TEST(Suite, Case).

Не изменять существующие тесты. Не добавлять новые include — все нужные заголовки уже есть.
  </action>

  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - grep выводит строку "ZoneSystem_ZoneEnteredEvent" в test_zone_system.cpp
    - grep выводит строку "ZoneSystem_ZoneExitedEvent" в test_zone_system.cpp
    - docker compose ... tests завершается с кодом 0
    - в выводе тестов присутствуют строки:
        [ RUN      ] ZoneSystem_ZoneEnteredEvent.PublishedOnAgentEnter
        [       OK ] ZoneSystem_ZoneEnteredEvent.PublishedOnAgentEnter
        [ RUN      ] ZoneSystem_ZoneExitedEvent.PublishedOnAgentExit
        [       OK ] ZoneSystem_ZoneExitedEvent.PublishedOnAgentExit
    - все ранее существующие тесты (ZoneSystem_AgentEnterSphere, ZoneSystem_AgentExitSphere и т.д.) по-прежнему PASSED
  </acceptance_criteria>

  <done>Два новых теста проходят. Все тесты сборки зелёные. Gap ARCH-04 закрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Граница | Описание |
|---------|---------|
| zone_system → event bus | ZoneSystem публикует события — подписчики получают данные зоны и entity без валидации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-06-01 | Tampering | ZoneSystem::on_agent_enter | accept | Симуляционный поток единственный, входные данные — внутренние структуры движка, не внешний ввод |
| T-00-06-02 | Denial of Service | EventBus dispatch | accept | Шина синхронная и не потокобезопасна по дизайну; подписчики в том же потоке, DoS невозможен извне |
</threat_model>

<verification>
После выполнения обеих задач:

1. grep -n "ZoneEntered\|ZoneExited" workspace/s2_core/src/zone_system.cpp
   — должен вернуть ровно 2 строки с bus.publish(event::ZoneEntered/ZoneExited)

2. docker compose --project-directory docker up --build tests
   — все тесты PASSED, включая новые ZoneSystem_ZoneEnteredEvent и ZoneSystem_ZoneExitedEvent

3. grep "AgentEnteredZone\|AgentExitedZone" workspace/s2_core/src/zone_system.cpp
   — legacy-события остаются нетронутыми (backward compat)
</verification>

<success_criteria>
- event::ZoneEntered публикуется ZoneSystem при входе агента в зону
- event::ZoneExited публикуется ZoneSystem при выходе агента из зоны
- Поля zone_id и entity_id содержат корректные значения (подтверждено тестами)
- Legacy-события AgentEnteredZone / AgentExitedZone продолжают публиковаться
- Все тесты s2_core зелёные
- ARCH-04 считается полностью закрытым
</success_criteria>

<output>
После выполнения создать `.planning/phases/00-core-architecture-foundation/00-06-SUMMARY.md`
</output>
