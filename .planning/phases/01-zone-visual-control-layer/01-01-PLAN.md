---
phase: 01-zone-visual-control-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_core/include/s2/zone.hpp
  - workspace/s2_core/include/s2/effect_context.hpp
  - workspace/s2_core/include/s2/world_snapshot.hpp
  - workspace/s2_core/tests/test_zone_lifecycle.cpp
  - workspace/s2_core/tests/test_zone_detection_mode.cpp
  - workspace/s2_core/tests/test_zone_self_destruct.cpp
autonomous: true
requirements:
  - ZONE-04
  - ZONE-06
  - ZONE-07

must_haves:
  truths:
    - "Zone struct содержит поле strength (0.0–1.0) с дефолтом 1.0"
    - "Zone struct содержит DetectionMode enum с тремя значениями CENTER/BOUNDING/PER_LINK"
    - "Zone struct содержит SelfDestructPolicy с типами NONE/ON_ANY_CONTACT/ON_EFFECT_APPLIED"
    - "Zone struct содержит ZoneLifecycle с полями growth_rate, decay_rate, decay_delay, remove_threshold, max_strength"
    - "Zone struct содержит attached_to_link (optional<string>) и attached_to_entity_id (string)"
    - "EffectContext содержит zone_strength (double) и contact_link (string)"
    - "ZoneSnapshot содержит strength (double) и visual_hints (vector)"
    - "Все существующие тесты (test_zone_system.cpp) по-прежнему компилируются и проходят"
  artifacts:
    - path: "workspace/s2_core/include/s2/zone.hpp"
      provides: "Расширенная Zone struct с lifecycle, detection_mode, self_destruct"
      contains: "enum class DetectionMode"
    - path: "workspace/s2_core/include/s2/effect_context.hpp"
      provides: "EffectContext с zone_strength и contact_link"
      contains: "zone_strength"
    - path: "workspace/s2_core/include/s2/world_snapshot.hpp"
      provides: "ZoneSnapshot с strength и visual_hints"
      contains: "visual_hints"
    - path: "workspace/s2_core/tests/test_zone_lifecycle.cpp"
      provides: "Тесты lifecycle полей Zone struct"
    - path: "workspace/s2_core/tests/test_zone_detection_mode.cpp"
      provides: "Тесты DetectionMode enum"
    - path: "workspace/s2_core/tests/test_zone_self_destruct.cpp"
      provides: "Тесты SelfDestructPolicy"
  key_links:
    - from: "zone.hpp / Zone::ZoneLifecycle"
      to: "zone_system.cpp"
      via: "ZoneSystem читает lifecycle поля в Plan 03"
    - from: "effect_context.hpp / EffectContext::zone_strength"
      to: "zone_system.cpp apply_active_effects"
      via: "ctx.zone_strength = zone.strength при создании EffectContext"
---

<objective>
Расширить структуры данных Zone, EffectContext и ZoneSnapshot для поддержки lifecycle, detection_mode и self_destruct. Это базовые типы данных — все остальные планы Phase 1 зависят от них.

Purpose: Без этих полей невозможно реализовать lifecycle (ZONE-04), detection_mode (ZONE-06) и self_destruct (ZONE-07). Это чисто аддитивное изменение — backward-compatible добавление полей с дефолтами.
Output: Расширенные заголовочные файлы + тестовые файлы (scaffold) для верификации.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-RESEARCH.md

<interfaces>
<!-- Текущее состояние файлов — прочитать перед изменением -->

Из workspace/s2_core/include/s2/zone.hpp (текущий):
```cpp
struct Zone {
    ZoneId id;
    bool enabled{true};
    ZoneShape shape;
    std::string detection_mode{"center"};  // старое строковое поле
    std::optional<ActorId>  attached_to_actor;
    std::optional<AgentId>  attached_to_agent;
    Vec3 attachment_offset{Vec3::Zero()};
    std::string color{"#4488FF"};
    double opacity{0.3};
    bool visible{true};
    std::string label;
    struct EffectDesc { ... std::unique_ptr<EffectPlugin> plugin; ... };
    std::vector<EffectDesc> effects;
    std::unordered_set<AgentId> inside_agents;
};
```

Из workspace/s2_core/include/s2/effect_context.hpp (текущий):
```cpp
struct EffectContext {
    double sim_time{0.0};
    double dt{0.0};
    ZoneId zone_id;
    Vec3 zone_center{Vec3::Zero()};
    Vec3 zone_half_size{Vec3::Zero()};
    AgentId agent_id{0};
    Vec3 agent_position{Vec3::Zero()};
    // Нет zone_strength, нет contact_link
};
```

Из workspace/s2_core/include/s2/world_snapshot.hpp (текущий):
```cpp
struct ZoneSnapshot {
    ZoneId id;
    bool enabled{true};
    ZoneShapeType shape_type{ZoneShapeType::SPHERE};
    Vec3 center{Vec3::Zero()};
    double radius{1.0};
    Vec3 half_size{Vec3::Zero()};
    double half_height{1.0};
    std::string color{"#4488FF"};
    double opacity{0.3};
    bool visible{true};
    std::string label;
    std::vector<AgentId> agents_inside;
    // Нет strength, нет visual_hints
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Расширить zone.hpp — добавить DetectionMode, ZoneLifecycle, SelfDestructPolicy и новые поля Zone</name>
  <read_first>
    - workspace/s2_core/include/s2/zone.hpp — текущая Zone struct (прочитать полностью)
    - workspace/s2_core/include/s2/types.hpp — типы ZoneId, AgentId, Vec3
    - workspace/s2_core/tests/test_zone_system.cpp — существующие тесты (не сломать)
  </read_first>
  <files>
    workspace/s2_core/include/s2/zone.hpp,
    workspace/s2_core/include/s2/effect_context.hpp,
    workspace/s2_core/tests/test_zone_lifecycle.cpp,
    workspace/s2_core/tests/test_zone_detection_mode.cpp,
    workspace/s2_core/tests/test_zone_self_destruct.cpp
  </files>
  <behavior>
    - Тест: Zone{} по умолчанию имеет strength == 1.0
    - Тест: Zone{} по умолчанию имеет detection_mode_enum == DetectionMode::CENTER
    - Тест: Zone{} по умолчанию имеет self_destruct.type == SelfDestructPolicy::Type::NONE
    - Тест: Zone{}.lifecycle.growth_rate == 0.0 и decay_rate == 0.0 и remove_threshold == 0.05
    - Тест: EffectContext{} по умолчанию имеет zone_strength == 1.0 и contact_link == ""
    - Тест: DetectionMode enum содержит CENTER, BOUNDING, PER_LINK
    - Тест: Создать Zone с detection_mode_enum = DetectionMode::PER_LINK — компилируется
  </behavior>
  <action>
В workspace/s2_core/include/s2/zone.hpp добавить (после существующих includes, перед struct Zone):

```cpp
/// Режим обнаружения агента в зоне (per D-12).
enum class DetectionMode {
    CENTER,   ///< Центр Entity (текущее поведение по умолчанию)
    BOUNDING, ///< Bounding shape Entity пересекает форму зоны
    PER_LINK  ///< Каждый линк kinematic_tree проверяется отдельно
};

/// Политика самоуничтожения зоны (per D-13).
struct SelfDestructPolicy {
    enum class Type { NONE, ON_ANY_CONTACT, ON_EFFECT_APPLIED };
    Type type{Type::NONE};
};

/// Параметры lifecycle зоны (per D-09, D-10).
struct ZoneLifecycle {
    double initial_strength{1.0};  ///< Начальная сила при спавне (0.0–1.0)
    double growth_rate{0.0};       ///< Прирост strength/сек (0 = нет роста)
    double max_strength{1.0};      ///< Потолок роста
    double decay_delay{0.0};       ///< Секунд до начала затухания
    double decay_rate{0.0};        ///< Убыль strength/сек (0 = нет затухания)
    double remove_threshold{0.05}; ///< Удалить зону если strength < этого значения
};
```

В struct Zone добавить новые поля ПОСЛЕ поля `std::string label;`:
```cpp
    // ── Lifecycle (ZONE-04) ──────────────────────────────────────────────────
    double strength{1.0};              ///< Текущая сила зоны (0.0–1.0)
    ZoneLifecycle lifecycle;           ///< Параметры роста/затухания

    // ── Detection mode (ZONE-06) ─────────────────────────────────────────────
    DetectionMode detection_mode_enum{DetectionMode::CENTER};

    // ── Self-destruct (ZONE-07) ──────────────────────────────────────────────
    SelfDestructPolicy self_destruct;

    // ── Owned zones / per-link attachment (ZONE-09) ──────────────────────────
    std::string attached_to_entity_id; ///< Generic EntityId для owned_zones
    std::optional<std::string> attached_to_link; ///< Линк для per-link attachment
```

Старое поле `std::string detection_mode{"center"};` ОСТАВИТЬ (backward compat) — добавить комментарий `///< @deprecated Используйте detection_mode_enum`.

В workspace/s2_core/include/s2/effect_context.hpp добавить поля ПОСЛЕ `Vec3 agent_position`:
```cpp
    double zone_strength{1.0};         ///< Текущая сила зоны (0.0–1.0)
    std::string contact_link;          ///< Имя линка при PER_LINK detection (пусто = CENTER/BOUNDING)
```

Написать тестовые файлы (сначала — per TDD):

workspace/s2_core/tests/test_zone_lifecycle.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/zone.hpp>

// Тест: поля lifecycle доступны и имеют правильные дефолты
TEST(ZoneLifecycle, DefaultValues) {
    s2::Zone z;
    EXPECT_DOUBLE_EQ(z.strength, 1.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.initial_strength, 1.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.growth_rate, 0.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.decay_rate, 0.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.decay_delay, 0.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.max_strength, 1.0);
    EXPECT_DOUBLE_EQ(z.lifecycle.remove_threshold, 0.05);
}

TEST(ZoneLifecycle, CanSetStrength) {
    s2::Zone z;
    z.strength = 0.5;
    EXPECT_DOUBLE_EQ(z.strength, 0.5);
}

TEST(ZoneLifecycle, EffectContextStrength) {
    s2::EffectContext ctx;
    EXPECT_DOUBLE_EQ(ctx.zone_strength, 1.0);
    EXPECT_EQ(ctx.contact_link, "");
    ctx.zone_strength = 0.3;
    ctx.contact_link = "left_wheel";
    EXPECT_DOUBLE_EQ(ctx.zone_strength, 0.3);
    EXPECT_EQ(ctx.contact_link, "left_wheel");
}
```

workspace/s2_core/tests/test_zone_detection_mode.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/zone.hpp>

TEST(ZoneDetectionMode, DefaultIsCenter) {
    s2::Zone z;
    EXPECT_EQ(z.detection_mode_enum, s2::DetectionMode::CENTER);
}

TEST(ZoneDetectionMode, CanSetAllModes) {
    s2::Zone z;
    z.detection_mode_enum = s2::DetectionMode::BOUNDING;
    EXPECT_EQ(z.detection_mode_enum, s2::DetectionMode::BOUNDING);
    z.detection_mode_enum = s2::DetectionMode::PER_LINK;
    EXPECT_EQ(z.detection_mode_enum, s2::DetectionMode::PER_LINK);
}
```

workspace/s2_core/tests/test_zone_self_destruct.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/zone.hpp>

TEST(ZoneSelfDestruct, DefaultIsNone) {
    s2::Zone z;
    EXPECT_EQ(z.self_destruct.type, s2::SelfDestructPolicy::Type::NONE);
}

TEST(ZoneSelfDestruct, CanSetOnAnyContact) {
    s2::Zone z;
    z.self_destruct.type = s2::SelfDestructPolicy::Type::ON_ANY_CONTACT;
    EXPECT_EQ(z.self_destruct.type, s2::SelfDestructPolicy::Type::ON_ANY_CONTACT);
}

TEST(ZoneSelfDestruct, CanSetOnEffectApplied) {
    s2::Zone z;
    z.self_destruct.type = s2::SelfDestructPolicy::Type::ON_EFFECT_APPLIED;
    EXPECT_EQ(z.self_destruct.type, s2::SelfDestructPolicy::Type::ON_EFFECT_APPLIED);
}
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/include/s2/zone.hpp содержит строку `enum class DetectionMode`
    - workspace/s2_core/include/s2/zone.hpp содержит строку `double strength{1.0};`
    - workspace/s2_core/include/s2/zone.hpp содержит строку `ZoneLifecycle lifecycle;`
    - workspace/s2_core/include/s2/zone.hpp содержит строку `SelfDestructPolicy self_destruct;`
    - workspace/s2_core/include/s2/zone.hpp содержит строку `std::optional<std::string> attached_to_link;`
    - workspace/s2_core/include/s2/effect_context.hpp содержит строку `double zone_strength{1.0};`
    - workspace/s2_core/include/s2/effect_context.hpp содержит строку `std::string contact_link;`
    - Docker build tests выходит с кодом 0
    - test_zone_lifecycle, test_zone_detection_mode, test_zone_self_destruct — все тесты проходят
    - Старые тесты test_zone_system.cpp продолжают проходить
  </acceptance_criteria>
  <done>
    Zone struct, EffectContext и тестовые файлы созданы/расширены. Docker build tests проходит без ошибок.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Расширить ZoneSnapshot — добавить strength и visual_hints</name>
  <read_first>
    - workspace/s2_core/include/s2/world_snapshot.hpp — текущий ZoneSnapshot (прочитать полностью)
    - workspace/s2_core/include/s2/interfaces/effect_plugin.hpp — VisualHint struct
    - workspace/s2_visualizer/src/snapshot_json.cpp — или grep snapshot_to_json для сериализации
  </read_first>
  <files>
    workspace/s2_core/include/s2/world_snapshot.hpp
  </files>
  <behavior>
    - Тест: ZoneSnapshot{} по умолчанию имеет strength == 1.0
    - Тест: ZoneSnapshot{}.visual_hints — пустой вектор
    - Тест: После добавления visual_hint — он присутствует в векторе
    - Тест: snapshot_to_json сериализует ZoneSnapshot.strength и visual_hints в JSON (если функция доступна в тестах)
  </behavior>
  <action>
В workspace/s2_core/include/s2/world_snapshot.hpp внутри struct ZoneSnapshot добавить новые поля ПОСЛЕ `std::vector<AgentId> agents_inside;`:

```cpp
    // ── Lifecycle (ZONE-04) ──────────────────────────────────────────────────
    double strength{1.0};          ///< Текущая сила зоны (0.0–1.0)

    // ── VisualHint (ZONE-02) ─────────────────────────────────────────────────
    struct Hint {
        std::string type;          ///< "glow", "arrows", "particles", "grid"
        nlohmann::json params;     ///< {"color":"#F00", "intensity":0.5, ...}
    };
    std::vector<Hint> visual_hints; ///< Подсказки для рендеринга в Three.js
```

Убедиться что `#include <nlohmann/json.hpp>` уже есть в заголовке (он должен быть — проверить).

Обновить функцию `snapshot_to_json` — найти её реализацию (обычно в workspace/s2_visualizer/src/ или workspace/s2_core/src/) и добавить сериализацию новых полей:

В serialization ZoneSnapshot добавить:
```cpp
jz["strength"] = zs.strength;
nlohmann::json hints_arr = nlohmann::json::array();
for (const auto& h : zs.visual_hints) {
    hints_arr.push_back({{"type", h.type}, {"params", h.params}});
}
jz["visual_hints"] = hints_arr;
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/include/s2/world_snapshot.hpp содержит строку `double strength{1.0};` в ZoneSnapshot
    - workspace/s2_core/include/s2/world_snapshot.hpp содержит строку `std::vector<Hint> visual_hints;`
    - workspace/s2_core/include/s2/world_snapshot.hpp содержит вложенную struct Hint с полями type и params
    - Реализация snapshot_to_json содержит `jz["strength"] = zs.strength`
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    ZoneSnapshot расширен, snapshot_to_json сериализует strength и visual_hints. Docker build tests проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zone.hpp → ZoneSystem | Структуры данных зоны читаются ZoneSystem и плагинами |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Zone::strength | accept | strength — внутреннее состояние симуляции, не принимается напрямую из REST в этом плане |
| T-01-02 | Tampering | Zone::attached_to_entity_id | mitigate | В Plan 04 apply_kernel_command проверяет существование entity_id перед attach |
</threat_model>

<verification>
Docker build tests проходит без регрессий (все 34 существующих теста + новые тесты lifecycle/detection_mode/self_destruct).
</verification>

<success_criteria>
- `grep -r "enum class DetectionMode" workspace/s2_core/include/s2/zone.hpp` — находит результат
- `grep "zone_strength" workspace/s2_core/include/s2/effect_context.hpp` — находит результат
- `grep "visual_hints" workspace/s2_core/include/s2/world_snapshot.hpp` — находит результат
- `docker compose --project-directory docker up --build tests` выходит с кодом 0
</success_criteria>

<output>
После выполнения создать `.planning/phases/01-zone-visual-control-layer/01-01-SUMMARY.md`
</output>
