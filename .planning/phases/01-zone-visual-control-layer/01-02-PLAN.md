---
phase: 01-zone-visual-control-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_plugins/include/s2/effects/fog_effect.hpp
  - workspace/s2_plugins/include/s2/effects/emi_effect.hpp
  - workspace/s2_plugins/src/effect_factory.cpp
  - workspace/s2_plugins/tests/test_effect_fog_emi.cpp
autonomous: true
requirements:
  - ZONE-03

must_haves:
  truths:
    - "FogEffect реализует EffectPlugin, required_capabilities: [optical_sensor], effect_type: SENSOR"
    - "FogEffect.sensor_mods() при zone_strength=1.0 возвращает multiplier=range_multiplier"
    - "FogEffect.sensor_mods() интерполирует между range_multiplier и 1.0 по zone_strength"
    - "EMIEffect реализует EffectPlugin, required_capabilities: [gnss_sensor, imu_sensor], effect_type: SENSOR"
    - "EMIEffect.sensor_mods() возвращает addend=noise_addend * zone_strength"
    - "fog и emi зарегистрированы в effect_factory.cpp"
  artifacts:
    - path: "workspace/s2_plugins/include/s2/effects/fog_effect.hpp"
      provides: "FogEffect : EffectPlugin"
      contains: "class FogEffect"
    - path: "workspace/s2_plugins/include/s2/effects/emi_effect.hpp"
      provides: "EMIEffect : EffectPlugin"
      contains: "class EMIEffect"
    - path: "workspace/s2_plugins/src/effect_factory.cpp"
      provides: "Регистрация fog и emi типов"
      contains: "fog"
    - path: "workspace/s2_plugins/tests/test_effect_fog_emi.cpp"
      provides: "Тесты FogEffect и EMIEffect"
  key_links:
    - from: "fog_effect.hpp / FogEffect::sensor_mods()"
      to: "ZoneSystem::apply_active_effects()"
      via: "EffectType::SENSOR вызывается в Plan 03"
---

<objective>
Реализовать два новых сенсорных эффекта: FogEffect (туман — ухудшает дальность optical_sensor) и EMIEffect (ЭМ-помехи — добавляет шум gnss_sensor/imu_sensor). Оба используют существующий механизм sensor_mods() из EffectPlugin интерфейса.

Purpose: ZONE-03 требует сенсорные эффекты. Этот план независим от Plan 01 — EffectPlugin интерфейс уже содержит sensor_mods() и SensorMod struct.
Output: fog_effect.hpp, emi_effect.hpp, регистрация в effect_factory, тесты.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-CONTEXT.md

<interfaces>
Из workspace/s2_core/include/s2/interfaces/effect_plugin.hpp:
```cpp
class EffectPlugin {
public:
    virtual void on_init(const YAML::Node& params) = 0;
    virtual EffectType effect_type() const = 0;
    virtual std::vector<std::string> required_capabilities() const { return {}; }
    struct SensorMod {
        std::string param;       // "max_range", "noise_std"
        double multiplier{1.0};
        double addend{0.0};
    };
    virtual std::vector<SensorMod> sensor_mods(const EffectContext& ctx) const { return {}; }
    struct VisualHint {
        std::string type;       // "arrows", "particles", "glow", "grid"
        nlohmann::json params;
    };
    virtual std::optional<VisualHint> visual_hint() const { return std::nullopt; }
};
```

Образец IceModifier (из workspace/s2_plugins/include/s2/effects/ice_modifier.hpp):
```cpp
class IceModifier : public EffectPlugin {
    void on_init(const YAML::Node& p) override { scale_ = p["scale"].as<double>(0.3); }
    EffectType effect_type() const override { return EffectType::MODIFIER; }
    void apply_modifier(SharedState& state, const EffectContext& ctx) override { state.add_scale(scale_); }
    std::optional<VisualHint> visual_hint() const override {
        return VisualHint{"glow", {{"color", "#88AAFF"}, {"pulse_rate", 1.5}, {"intensity", 0.6}}};
    }
private:
    double scale_{0.3};
};
```

EffectContext (после Plan 01 добавит zone_strength — Plan 02 идёт параллельно Wave 1, zone_strength доступен):
```cpp
struct EffectContext {
    double zone_strength{1.0};  // Plan 01 добавляет это поле
    double sim_time{0.0};
    double dt{0.0};
    ZoneId zone_id;
    AgentId agent_id{0};
    Vec3 agent_position{Vec3::Zero()};
    std::string contact_link;
};
```

EffectType enum (из workspace/s2_core/include/s2/types.hpp):
```cpp
enum class EffectType { MODIFIER, CONTINUOUS, MUTATION, SENSOR };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Реализовать FogEffect и EMIEffect плагины</name>
  <read_first>
    - workspace/s2_plugins/include/s2/effects/ice_modifier.hpp — образец MODIFIER эффекта
    - workspace/s2_plugins/include/s2/effects/charging_effect.hpp — образец CONTINUOUS эффекта
    - workspace/s2_core/include/s2/interfaces/effect_plugin.hpp — интерфейс EffectPlugin (полностью)
    - workspace/s2_core/include/s2/effect_context.hpp — EffectContext поля
    - workspace/s2_core/include/s2/types.hpp — EffectType enum
  </read_first>
  <files>
    workspace/s2_plugins/include/s2/effects/fog_effect.hpp,
    workspace/s2_plugins/include/s2/effects/emi_effect.hpp,
    workspace/s2_plugins/tests/test_effect_fog_emi.cpp
  </files>
  <behavior>
    - FogEffect: effect_type() == EffectType::SENSOR
    - FogEffect: required_capabilities() == {"optical_sensor"} (ровно один элемент)
    - FogEffect: sensor_mods() при zone_strength=1.0 и range_multiplier=0.3 → [{param:"max_range", multiplier:0.3}]
    - FogEffect: sensor_mods() при zone_strength=0.5 и range_multiplier=0.3 → [{param:"max_range", multiplier:0.65}] (0.3 + 0.7*0.5)
    - FogEffect: visual_hint() возвращает VisualHint с type=="glow"
    - EMIEffect: effect_type() == EffectType::SENSOR
    - EMIEffect: required_capabilities() содержит "gnss_sensor" и "imu_sensor" (2 элемента)
    - EMIEffect: sensor_mods() при zone_strength=1.0 и noise_addend=0.5 → 2 мода с addend=0.5
    - EMIEffect: sensor_mods() при zone_strength=0.5 и noise_addend=0.5 → 2 мода с addend=0.25
    - EMIEffect: visual_hint() params["color"] == "#FFAA22"
  </behavior>
  <action>
Сначала написать тест (RED), затем реализацию (GREEN).

ВАЖНО: если zone_strength ещё не в EffectContext (Plan 01 параллелен), Plan 01 добавляет поле с дефолтом 1.0 — тесты компилируются с дефолтом.

workspace/s2_plugins/tests/test_effect_fog_emi.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/effects/fog_effect.hpp>
#include <s2/effects/emi_effect.hpp>
#include <s2/effect_context.hpp>

// ── FogEffect ────────────────────────────────────────────────────────────────

TEST(FogEffect, EffectTypeSensor) {
    s2::FogEffect fog;
    YAML::Node params;
    fog.on_init(params);
    EXPECT_EQ(fog.effect_type(), s2::EffectType::SENSOR);
}

TEST(FogEffect, RequiresOpticalSensor) {
    s2::FogEffect fog;
    YAML::Node params;
    fog.on_init(params);
    auto caps = fog.required_capabilities();
    ASSERT_EQ(caps.size(), 1u);
    EXPECT_EQ(caps[0], "optical_sensor");
}

TEST(FogEffect, SensorModFullStrength) {
    s2::FogEffect fog;
    YAML::Node params;
    params["range_multiplier"] = 0.3;
    fog.on_init(params);
    s2::EffectContext ctx;
    ctx.zone_strength = 1.0;
    auto mods = fog.sensor_mods(ctx);
    ASSERT_EQ(mods.size(), 1u);
    EXPECT_EQ(mods[0].param, "max_range");
    EXPECT_NEAR(mods[0].multiplier, 0.3, 1e-9);
}

TEST(FogEffect, SensorModHalfStrength) {
    s2::FogEffect fog;
    YAML::Node params;
    params["range_multiplier"] = 0.3;
    fog.on_init(params);
    s2::EffectContext ctx;
    ctx.zone_strength = 0.5;
    auto mods = fog.sensor_mods(ctx);
    ASSERT_EQ(mods.size(), 1u);
    // 0.3 + (1.0 - 0.3) * (1.0 - 0.5) = 0.65
    EXPECT_NEAR(mods[0].multiplier, 0.65, 1e-9);
}

TEST(FogEffect, VisualHintGlow) {
    s2::FogEffect fog;
    YAML::Node params;
    fog.on_init(params);
    auto hint = fog.visual_hint();
    ASSERT_TRUE(hint.has_value());
    EXPECT_EQ(hint->type, "glow");
}

// ── EMIEffect ────────────────────────────────────────────────────────────────

TEST(EMIEffect, EffectTypeSensor) {
    s2::EMIEffect emi;
    YAML::Node params;
    emi.on_init(params);
    EXPECT_EQ(emi.effect_type(), s2::EffectType::SENSOR);
}

TEST(EMIEffect, RequiresGnssAndImu) {
    s2::EMIEffect emi;
    YAML::Node params;
    emi.on_init(params);
    auto caps = emi.required_capabilities();
    ASSERT_EQ(caps.size(), 2u);
    EXPECT_EQ(caps[0], "gnss_sensor");
    EXPECT_EQ(caps[1], "imu_sensor");
}

TEST(EMIEffect, SensorModFullStrength) {
    s2::EMIEffect emi;
    YAML::Node params;
    params["noise_addend"] = 0.5;
    emi.on_init(params);
    s2::EffectContext ctx;
    ctx.zone_strength = 1.0;
    auto mods = emi.sensor_mods(ctx);
    ASSERT_EQ(mods.size(), 2u);
    EXPECT_EQ(mods[0].param, "noise_std");
    EXPECT_NEAR(mods[0].addend, 0.5, 1e-9);
}

TEST(EMIEffect, SensorModHalfStrength) {
    s2::EMIEffect emi;
    YAML::Node params;
    params["noise_addend"] = 0.5;
    emi.on_init(params);
    s2::EffectContext ctx;
    ctx.zone_strength = 0.5;
    auto mods = emi.sensor_mods(ctx);
    ASSERT_EQ(mods.size(), 2u);
    EXPECT_NEAR(mods[0].addend, 0.25, 1e-9);
}

TEST(EMIEffect, VisualHintGlowOrange) {
    s2::EMIEffect emi;
    YAML::Node params;
    emi.on_init(params);
    auto hint = emi.visual_hint();
    ASSERT_TRUE(hint.has_value());
    EXPECT_EQ(hint->type, "glow");
    EXPECT_EQ(hint->params["color"].get<std::string>(), "#FFAA22");
}
```

workspace/s2_plugins/include/s2/effects/fog_effect.hpp:
```cpp
#pragma once

#include <s2/interfaces/effect_plugin.hpp>
#include <yaml-cpp/yaml.h>

namespace s2 {

/**
 * @brief Эффект тумана — ухудшает дальность оптических сенсоров (per D-07, D-08).
 *
 * required_capabilities: [optical_sensor]
 * Масштабирует max_range по zone_strength:
 *   multiplier = range_multiplier + (1 - range_multiplier) * (1 - zone_strength)
 *
 * YAML параметры:
 *   range_multiplier: 0.3  # Минимальный множитель при zone_strength=1.0
 */
class FogEffect : public EffectPlugin
{
public:
    void on_init(const YAML::Node& params) override
    {
        range_multiplier_ = params["range_multiplier"].as<double>(0.3);
    }

    EffectType effect_type() const override { return EffectType::SENSOR; }

    std::vector<std::string> required_capabilities() const override
    {
        return {"optical_sensor"};
    }

    std::vector<SensorMod> sensor_mods(const EffectContext& ctx) const override
    {
        double mult = range_multiplier_ + (1.0 - range_multiplier_) * (1.0 - ctx.zone_strength);
        return {{.param = "max_range", .multiplier = mult, .addend = 0.0}};
    }

    std::optional<VisualHint> visual_hint() const override
    {
        return VisualHint{
            "glow",
            {{"color", "#AADDFF"}, {"pulse_rate", 0.5}, {"intensity", 0.4}}
        };
    }

private:
    double range_multiplier_{0.3};
};

} // namespace s2
```

workspace/s2_plugins/include/s2/effects/emi_effect.hpp:
```cpp
#pragma once

#include <s2/interfaces/effect_plugin.hpp>
#include <yaml-cpp/yaml.h>

namespace s2 {

/**
 * @brief ЭМ-помехи — добавляет шум GNSS/IMU сенсорам (per D-07, D-08).
 *
 * required_capabilities: [gnss_sensor, imu_sensor]
 * Добавляет noise_addend * zone_strength к noise_std.
 *
 * YAML параметры:
 *   noise_addend: 0.5  # Добавка шума при zone_strength=1.0
 */
class EMIEffect : public EffectPlugin
{
public:
    void on_init(const YAML::Node& params) override
    {
        noise_addend_ = params["noise_addend"].as<double>(0.5);
    }

    EffectType effect_type() const override { return EffectType::SENSOR; }

    std::vector<std::string> required_capabilities() const override
    {
        return {"gnss_sensor", "imu_sensor"};
    }

    std::vector<SensorMod> sensor_mods(const EffectContext& ctx) const override
    {
        double scaled = noise_addend_ * ctx.zone_strength;
        // Два мода — один для gnss_sensor, один для imu_sensor
        return {
            {.param = "noise_std", .multiplier = 1.0, .addend = scaled},
            {.param = "noise_std", .multiplier = 1.0, .addend = scaled}
        };
    }

    std::optional<VisualHint> visual_hint() const override
    {
        return VisualHint{
            "glow",
            {{"color", "#FFAA22"}, {"pulse_rate", 2.0}, {"intensity", 0.7}}
        };
    }

private:
    double noise_addend_{0.5};
};

} // namespace s2
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_plugins/include/s2/effects/fog_effect.hpp существует и содержит `class FogEffect`
    - workspace/s2_plugins/include/s2/effects/emi_effect.hpp существует и содержит `class EMIEffect`
    - `grep "optical_sensor" workspace/s2_plugins/include/s2/effects/fog_effect.hpp` находит результат
    - `grep "gnss_sensor" workspace/s2_plugins/include/s2/effects/emi_effect.hpp` находит результат
    - workspace/s2_plugins/tests/test_effect_fog_emi.cpp существует и содержит минимум 9 TEST()
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    FogEffect и EMIEffect реализованы. 10 тестов проходят.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Зарегистрировать fog и emi в effect_factory.cpp</name>
  <read_first>
    - workspace/s2_plugins/src/effect_factory.cpp — текущие зарегистрированные эффекты (прочитать полностью)
    - workspace/s2_plugins/include/s2/effects/ice_modifier.hpp — образец паттерна регистрации
  </read_first>
  <files>
    workspace/s2_plugins/src/effect_factory.cpp
  </files>
  <action>
Прочитать effect_factory.cpp полностью. Найти паттерн регистрации существующих эффектов (ice_modifier, charging, boost и т.д.) и применить тот же паттерн для fog и emi.

Добавить в начало файла:
```cpp
#include <s2/effects/fog_effect.hpp>
#include <s2/effects/emi_effect.hpp>
```

Добавить регистрацию по аналогии с существующим кодом:
- тип "fog" → создаёт экземпляр FogEffect
- тип "emi" → создаёт экземпляр EMIEffect

Конкретный синтаксис зависит от структуры effect_factory.cpp — прочитать файл перед изменением.
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "fog" workspace/s2_plugins/src/effect_factory.cpp` находит регистрацию
    - `grep "emi" workspace/s2_plugins/src/effect_factory.cpp` находит регистрацию
    - `grep -c "#include.*fog_effect\|#include.*emi_effect" workspace/s2_plugins/src/effect_factory.cpp` возвращает 2
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    fog и emi зарегистрированы. Docker build tests проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML params → FogEffect/EMIEffect | Параметры из YAML сцены, не из REST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | FogEffect/EMIEffect YAML params | mitigate | Использовать .as<double>(default) — YAML-cpp возвращает дефолт при ошибке |
| T-01-04 | Denial of Service | EMIEffect sensor_mods вектор | accept | Возвращает фиксированный размер 2, нет роста |
</threat_model>

<verification>
Docker build tests проходит. Все тесты test_effect_fog_emi (10 тестов) проходят. Старые тесты не регрессировали.
</verification>

<success_criteria>
- `grep "class FogEffect" workspace/s2_plugins/include/s2/effects/fog_effect.hpp` — находит результат
- `grep "class EMIEffect" workspace/s2_plugins/include/s2/effects/emi_effect.hpp` — находит результат
- `docker compose --project-directory docker up --build tests` выходит с кодом 0
</success_criteria>

<output>
После выполнения создать `.planning/phases/01-zone-visual-control-layer/01-02-SUMMARY.md`
</output>
