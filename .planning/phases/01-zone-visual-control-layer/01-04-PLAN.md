---
phase: 01-zone-visual-control-layer
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
  - 01-03-PLAN.md
files_modified:
  - workspace/s2_core/include/s2/sim_engine.hpp
  - workspace/s2_core/include/s2/zone_spawn_system.hpp
  - workspace/s2_core/src/zone_spawn_system.cpp
  - workspace/s2_core/tests/test_zone_commands.cpp
  - workspace/s2_core/tests/test_zone_spawn_triggers.cpp
autonomous: true
requirements:
  - ZONE-05
  - ZONE-08
  - ZONE-10

must_haves:
  truths:
    - "SpawnZone KernelCommand создаёт зону в ZoneSystem через apply_kernel_command()"
    - "DespawnZone KernelCommand вызывает zone_system_.remove_zone() с отправкой ZoneExited"
    - "ToggleZone KernelCommand вызывает zone_system_.toggle_zone_with_events()"
    - "SpawnZone с attached_to заполняет zone.attached_to_entity_id"
    - "ZoneSpawnSystem реализует 4 типа триггеров: command, event, timer, state_change"
    - "event trigger: ZoneSpawnSystem подписывается на EventBus, при совпадении event_type публикует SpawnZone"
    - "timer trigger: зона спавнится через N секунд sim_time"
    - "state_change trigger: ZoneSpawnSystem подписывается на ActorStateChanged, фильтрует по entity_id + state_value"
    - "ZONE-10: SpawnZone{attached_to: prop_id} — работает через тот же механизм что и ZONE-09"
  artifacts:
    - path: "workspace/s2_core/include/s2/zone_spawn_system.hpp"
      provides: "ZoneSpawnSystem с 4 типами триггеров"
      contains: "class ZoneSpawnSystem"
    - path: "workspace/s2_core/src/zone_spawn_system.cpp"
      provides: "Реализация ZoneSpawnSystem"
    - path: "workspace/s2_core/tests/test_zone_commands.cpp"
      provides: "Тесты SpawnZone/DespawnZone/ToggleZone KernelCommands"
    - path: "workspace/s2_core/tests/test_zone_spawn_triggers.cpp"
      provides: "Тесты event/timer/state_change триггеров"
  key_links:
    - from: "SimEngine::apply_kernel_command<cmd::SpawnZone>()"
      to: "ZoneSystem::add_zone()"
      via: "zone создаётся из cmd параметров"
    - from: "ZoneSpawnSystem::on_event()"
      to: "SimEngine::command_queue_"
      via: "push SpawnZone команду в очередь"
---

<objective>
Реализовать обработчики KernelCommands для зон (SpawnZone/DespawnZone/ToggleZone) в SimEngine и создать ZoneSpawnSystem с четырьмя типами триггеров (event, timer, state_change, command).

Purpose: ZONE-05 требует рабочие KernelCommands для зон. ZONE-08 требует декларативные spawn triggers. ZONE-10 переиспользует SpawnZone{attached_to} из ZONE-09 — никакого специального кода.
Output: Обработчики в sim_engine.hpp + zone_spawn_system.hpp/.cpp + тесты.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-01-SUMMARY.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-03-SUMMARY.md

<interfaces>
Из workspace/s2_core/include/s2/kernel_command.hpp (cmd::SpawnZone):
```cpp
struct SpawnZone {
    ZoneShape                shape;
    std::vector<std::string> effects;
    std::optional<EntityId>  attached_to;
    std::string              id_hint;
    bool                     visible{true};
    std::string              color{"#FFFFFF"};
    double                   opacity{0.3};
    std::string              label;
};
struct DespawnZone { ZoneId id; };
struct ToggleZone { ZoneId id; bool enabled{true}; };
```

Из workspace/s2_core/include/s2/sim_engine.hpp — apply_kernel_command (текущие заглушки):
```cpp
// Все zone команды попадают в else { (void)cmd; }
```

Из workspace/s2_core/include/s2/event_bus.hpp — события:
```cpp
// bus_.subscribe<event::ZoneEntered>([&](const auto& e) { ... });
// bus_.publish(event::ActorStateChanged{...});
```

Из workspace/s2_core/include/s2/sim_engine.hpp — поля SimEngine:
```cpp
// ZoneSystem zone_system_;
// KernelCommandQueue command_queue_;  // mutex-protected
// SimBus bus_;
// double sim_time_;
```

Формат YAML zone_templates (из RESEARCH.md):
```yaml
zone_templates:
  dust_cloud:
    shape: { type: sphere, radius: 2.0 }
    lifecycle:
      initial_strength: 0.1
      growth: { rate: 1.5, max: 1.0 }
      decay: { delay: 2.0, rate: 0.2, remove_at: 0.05 }
    spawn_trigger:
      type: event            # event | timer | state_change
      event_type: ZoneExited
      source_entity: ""      # пусто = любой
    effects:
      - type: fog
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Реализовать обработчики SpawnZone/DespawnZone/ToggleZone в SimEngine</name>
  <read_first>
    - workspace/s2_core/include/s2/sim_engine.hpp — apply_kernel_command() (прочитать полностью, найти else-ветку)
    - workspace/s2_core/include/s2/zone_system.hpp — remove_zone, toggle_zone_with_events (после Plan 03)
    - workspace/s2_core/include/s2/kernel_command.hpp — cmd::SpawnZone/DespawnZone/ToggleZone поля
    - workspace/s2_core/include/s2/zone.hpp — Zone struct, EffectDesc
    - workspace/s2_core/include/s2/scene_loader.hpp — паттерн создания EffectDesc (если используется)
    - workspace/s2_core/tests/test_zone_system.cpp — образец создания зон
  </read_first>
  <files>
    workspace/s2_core/include/s2/sim_engine.hpp,
    workspace/s2_core/tests/test_zone_commands.cpp
  </files>
  <behavior>
    - SpawnZone: zone создаётся с id = cmd.id_hint если не пустой, иначе "zone_" + next_zone_id_++
    - SpawnZone: effects строки → Zone::EffectDesc с type; plugin создаётся через effect_factory_ если задана
    - SpawnZone: attached_to задан → zone.attached_to_entity_id = *cmd.attached_to (ZONE-09, ZONE-10)
    - SpawnZone: zone добавляется через zone_system_.add_zone()
    - DespawnZone: zone_system_.remove_zone(cmd.id, world_.agents(), bus_)
    - ToggleZone: zone_system_.toggle_zone_with_events(cmd.id, cmd.enabled, world_.agents(), bus_)
    - SpawnZone с неизвестным effect_type: EffectDesc создаётся с type, plugin остаётся nullptr (не crash)
  </behavior>
  <action>
В workspace/s2_core/include/s2/sim_engine.hpp найти apply_kernel_command() и заменить общую else-ветку.

Добавить поле в SimEngine (если нет):
```cpp
int next_zone_id_{0};  ///< Счётчик для auto-generated ZoneId
```

В apply_kernel_command() добавить ветки ПЕРЕД финальным else:
```cpp
else if constexpr (std::is_same_v<T, cmd::SpawnZone>)
{
    Zone z;
    z.id = cmd.id_hint.empty()
        ? "zone_" + std::to_string(next_zone_id_++)
        : cmd.id_hint;
    z.shape   = cmd.shape;
    z.visible = cmd.visible;
    z.color   = cmd.color;
    z.opacity = cmd.opacity;
    z.label   = cmd.label;

    // Создать EffectDesc для каждого типа эффекта
    for (const auto& eff_type : cmd.effects) {
        Zone::EffectDesc desc;
        desc.type = eff_type;
        z.effects.push_back(std::move(desc));
    }

    // Привязка к entity (ZONE-09, ZONE-10)
    if (cmd.attached_to.has_value()) {
        z.attached_to_entity_id = cmd.attached_to.value();
    }

    zone_system_.add_zone(std::move(z));
}
else if constexpr (std::is_same_v<T, cmd::DespawnZone>)
{
    zone_system_.remove_zone(cmd.id, world_.agents(), bus_);
}
else if constexpr (std::is_same_v<T, cmd::ToggleZone>)
{
    zone_system_.toggle_zone_with_events(cmd.id, cmd.enabled, world_.agents(), bus_);
}
```

workspace/s2_core/tests/test_zone_commands.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/sim_engine.hpp>
#include <s2/kernel_command.hpp>

// Тест: SpawnZone создаёт зону
TEST(ZoneCommands, SpawnZoneCreatesZone) {
    // Создать SimEngine с тестовой конфигурацией
    // Отправить cmd::SpawnZone через command_queue_
    // Вызвать один tick
    // Проверить zone_system_.all_zones().size() == 1
    // Проверить zone.id == id_hint если задан
}

// Тест: DespawnZone удаляет зону
TEST(ZoneCommands, DespawnZoneRemovesZone) {
    // SpawnZone → DespawnZone → проверить all_zones().empty()
}

// Тест: ToggleZone false → ZoneExited событие для агентов внутри
TEST(ZoneCommands, ToggleZoneSendsExitEvents) {
    // Создать зону с агентом внутри
    // ToggleZone{id, false}
    // Проверить ZoneExited на EventBus
}

// Тест: SpawnZone с id_hint использует заданный id
TEST(ZoneCommands, SpawnZoneUsesIdHint) {
    // SpawnZone{id_hint: "my_zone"}
    // Проверить all_zones()[0].id == "my_zone"
}
```

ПРИМЕЧАНИЕ: Конкретная реализация тестов зависит от API SimEngine (конструктор, как передать команду, как вызвать tick). Прочитать test_zone_system.cpp для паттерна инициализации. Если SimEngine сложно создать в тесте — использовать ZoneSystem напрямую для unit-тестов обработчиков.
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 10 "is_same_v<T, cmd::SpawnZone>" workspace/s2_core/include/s2/sim_engine.hpp` находит `zone_system_.add_zone`
    - `grep "zone_system_.remove_zone" workspace/s2_core/include/s2/sim_engine.hpp` находит результат
    - `grep "toggle_zone_with_events" workspace/s2_core/include/s2/sim_engine.hpp` находит результат
    - workspace/s2_core/tests/test_zone_commands.cpp существует с тестами GTest
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    Обработчики SpawnZone/DespawnZone/ToggleZone реализованы. Тесты проходят.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: Создать ZoneSpawnSystem с 4 типами триггеров</name>
  <read_first>
    - workspace/s2_core/include/s2/event_bus.hpp — API подписки subscribe<T>(), publish()
    - workspace/s2_core/include/s2/sim_bus.hpp — SimBus = EventBus алиас
    - workspace/s2_core/include/s2/types.hpp — EventBus event типы (ActorStateChanged и др.)
    - workspace/s2_core/tests/test_zone_system.cpp — паттерн subscribe в тестах
    - workspace/s2_core/include/s2/sim_engine.hpp — как хранятся поля (bus_, zone_system_)
    - workspace/s2_core/include/s2/scene_loader.hpp — как парсить YAML zone_templates секцию
  </read_first>
  <files>
    workspace/s2_core/include/s2/zone_spawn_system.hpp,
    workspace/s2_core/src/zone_spawn_system.cpp,
    workspace/s2_core/tests/test_zone_spawn_triggers.cpp
  </files>
  <behavior>
    - timer trigger: после N секунд sim_time с момента регистрации шаблона → SpawnZone в command_queue_
    - event trigger: при получении EventBus события совпадающего по типу (и source_entity если задан) → SpawnZone
    - state_change trigger: при ActorStateChanged{entity_id, state_value} совпадающем с фильтром → SpawnZone
    - ZoneSpawnSystem::init(bus, command_queue_ref) подписывается на EventBus
    - ZoneSpawnSystem::tick(sim_time) проверяет timer триггеры
    - ZoneSpawnSystem::add_template(name, ZoneTemplate) регистрирует шаблон
    - Один шаблон может сработать несколько раз (если timer без cooldown — ровно один раз)
    - Pitfall 6 (RESEARCH.md): state_change тест использует ручной publish(ActorStateChanged{...})
  </behavior>
  <action>
workspace/s2_core/include/s2/zone_spawn_system.hpp:
```cpp
#pragma once

#include <s2/kernel_command.hpp>
#include <s2/sim_bus.hpp>
#include <s2/zone.hpp>
#include <functional>
#include <string>
#include <vector>

namespace s2 {

/**
 * @brief Система декларативного спавна зон по триггерам (per D-14, D-15, D-16).
 *
 * Типы триггеров:
 *  - timer:       зона спавнится через N секунд sim_time
 *  - event:       EventBus событие с фильтром по типу + source_entity
 *  - state_change: ActorStateChanged с фильтром entity_id + state_value
 *  - command:     прямой SpawnZone KernelCommand (уже есть, не требует ZoneSpawnSystem)
 *
 * ZoneSpawnSystem — отдельная система в SimEngine.
 * Работает только в sim_thread — не использует std::thread.
 */
class ZoneSpawnSystem
{
public:
    // ── Структуры триггеров ──────────────────────────────────────────────────

    struct TimerTrigger {
        double delay_seconds{0.0};  ///< Секунд от момента регистрации
        double trigger_time{-1.0};  ///< Абсолютное sim_time срабатывания (вычисляется в add_template)
        bool   fired{false};
    };

    struct EventTrigger {
        std::string event_type;    ///< Имя типа события: "ZoneEntered", "GrabSucceeded", ...
        std::string source_entity; ///< Фильтр по entity_id (пусто = любой)
    };

    struct StateChangeTrigger {
        std::string entity_id;     ///< Фильтр по id актора
        std::string state_value;   ///< Ожидаемое значение состояния
    };

    struct ZoneTemplate {
        std::string name;
        cmd::SpawnZone spawn_cmd;   ///< Команда создания зоны (shape, effects, color, ...)

        // Ровно один из триггеров задан (остальные — дефолт / не проверяются)
        enum class TriggerType { TIMER, EVENT, STATE_CHANGE } trigger_type{TriggerType::TIMER};
        TimerTrigger      timer;
        EventTrigger      event;
        StateChangeTrigger state_change;
    };

    // ── API ──────────────────────────────────────────────────────────────────

    /// Инициализация: подписаться на EventBus (для event и state_change триггеров).
    /// Вызывается из SimEngine::initialize() или load_world().
    void init(SimBus& bus, KernelCommandQueue& out_queue, double current_sim_time);

    /// Зарегистрировать шаблон зоны с триггером.
    void add_template(ZoneTemplate tmpl, double current_sim_time);

    /// Проверить timer-триггеры. Вызывается в Phase 0 тика.
    void tick(double sim_time);

    /// Очистить все шаблоны (при смене сцены).
    void clear();

private:
    std::vector<ZoneTemplate> templates_;
    KernelCommandQueue*       out_queue_{nullptr};
};

} // namespace s2
```

workspace/s2_core/src/zone_spawn_system.cpp:
```cpp
#include <s2/zone_spawn_system.hpp>
#include <s2/event_bus.hpp>

namespace s2 {

void ZoneSpawnSystem::init(SimBus& bus, KernelCommandQueue& out_queue, double current_sim_time)
{
    out_queue_ = &out_queue;

    // Подписка на все EventBus события для event-триггеров
    // Используем типизированную подписку на каждый нужный тип
    // Т.к. event_type — строка, нужен общий механизм
    // Вариант: подписаться на все известные типы и матчить по строке
    bus.subscribe<event::ZoneEntered>([this](const event::ZoneEntered& e) {
        check_event_triggers("ZoneEntered", e.entity_id);
    });
    bus.subscribe<event::ZoneExited>([this](const event::ZoneExited& e) {
        check_event_triggers("ZoneExited", e.entity_id);
    });
    // ... аналогично для других типов событий из event_bus.hpp

    // Подписка на ActorStateChanged для state_change триггеров
    bus.subscribe<event::ActorStateChanged>([this](const event::ActorStateChanged& e) {
        check_state_change_triggers(e.entity_id, e.state);
    });
}

void ZoneSpawnSystem::add_template(ZoneTemplate tmpl, double current_sim_time)
{
    if (tmpl.trigger_type == ZoneTemplate::TriggerType::TIMER) {
        tmpl.timer.trigger_time = current_sim_time + tmpl.timer.delay_seconds;
    }
    templates_.push_back(std::move(tmpl));
}

void ZoneSpawnSystem::tick(double sim_time)
{
    if (!out_queue_) return;
    for (auto& tmpl : templates_) {
        if (tmpl.trigger_type != ZoneTemplate::TriggerType::TIMER) continue;
        if (tmpl.timer.fired) continue;
        if (sim_time >= tmpl.timer.trigger_time) {
            out_queue_->push_back(tmpl.spawn_cmd);
            tmpl.timer.fired = true;
        }
    }
}

void ZoneSpawnSystem::clear() { templates_.clear(); }

// Приватные методы
void ZoneSpawnSystem::check_event_triggers(const std::string& event_type,
                                            const std::string& source_entity)
{
    if (!out_queue_) return;
    for (auto& tmpl : templates_) {
        if (tmpl.trigger_type != ZoneTemplate::TriggerType::EVENT) continue;
        if (tmpl.event.event_type != event_type) continue;
        if (!tmpl.event.source_entity.empty() &&
            tmpl.event.source_entity != source_entity) continue;
        out_queue_->push_back(tmpl.spawn_cmd);
    }
}

void ZoneSpawnSystem::check_state_change_triggers(const std::string& entity_id,
                                                    const std::string& state_value)
{
    if (!out_queue_) return;
    for (auto& tmpl : templates_) {
        if (tmpl.trigger_type != ZoneTemplate::TriggerType::STATE_CHANGE) continue;
        if (tmpl.state_change.entity_id != entity_id) continue;
        if (tmpl.state_change.state_value != state_value) continue;
        out_queue_->push_back(tmpl.spawn_cmd);
    }
}

} // namespace s2
```

ЗАМЕЧАНИЯ по реализации:
1. Прочитать event_bus.hpp для точных типов событий (entity_id — AgentId или std::string?)
2. event::ActorStateChanged — проверить поля в types.hpp или event_bus.hpp
3. check_event_triggers/check_state_change_triggers — добавить как private методы в .hpp
4. KernelCommandQueue — вектор, push_back thread-unsafe — ZoneSpawnSystem работает только в sim_thread (per RESEARCH.md anti-pattern: не std::thread)

workspace/s2_core/tests/test_zone_spawn_triggers.cpp:
```cpp
#include <gtest/gtest.h>
#include <s2/zone_spawn_system.hpp>
#include <s2/sim_bus.hpp>
#include <s2/kernel_command.hpp>

// Тест: timer trigger срабатывает через N секунд
TEST(ZoneSpawnTriggers, TimerFires) {
    s2::KernelCommandQueue queue;
    s2::SimBus bus;
    s2::ZoneSpawnSystem zss;
    zss.init(bus, queue, 0.0);

    s2::ZoneSpawnSystem::ZoneTemplate tmpl;
    tmpl.name = "test_timer";
    tmpl.trigger_type = s2::ZoneSpawnSystem::ZoneTemplate::TriggerType::TIMER;
    tmpl.timer.delay_seconds = 5.0;
    tmpl.spawn_cmd.id_hint = "timer_zone";
    zss.add_template(std::move(tmpl), 0.0);

    zss.tick(3.0);  // Не сработал
    EXPECT_EQ(queue.size(), 0u);

    zss.tick(5.5);  // Сработал
    EXPECT_EQ(queue.size(), 1u);

    zss.tick(6.0);  // Повторно не срабатывает (fired=true)
    EXPECT_EQ(queue.size(), 1u);
}

// Тест: event trigger срабатывает при EventBus событии
TEST(ZoneSpawnTriggers, EventFires) {
    s2::KernelCommandQueue queue;
    s2::SimBus bus;
    s2::ZoneSpawnSystem zss;
    zss.init(bus, queue, 0.0);

    s2::ZoneSpawnSystem::ZoneTemplate tmpl;
    tmpl.name = "test_event";
    tmpl.trigger_type = s2::ZoneSpawnSystem::ZoneTemplate::TriggerType::EVENT;
    tmpl.event.event_type = "ZoneEntered";
    tmpl.event.source_entity = "";  // любой
    tmpl.spawn_cmd.id_hint = "event_zone";
    zss.add_template(std::move(tmpl), 0.0);

    // Ручная публикация события
    // (точный синтаксис — прочитать event_bus.hpp)
    bus.publish(s2::event::ZoneEntered{.zone_id = "z1", .entity_id = "agent_1"});

    EXPECT_EQ(queue.size(), 1u);
}

// Тест: state_change trigger срабатывает при ActorStateChanged
TEST(ZoneSpawnTriggers, StateChangeFires) {
    s2::KernelCommandQueue queue;
    s2::SimBus bus;
    s2::ZoneSpawnSystem zss;
    zss.init(bus, queue, 0.0);

    s2::ZoneSpawnSystem::ZoneTemplate tmpl;
    tmpl.name = "test_state";
    tmpl.trigger_type = s2::ZoneSpawnSystem::ZoneTemplate::TriggerType::STATE_CHANGE;
    tmpl.state_change.entity_id = "door_1";
    tmpl.state_change.state_value = "OPEN";
    tmpl.spawn_cmd.id_hint = "state_zone";
    zss.add_template(std::move(tmpl), 0.0);

    // Ручная публикация ActorStateChanged
    // (точный синтаксис — прочитать event_bus.hpp / types.hpp)
    // bus.publish(s2::event::ActorStateChanged{...});
    // EXPECT_EQ(queue.size(), 1u);
    // NOTE: пока заглушка — добавить после изучения API ActorStateChanged
}
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/include/s2/zone_spawn_system.hpp существует и содержит `class ZoneSpawnSystem`
    - `grep "TriggerType" workspace/s2_core/include/s2/zone_spawn_system.hpp` находит enum с TIMER, EVENT, STATE_CHANGE
    - workspace/s2_core/src/zone_spawn_system.cpp существует с реализацией tick()
    - workspace/s2_core/tests/test_zone_spawn_triggers.cpp существует и содержит минимум 2 теста GTest
    - Docker build tests выходит с кодом 0
    - Тест ZoneSpawnTriggers::TimerFires проходит
  </acceptance_criteria>
  <done>
    ZoneSpawnSystem создан, 3 типа триггеров реализованы. Тест timer_fires проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST API → cmd::SpawnZone | SpawnZone поступает через REST /command endpoint |
| EventBus → ZoneSpawnSystem | EventBus события инициируют спавн зон |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | cmd::SpawnZone.attached_to несуществующий id | mitigate | В apply_kernel_command: zone.attached_to_entity_id заполняется, но если агент не найден в update_owned_zones_positions — молча игнорируется |
| T-01-09 | Denial of Service | event trigger бесконечный цикл (ZoneEntered → SpawnZone → ZoneEntered) | mitigate | ZoneSpawnSystem не добавляет cooldown в Phase 1 — документировать ограничение. В продакшн добавить max_spawns_per_template (Phase 5 backlog) |
| T-01-10 | Tampering | ZoneId injection через id_hint | mitigate | ZoneId — std::string используется как ключ только в map lookup, не исполняется |
</threat_model>

<verification>
Docker build tests проходит. Тесты ZoneCommands (SpawnZone/DespawnZone/ToggleZone) и ZoneSpawnTriggers (timer/event) проходят.
</verification>

<success_criteria>
- `grep "zone_system_.add_zone" workspace/s2_core/include/s2/sim_engine.hpp` — находит в SpawnZone ветке
- `grep "class ZoneSpawnSystem" workspace/s2_core/include/s2/zone_spawn_system.hpp` — находит результат
- `docker compose --project-directory docker up --build tests` выходит с кодом 0
</success_criteria>

<output>
После выполнения создать `.planning/phases/01-zone-visual-control-layer/01-04-SUMMARY.md`
</output>
