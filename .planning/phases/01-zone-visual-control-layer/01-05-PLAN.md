---
phase: 01-zone-visual-control-layer
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
  - 01-03-PLAN.md
  - 01-04-PLAN.md
files_modified:
  - workspace/s2_core/include/s2/scene_loader.hpp
  - workspace/s2_core/include/s2/sim_engine.hpp
autonomous: true
requirements:
  - ZONE-04
  - ZONE-08
  - ZONE-09

must_haves:
  truths:
    - "SceneLoader парсит detection_mode строку и конвертирует в DetectionMode enum"
    - "SceneLoader парсит lifecycle секцию: initial_strength, growth_rate, max_strength, decay_delay, decay_rate, remove_threshold"
    - "SceneLoader парсит self_destruct_policy: none/on_any_contact/on_effect_applied"
    - "SceneLoader парсит owned_zones у агентов: список зон с attached_to_link"
    - "SceneLoader парсит zone_templates секцию для ZoneSpawnSystem"
    - "SimEngine::build_snapshot() заполняет ZoneSnapshot.strength из zone.strength"
    - "SimEngine::build_snapshot() собирает visual_hints из zone.effects[].plugin->visual_hint()"
    - "SimEngine::phase0_kernel_commands() вызывает zone_spawn_system_.tick(sim_time)"
  artifacts:
    - path: "workspace/s2_core/include/s2/scene_loader.hpp"
      provides: "Расширенный парсинг зон: lifecycle, detection_mode, owned_zones, zone_templates"
      contains: "owned_zones"
    - path: "workspace/s2_core/include/s2/sim_engine.hpp"
      provides: "build_snapshot с strength/visual_hints + zone_spawn_system_ инициализация"
      contains: "zone_spawn_system_"
  key_links:
    - from: "scene_loader.hpp / YAML owned_zones"
      to: "SimEngine::load_world()"
      via: "SceneLoader::load() возвращает агентов с owned_zones → SpawnZone команды"
    - from: "SimEngine::build_snapshot()"
      to: "ZoneSnapshot::visual_hints"
      via: "zone.effects[i].plugin->visual_hint() собирается в snapshot"
---

<objective>
Расширить SceneLoader для парсинга новых YAML полей (lifecycle, detection_mode enum, self_destruct, owned_zones, zone_templates) и обновить SimEngine: сборку ZoneSnapshot.strength/visual_hints в build_snapshot(), инициализацию ZoneSpawnSystem, вызов zone_spawn_system_.tick().

Purpose: Plans 01-04 создали типы и логику — этот план связывает их с загрузкой сцены и snapshot-сборкой, замыкая backend pipeline ZONE-04/08/09.
Output: scene_loader.hpp расширен, sim_engine.hpp дополнен.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-CONTEXT.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-01-SUMMARY.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-03-SUMMARY.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-04-SUMMARY.md

<interfaces>
Из workspace/s2_core/include/s2/scene_loader.hpp — текущий парсинг зоны (строки 356–413):
```cpp
// Читает: id, enabled, color, opacity, visible, label, detection_mode (строка),
//         shape, attached_to (actor name), effects
// НЕ читает: lifecycle, DetectionMode enum, self_destruct, owned_zones, zone_templates
z.detection_mode = zn["detection_mode"].as<std::string>("center");
```

Из workspace/s2_core/include/s2/zone.hpp (после Plan 01):
```cpp
enum class DetectionMode { CENTER, BOUNDING, PER_LINK };
struct ZoneLifecycle { initial_strength, growth_rate, max_strength, decay_delay, decay_rate, remove_threshold };
struct SelfDestructPolicy { enum class Type { NONE, ON_ANY_CONTACT, ON_EFFECT_APPLIED }; };
struct Zone {
    DetectionMode detection_mode_enum{DetectionMode::CENTER};
    ZoneLifecycle lifecycle;
    SelfDestructPolicy self_destruct;
    std::string attached_to_entity_id;
    std::optional<std::string> attached_to_link;
    // ... existing fields
};
```

Из workspace/s2_core/include/s2/world_snapshot.hpp (после Plan 01):
```cpp
struct ZoneSnapshot {
    double strength{1.0};
    struct Hint { std::string type; nlohmann::json params; };
    std::vector<Hint> visual_hints;
    // ... existing fields
};
```

Из workspace/s2_core/include/s2/zone_spawn_system.hpp (после Plan 04):
```cpp
class ZoneSpawnSystem {
    void init(SimBus& bus, KernelCommandQueue& out_queue, double current_sim_time);
    void add_template(ZoneTemplate tmpl, double current_sim_time);
    void tick(double sim_time);
    void clear();
};
```

Рекомендуемый YAML формат owned_zones (из RESEARCH.md):
```yaml
- type: agent
  name: delivery_robot
  plugins:
    - type: diff_drive
  owned_zones:
    - id: freezer_compartment
      shape: { type: sphere, radius: 0.3 }
      attached_to_link: cargo_link
      color: "#88AAFF"
      effects:
        - type: fog
```

Рекомендуемый YAML формат zone_templates:
```yaml
zone_templates:
  dust_cloud:
    shape: { type: sphere, radius: 2.0 }
    lifecycle:
      initial_strength: 0.1
      growth_rate: 1.5
      max_strength: 1.0
      decay_delay: 2.0
      decay_rate: 0.2
      remove_threshold: 0.05
    spawn_trigger:
      type: event
      event_type: ZoneExited
      source_entity: ""
    effects:
      - type: fog
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Расширить SceneLoader — lifecycle, detection_mode enum, self_destruct, owned_zones, zone_templates</name>
  <read_first>
    - workspace/s2_core/include/s2/scene_loader.hpp — текущий парсинг зон строки 356–413 (прочитать полностью)
    - workspace/s2_core/include/s2/zone.hpp — DetectionMode, ZoneLifecycle, SelfDestructPolicy (после Plan 01)
    - workspace/s2_core/include/s2/zone_spawn_system.hpp — ZoneTemplate struct (после Plan 04)
    - workspace/s2_core/include/s2/agent.hpp — Agent struct (для owned_zones)
    - workspace/s2_configs/scenes/ — существующие YAML сцены (посмотреть как агенты объявляются)
  </read_first>
  <files>
    workspace/s2_core/include/s2/scene_loader.hpp
  </files>
  <action>
В блоке парсинга Zone (строки ~356–413) ДОПОЛНИТЬ (не заменять) существующий код:

1. **detection_mode enum**. После строки `z.detection_mode = zn["detection_mode"].as<std::string>("center");` добавить:
```cpp
// Конвертировать строку в enum (per D-12)
{
    const std::string dm_str = zn["detection_mode"].as<std::string>("center");
    if (dm_str == "bounding")   z.detection_mode_enum = DetectionMode::BOUNDING;
    else if (dm_str == "per_link") z.detection_mode_enum = DetectionMode::PER_LINK;
    else                           z.detection_mode_enum = DetectionMode::CENTER;
}
```

2. **lifecycle секция**:
```cpp
if (const auto& lc = zn["lifecycle"]) {
    z.lifecycle.initial_strength  = lc["initial_strength"].as<double>(1.0);
    z.lifecycle.growth_rate       = lc["growth_rate"].as<double>(0.0);
    z.lifecycle.max_strength      = lc["max_strength"].as<double>(1.0);
    z.lifecycle.decay_delay       = lc["decay_delay"].as<double>(0.0);
    z.lifecycle.decay_rate        = lc["decay_rate"].as<double>(0.0);
    z.lifecycle.remove_threshold  = lc["remove_threshold"].as<double>(0.05);
    z.strength = z.lifecycle.initial_strength;  // установить начальную силу
}
```

3. **self_destruct_policy**:
```cpp
if (const auto& sd = zn["self_destruct_policy"]) {
    std::string sdt = sd.as<std::string>("none");
    if (sdt == "on_any_contact")
        z.self_destruct.type = SelfDestructPolicy::Type::ON_ANY_CONTACT;
    else if (sdt == "on_effect_applied")
        z.self_destruct.type = SelfDestructPolicy::Type::ON_EFFECT_APPLIED;
    else
        z.self_destruct.type = SelfDestructPolicy::Type::NONE;
}
```

4. **owned_zones у агентов**. Найти блок парсинга агентов (где добавляются агенты в scene.agents). После базовой инициализации агента добавить:
```cpp
// owned_zones — спавнить как зоны с attached_to_entity_id = agent.id
if (const auto& owned = an["owned_zones"]) {
    for (const auto& zn_owned : owned) {
        Zone oz;
        oz.id = zn_owned["id"].as<std::string>(
            agent.name + "_zone_" + std::to_string(scene.zones.size()));
        oz.color   = zn_owned["color"].as<std::string>("#4488FF");
        oz.opacity = zn_owned["opacity"].as<double>(0.3);
        oz.visible = zn_owned["visible"].as<bool>(true);
        if (zn_owned["shape"]) oz.shape = parse_zone_shape(zn_owned["shape"]);
        oz.attached_to_entity_id = std::to_string(agent.id);
        if (zn_owned["attached_to_link"])
            oz.attached_to_link = zn_owned["attached_to_link"].as<std::string>();
        // Эффекты — по тому же паттерну что и zone.effects
        if (zn_owned["effects"]) {
            for (const auto& en : zn_owned["effects"]) {
                Zone::EffectDesc desc;
                desc.type   = en["type"].as<std::string>("");
                desc.params = en["params"] ? en["params"] : YAML::Node{};
                oz.effects.push_back(std::move(desc));
            }
        }
        scene.zones.push_back(std::move(oz));
    }
}
```

5. **zone_templates секция** (для ZoneSpawnSystem). Добавить после парсинга zones:
```cpp
if (const auto& templates_node = root["s2"]["zone_templates"]) {
    for (const auto& tmpl_kv : templates_node) {
        std::string tmpl_name = tmpl_kv.first.as<std::string>();
        const auto& tmpl_node = tmpl_kv.second;

        ZoneSpawnSystem::ZoneTemplate tmpl;
        tmpl.name = tmpl_name;

        // Парсить spawn_cmd (shape + effects + color + ...)
        if (tmpl_node["shape"]) tmpl.spawn_cmd.shape = parse_zone_shape(tmpl_node["shape"]);
        tmpl.spawn_cmd.color   = tmpl_node["color"].as<std::string>("#4488FF");
        tmpl.spawn_cmd.opacity = tmpl_node["opacity"].as<double>(0.3);
        if (tmpl_node["effects"]) {
            for (const auto& en : tmpl_node["effects"]) {
                tmpl.spawn_cmd.effects.push_back(en["type"].as<std::string>(""));
            }
        }

        // Парсить lifecycle (если есть — добавить в Zone при спавне; сохранить в template)
        // NOTE: lifecycle в шаблоне — пока только сохраняется, SimEngine применяет при SpawnZone

        // Парсить spawn_trigger
        if (const auto& st = tmpl_node["spawn_trigger"]) {
            std::string tt = st["type"].as<std::string>("timer");
            if (tt == "timer") {
                tmpl.trigger_type = ZoneSpawnSystem::ZoneTemplate::TriggerType::TIMER;
                tmpl.timer.delay_seconds = st["delay"].as<double>(0.0);
            } else if (tt == "event") {
                tmpl.trigger_type = ZoneSpawnSystem::ZoneTemplate::TriggerType::EVENT;
                tmpl.event.event_type    = st["event_type"].as<std::string>("");
                tmpl.event.source_entity = st["source_entity"].as<std::string>("");
            } else if (tt == "state_change") {
                tmpl.trigger_type = ZoneSpawnSystem::ZoneTemplate::TriggerType::STATE_CHANGE;
                tmpl.state_change.entity_id   = st["entity_id"].as<std::string>("");
                tmpl.state_change.state_value = st["state_value"].as<std::string>("");
            }
        }

        scene.zone_templates.push_back(std::move(tmpl));
    }
}
```

ВАЖНО: добавить `std::vector<ZoneSpawnSystem::ZoneTemplate> zone_templates;` в struct Scene (в scene_loader.hpp), и соответствующий include `#include <s2/zone_spawn_system.hpp>`.
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "owned_zones" workspace/s2_core/include/s2/scene_loader.hpp` находит парсинг
    - `grep "detection_mode_enum" workspace/s2_core/include/s2/scene_loader.hpp` находит конвертацию
    - `grep "lifecycle" workspace/s2_core/include/s2/scene_loader.hpp` находит парсинг lifecycle секции
    - `grep "self_destruct" workspace/s2_core/include/s2/scene_loader.hpp` находит парсинг
    - `grep "zone_templates" workspace/s2_core/include/s2/scene_loader.hpp` находит секцию zone_templates
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    SceneLoader расширен. Docker build tests проходит.
  </done>
</task>

<task type="auto">
  <name>Задача 2: SimEngine — build_snapshot ZoneSnapshot.strength/visual_hints + ZoneSpawnSystem инициализация</name>
  <read_first>
    - workspace/s2_core/include/s2/sim_engine.hpp — build_snapshot() метод, поля SimEngine, phase0_kernel_commands() (прочитать полностью)
    - workspace/s2_core/include/s2/world_snapshot.hpp — ZoneSnapshot struct (после Plan 01)
    - workspace/s2_core/include/s2/zone.hpp — Zone struct поля
    - workspace/s2_core/include/s2/zone_spawn_system.hpp — ZoneSpawnSystem API (после Plan 04)
    - workspace/s2_core/include/s2/interfaces/effect_plugin.hpp — VisualHint struct
  </read_first>
  <files>
    workspace/s2_core/include/s2/sim_engine.hpp
  </files>
  <action>
1. **Добавить ZoneSpawnSystem в SimEngine**. Найти блок полей private SimEngine и добавить:
```cpp
ZoneSpawnSystem zone_spawn_system_;  ///< Система спавна зон по триггерам (ZONE-08)
```

Добавить include:
```cpp
#include <s2/zone_spawn_system.hpp>
```

2. **Инициализация ZoneSpawnSystem**. В методе load_world() (или initialize()) после инициализации zone_system_ добавить:
```cpp
zone_spawn_system_.clear();
zone_spawn_system_.init(bus_, command_queue_, sim_time_);
// Зарегистрировать шаблоны из сцены
for (auto& tmpl : scene.zone_templates) {
    zone_spawn_system_.add_template(std::move(tmpl), sim_time_);
}
```

3. **Вызов zone_spawn_system_.tick()** в phase0_kernel_commands():
```cpp
void phase0_kernel_commands() {
    // ... существующий drain command_queue_ ...
    zone_spawn_system_.tick(sim_time_);  // Проверить timer-триггеры (ZONE-08)
    // ... остальной код ...
}
```

4. **build_snapshot() — заполнить strength и visual_hints**. Найти блок создания ZoneSnapshot и добавить/изменить:
```cpp
for (const auto& zone : zone_system_.all_zones()) {
    ZoneSnapshot zs;
    // ... существующее заполнение id, enabled, shape_type, center, color, opacity ...
    
    // НОВОЕ: strength (ZONE-04, per Plan 01)
    zs.strength = zone.strength;
    
    // НОВОЕ: visual_hints (ZONE-02) — собрать из всех эффектов зоны
    for (const auto& eff_desc : zone.effects) {
        if (eff_desc.plugin) {
            auto hint = eff_desc.plugin->visual_hint();
            if (hint.has_value()) {
                ZoneSnapshot::Hint h;
                h.type   = hint->type;
                h.params = hint->params;
                zs.visual_hints.push_back(std::move(h));
            }
        }
    }
    
    // ... существующее добавление агентов внутри ...
    snapshot.zones.push_back(std::move(zs));
}
```

5. **update_owned_zones_positions в Phase 6**. В phase6_attachments() добавить:
```cpp
zone_system_.update_owned_zones_positions(world_.agents());
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "zone_spawn_system_" workspace/s2_core/include/s2/sim_engine.hpp` находит поле и инициализацию
    - `grep "zone_spawn_system_.tick" workspace/s2_core/include/s2/sim_engine.hpp` находит вызов в phase0
    - `grep "zs.strength = zone.strength" workspace/s2_core/include/s2/sim_engine.hpp` находит результат
    - `grep "visual_hint" workspace/s2_core/include/s2/sim_engine.hpp` находит сборку visual_hints
    - `grep "update_owned_zones_positions" workspace/s2_core/include/s2/sim_engine.hpp` находит вызов в phase6
    - Docker build tests выходит с кодом 0
  </acceptance_criteria>
  <done>
    SimEngine: ZoneSpawnSystem инициализирован, build_snapshot заполняет strength/visual_hints, phase6 обновляет owned_zones позиции. Docker build tests проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML файл → SceneLoader | Файл сцены загружается при старте и /scenes/load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | SceneLoader YAML owned_zones.id | mitigate | auto-generate id если пустой; id — только ключ в map, не исполняется |
| T-01-12 | Tampering | zone_templates spawn_trigger event_type | accept | event_type — строка для матчинга, не eval; неизвестный тип = триггер никогда не сработает |
| T-01-13 | Information Disclosure | ZoneSnapshot.visual_hints в SSE | accept | visual_hints — визуальные параметры, не секретные данные; клиент браузер получает их для рендеринга |
</threat_model>

<verification>
Docker build tests проходит. Сцена test_phase0.yaml (если содержит зоны) загружается без ошибок.
</verification>

<success_criteria>
- `grep "owned_zones" workspace/s2_core/include/s2/scene_loader.hpp` — находит результат
- `grep "zs.strength = zone.strength" workspace/s2_core/include/s2/sim_engine.hpp` — находит результат
- `docker compose --project-directory docker up --build tests` выходит с кодом 0
</success_criteria>

<output>
После выполнения создать `.planning/phases/01-zone-visual-control-layer/01-05-SUMMARY.md`
</output>
