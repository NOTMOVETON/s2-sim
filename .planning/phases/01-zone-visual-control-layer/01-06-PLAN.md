---
phase: 01-zone-visual-control-layer
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_visualizer/web/index.html
  - workspace/s2_visualizer/web/js/app.js
autonomous: false
requirements:
  - ZONE-01
  - ZONE-02

must_haves:
  truths:
    - "В Edit Scene панели появилась вкладка 'Зоны' рядом с 'Геометрия' и 'Агенты'"
    - "Форма создания зоны имеет: тип формы (sphere/box/cylinder), позицию X/Y, размер, цвет, opacity, dropdown эффектов"
    - "Dropdown эффектов hardcoded: ice, boost, lock, charging, conveyor, wind, teleport, fog, emi (per D-03)"
    - "Кнопка + Добавить зону → форма → Применить → POST /command?cmd=SpawnZone"
    - "Кнопка Delete в форме редактирования → POST /command?cmd=DespawnZone (per D-04)"
    - "Список зон отображается в панели с id и кнопкой Edit"
    - "app.js рендерит VisualHint type glow через Three.js PointLight под центром зоны"
    - "app.js рендерит VisualHint type arrows через ArrowHelper"
    - "app.js рендерит VisualHint type particles через Three.js Points"
    - "app.js рендерит VisualHint type grid через GridHelper"
  artifacts:
    - path: "workspace/s2_visualizer/web/index.html"
      provides: "Вкладка Зоны с формой CRUD"
      contains: "editor-tab-zones"
    - path: "workspace/s2_visualizer/web/js/app.js"
      provides: "Zone Inspector логика + VisualHint рендеринг"
      contains: "renderVisualHints"
  key_links:
    - from: "app.js / confirmZoneForm()"
      to: "/command?cmd=SpawnZone (REST)"
      via: "fetch POST с JSON параметрами зоны"
    - from: "app.js / renderVisualHints()"
      to: "ZoneSnapshot.visual_hints"
      via: "data.zones[i].visual_hints читается в updateWorld()"
---

<objective>
Добавить UI Zone Inspector (вкладка Зоны в Edit Scene) и реализовать рендеринг четырёх типов VisualHint в Three.js (glow/arrows/particles/grid).

Purpose: ZONE-01 требует UI для создания/удаления зон. ZONE-02 требует визуальные индикаторы типа зоны через VisualHint. Этот план полностью независим от backend (Wave 1).
Output: Новая вкладка Зоны в index.html, zone inspector логика + visual hint рендеринг в app.js.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/phases/01-zone-visual-control-layer/01-CONTEXT.md

<interfaces>
Из workspace/s2_visualizer/web/index.html — структура editor tabs (строки 635–639):
```html
<div class="editor-tabs">
    <button class="editor-tab-btn active" data-tab="geometry" onclick="switchEditorTab('geometry')">Геометрия</button>
    <button class="editor-tab-btn" data-tab="agents" onclick="switchEditorTab('agents')">Агенты</button>
</div>
<div id="editor-tab-geometry"> ... </div>
<div id="editor-tab-agents" style="display:none"> ... </div>
```

Из workspace/s2_visualizer/web/js/app.js — switchEditorTab (строки 2181–2204):
```javascript
window.switchEditorTab = function(tab) {
    activeEditorTab = tab;
    document.getElementById('editor-tab-geometry').style.display = tab === 'geometry' ? '' : 'none';
    document.getElementById('editor-tab-agents').style.display   = tab === 'agents' ? '' : 'none';
    document.querySelectorAll('.editor-tab-btn').forEach(b => {
        b.classList.toggle('active', b.dataset.tab === tab);
    });
    // ...
};
```

Из workspace/s2_visualizer/web/js/app.js — updateWorld() зоны (строки 1564–1620):
```javascript
if (data.zones) {
    data.zones.forEach(z => {
        const key = `zone_${z.id}`;
        // ... рендеринг sphere/aabb/cylinder через updateOrCreateMesh ...
        // НЕТ visual_hints рендеринга
    });
}
```

Из workspace/s2_visualizer/web/js/app.js — sendCommand (строка 1626):
```javascript
function sendCommand(cmd) {
    fetch(`http://${host}:${port}/command?cmd=${cmd}`, { method: 'POST' })
        .then(r => r.json())...
}
```

Из workspace/s2_visualizer/web/js/app.js — updateOrCreateMesh (есть в app.js):
```javascript
// Создаёт или обновляет Three.js меш по ключу
updateOrCreateMesh(key, type, pose, geomParams, matParams)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Добавить вкладку Зоны в index.html и Zone Inspector логику в app.js</name>
  <read_first>
    - workspace/s2_visualizer/web/index.html — editor-tabs блок (строки 635–750), паттерн вкладки agents (строки 694–755) (прочитать полностью)
    - workspace/s2_visualizer/web/js/app.js — switchEditorTab (строки 2181–2204), sendCommand, updateWorld zones секция (строки 1562–1620) (прочитать полностью эти секции)
  </read_first>
  <files>
    workspace/s2_visualizer/web/index.html,
    workspace/s2_visualizer/web/js/app.js
  </files>
  <action>
В workspace/s2_visualizer/web/index.html:

1. В `<div class="editor-tabs">` добавить кнопку ПОСЛЕ Агенты:
```html
<button class="editor-tab-btn" data-tab="zones" onclick="switchEditorTab('zones')">Зоны</button>
```

2. После блока `<div id="editor-tab-agents" ...>` добавить новую вкладку:
```html
<!-- Вкладка: Зоны -->
<div id="editor-tab-zones" style="display:none">
    <!-- Список зон -->
    <div id="zones-list-view">
        <button class="save" onclick="startAddZone()" style="width:100%;margin-bottom:6px;">+ Добавить зону</button>
        <div id="zone-list"></div>
    </div>

    <!-- Форма создания/редактирования зоны -->
    <div id="zone-form-view" style="display:none">
        <h5 id="zf-title">Новая зона</h5>
        <label>ID: <input type="text" id="zf-id" style="width:120px;"></label>
        <label>Форма:
            <select id="zf-shape" onchange="onZoneShapeChange()">
                <option value="sphere">Sphere</option>
                <option value="box">Box</option>
                <option value="cylinder">Cylinder</option>
            </select>
        </label>
        <div id="zf-sphere-params">
            <label>Radius: <input type="number" id="zf-radius" step="0.1" value="2.0" min="0.1" style="width:70px;"></label>
        </div>
        <div id="zf-box-params" style="display:none">
            <label>W: <input type="number" id="zf-bx" step="0.1" value="2.0" min="0.1" style="width:60px;"></label>
            <label>D: <input type="number" id="zf-by" step="0.1" value="2.0" min="0.1" style="width:60px;"></label>
            <label>H: <input type="number" id="zf-bz" step="0.1" value="2.0" min="0.1" style="width:60px;"></label>
        </div>
        <div id="zf-cylinder-params" style="display:none">
            <label>Radius: <input type="number" id="zf-cr" step="0.1" value="2.0" min="0.1" style="width:60px;"></label>
            <label>Height: <input type="number" id="zf-ch" step="0.1" value="1.0" min="0.1" style="width:60px;"></label>
        </div>
        <label>X: <input type="number" id="zf-x" step="0.1" value="0" style="width:70px;"></label>
        <label>Y: <input type="number" id="zf-y" step="0.1" value="0" style="width:70px;"></label>
        <label>Цвет: <input type="color" id="zf-color" value="#4488FF"></label>
        <label>Прозрачность: <input type="range" id="zf-opacity" min="0" max="1" step="0.05" value="0.3"></label>
        <label>Эффекты:<br>
            <select id="zf-effects" multiple size="5" style="width:100%;">
                <option value="ice">ice</option>
                <option value="boost">boost</option>
                <option value="lock">lock</option>
                <option value="charging">charging</option>
                <option value="conveyor">conveyor</option>
                <option value="wind">wind</option>
                <option value="teleport">teleport</option>
                <option value="fog">fog</option>
                <option value="emi">emi</option>
            </select>
        </label>
        <div style="display:flex;gap:4px;margin-top:6px;">
            <button class="save" onclick="confirmZoneForm()" style="flex:1;">Применить</button>
            <button onclick="cancelZoneForm()">Отмена</button>
        </div>
        <button id="zf-delete-btn" class="danger" onclick="deleteCurrentZone()" style="width:100%;margin-top:6px;display:none;">Удалить</button>
    </div>
</div>
```

В workspace/s2_visualizer/web/js/app.js:

1. В `switchEditorTab()` добавить поддержку вкладки zones:
```javascript
window.switchEditorTab = function(tab) {
    activeEditorTab = tab;
    document.getElementById('editor-tab-geometry').style.display = tab === 'geometry' ? '' : 'none';
    document.getElementById('editor-tab-agents').style.display   = tab === 'agents' ? '' : 'none';
    document.getElementById('editor-tab-zones').style.display    = tab === 'zones'  ? '' : 'none';
    document.querySelectorAll('.editor-tab-btn').forEach(b => {
        b.classList.toggle('active', b.dataset.tab === tab);
    });
    if (tab === 'agents') {
        // ...существующий код...
    } else if (tab === 'zones') {
        renderZoneList();
    } else {
        closeAgentForm();
    }
};
```

2. Добавить переменные состояния в начало соответствующей секции:
```javascript
// Zone Inspector state
let editorZones = [];        // список зон из snapshot
let editingZoneId = null;    // null = новая зона, string = редактирование

function renderZoneList() {
    // Заполнить #zone-list из editorZones (читаем из последнего snapshot)
    const container = document.getElementById('zone-list');
    if (!container) return;
    container.innerHTML = '';
    editorZones.forEach(z => {
        const div = document.createElement('div');
        div.className = 'agent-list-item';  // переиспользуем существующий стиль
        div.innerHTML = `<span>${z.id || 'zone'}</span>
            <button onclick="startEditZone('${z.id}')">Edit</button>`;
        container.appendChild(div);
    });
}

window.startAddZone = function() {
    editingZoneId = null;
    document.getElementById('zf-title').textContent = 'Новая зона';
    document.getElementById('zf-id').value = '';
    document.getElementById('zf-x').value = '0';
    document.getElementById('zf-y').value = '0';
    document.getElementById('zf-radius').value = '2.0';
    document.getElementById('zf-color').value = '#4488FF';
    document.getElementById('zf-opacity').value = '0.3';
    document.getElementById('zf-delete-btn').style.display = 'none';
    document.getElementById('zones-list-view').style.display = 'none';
    document.getElementById('zone-form-view').style.display = '';
};

window.startEditZone = function(zoneId) {
    editingZoneId = zoneId;
    const z = editorZones.find(z => z.id === zoneId);
    if (!z) return;
    document.getElementById('zf-title').textContent = 'Редактировать зону';
    document.getElementById('zf-id').value = zoneId;
    document.getElementById('zf-color').value = z.color || '#4488FF';
    document.getElementById('zf-delete-btn').style.display = '';
    document.getElementById('zones-list-view').style.display = 'none';
    document.getElementById('zone-form-view').style.display = '';
};

window.cancelZoneForm = function() {
    document.getElementById('zone-form-view').style.display = 'none';
    document.getElementById('zones-list-view').style.display = '';
};

window.confirmZoneForm = function() {
    const host = window.location.hostname || 'localhost';
    const port = window.location.port || '1937';
    const shapeType = document.getElementById('zf-shape').value;
    const idHint = document.getElementById('zf-id').value.trim();
    const x = parseFloat(document.getElementById('zf-x').value) || 0;
    const y = parseFloat(document.getElementById('zf-y').value) || 0;
    const color = document.getElementById('zf-color').value;
    const opacity = parseFloat(document.getElementById('zf-opacity').value);

    // Собрать выбранные эффекты
    const effectsSelect = document.getElementById('zf-effects');
    const effects = Array.from(effectsSelect.selectedOptions).map(o => o.value);

    // Собрать shape
    let shape;
    if (shapeType === 'sphere') {
        const r = parseFloat(document.getElementById('zf-radius').value) || 2.0;
        shape = { type: 'sphere', center: [x, y, 0], radius: r };
    } else if (shapeType === 'box') {
        const bx = parseFloat(document.getElementById('zf-bx').value) || 2.0;
        const by = parseFloat(document.getElementById('zf-by').value) || 2.0;
        const bz = parseFloat(document.getElementById('zf-bz').value) || 2.0;
        shape = { type: 'box', center: [x, y, 0], half_size: [bx/2, by/2, bz/2] };
    } else {
        const cr = parseFloat(document.getElementById('zf-cr').value) || 2.0;
        const ch = parseFloat(document.getElementById('zf-ch').value) || 1.0;
        shape = { type: 'cylinder', center: [x, y, 0], radius: cr, half_height: ch/2 };
    }

    const cmd = { shape, effects, color, opacity,
                  id_hint: idHint, visible: true };

    fetch(`http://${host}:${port}/command`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ type: 'SpawnZone', ...cmd })
    }).catch(e => console.error('[ZoneForm]', e));

    cancelZoneForm();
};

window.deleteCurrentZone = function() {
    if (!editingZoneId) return;
    const host = window.location.hostname || 'localhost';
    const port = window.location.port || '1937';
    fetch(`http://${host}:${port}/command`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ type: 'DespawnZone', id: editingZoneId })
    }).catch(e => console.error('[ZoneDelete]', e));
    cancelZoneForm();
};

window.onZoneShapeChange = function() {
    const s = document.getElementById('zf-shape').value;
    document.getElementById('zf-sphere-params').style.display   = s === 'sphere'   ? '' : 'none';
    document.getElementById('zf-box-params').style.display      = s === 'box'      ? '' : 'none';
    document.getElementById('zf-cylinder-params').style.display = s === 'cylinder' ? '' : 'none';
};
```

3. В updateWorld() в блоке `if (data.zones)` обновлять editorZones:
```javascript
if (data.zones) {
    editorZones = data.zones;  // Сохранить для Zone Inspector
    // ... существующий рендеринг zone меш ...
}
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build sim 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "editor-tab-zones" workspace/s2_visualizer/web/index.html` находит div
    - `grep 'data-tab="zones"' workspace/s2_visualizer/web/index.html` находит кнопку
    - `grep "fog" workspace/s2_visualizer/web/index.html` находит option в zf-effects select
    - `grep "emi" workspace/s2_visualizer/web/index.html` находит option в zf-effects select
    - `grep "confirmZoneForm" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "deleteCurrentZone" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "DespawnZone" workspace/s2_visualizer/web/js/app.js` находит тело deleteCurrentZone
    - Docker build sim выходит с кодом 0 (visualizer собирается)
  </acceptance_criteria>
  <done>
    Вкладка Зоны добавлена в Edit Scene. Форма CRUD работает. Docker build sim проходит.
  </done>
</task>

<task type="auto">
  <name>Задача 2: Реализовать VisualHint рендеринг в app.js — glow, arrows, particles, grid</name>
  <read_first>
    - workspace/s2_visualizer/web/js/app.js — updateWorld() зоны секция (строки 1562–1620) (прочитать полностью)
    - workspace/s2_visualizer/web/js/app.js — updateOrCreateMesh, removeMesh функции (найти через grep)
    - workspace/s2_visualizer/web/js/app.js — Three.js imports/setup (строки 1–50 или где THREE используется)
  </read_first>
  <files>
    workspace/s2_visualizer/web/js/app.js
  </files>
  <action>
В updateWorld() в блоке `data.zones.forEach(z => {` ПОСЛЕ рендеринга геометрии зоны (после else { removeMesh(key); }) добавить вызов renderVisualHints:

```javascript
// ПОСЛЕ блока рендеринга геометрии (sphere/aabb/cylinder), внутри forEach:
if (z.visual_hints && z.visual_hints.length > 0) {
    renderVisualHints(key, z, z.visual_hints);
}
```

Добавить функцию renderVisualHints и четыре рендерера. Вставить ПЕРЕД или ПОСЛЕ блока updateWorld (в секцию рендеринга):

```javascript
// ============================================================
// VisualHint рендеринг (ZONE-02, per D-06)
// ============================================================

// Карта: hintKey → объект Three.js (для cleanup при удалении)
const visualHintObjects = {};

function removeVisualHint(hintKey) {
    if (visualHintObjects[hintKey]) {
        scene.remove(visualHintObjects[hintKey]);
        if (visualHintObjects[hintKey].geometry) visualHintObjects[hintKey].geometry.dispose();
        if (visualHintObjects[hintKey].material) visualHintObjects[hintKey].material.dispose();
        delete visualHintObjects[hintKey];
    }
}

function renderVisualHints(zoneKey, zone, hints) {
    if (!hints) return;
    hints.forEach((hint, i) => {
        const hintKey = `${zoneKey}_hint_${i}`;
        // Удалить старый если тип изменился
        if (visualHintObjects[hintKey] &&
            visualHintObjects[hintKey].userData.hintType !== hint.type) {
            removeVisualHint(hintKey);
        }

        const cx = Array.isArray(zone.center) ? zone.center[0] : 0;
        const cy = Array.isArray(zone.center) ? zone.center[1] : 0;
        const cz = Array.isArray(zone.center) ? zone.center[2] : 0;
        // Three.js координаты: x=sim_x, y=sim_z, z=-sim_y
        const tx = cx, ty = cz, tz = -cy;

        if (hint.type === 'glow') {
            renderGlowHint(hintKey, tx, ty, tz, hint.params);
        } else if (hint.type === 'arrows') {
            renderArrowsHint(hintKey, tx, ty, tz, hint.params);
        } else if (hint.type === 'particles') {
            renderParticlesHint(hintKey, tx, ty, tz, hint.params);
        } else if (hint.type === 'grid') {
            renderGridHint(hintKey, tx, ty, tz, hint.params);
        }
    });
}

function renderGlowHint(hintKey, tx, ty, tz, params) {
    // PointLight под зоной (per RESEARCH.md §Pattern 4)
    if (!visualHintObjects[hintKey]) {
        const color = (params && params.color) ? params.color : '#4488FF';
        const intensity = (params && params.intensity !== undefined) ? params.intensity : 0.5;
        const light = new THREE.PointLight(color, intensity, 10);
        light.userData.hintType = 'glow';
        scene.add(light);
        visualHintObjects[hintKey] = light;
    }
    visualHintObjects[hintKey].position.set(tx, ty, tz);
    // Пульс через intensity (анимируется в animate loop если нужно)
}

function renderArrowsHint(hintKey, tx, ty, tz, params) {
    // ArrowHelper — направление по умолчанию вверх (Y), настраивается через params.direction
    if (!visualHintObjects[hintKey]) {
        const dir = new THREE.Vector3(0, 1, 0);
        const color = (params && params.color) ? params.color : '#FF6600';
        const arrow = new THREE.ArrowHelper(dir, new THREE.Vector3(tx, ty, tz), 1.5, color);
        arrow.userData.hintType = 'arrows';
        scene.add(arrow);
        visualHintObjects[hintKey] = arrow;
    }
    visualHintObjects[hintKey].position.set(tx, ty, tz);
}

function renderParticlesHint(hintKey, tx, ty, tz, params) {
    // Points с небольшим BufferGeometry (20 точек в случайных позициях вокруг центра)
    if (!visualHintObjects[hintKey]) {
        const count = 20;
        const positions = new Float32Array(count * 3);
        for (let j = 0; j < count; j++) {
            positions[j * 3]     = (Math.random() - 0.5) * 2;
            positions[j * 3 + 1] = Math.random() * 2;
            positions[j * 3 + 2] = (Math.random() - 0.5) * 2;
        }
        const geo = new THREE.BufferGeometry();
        geo.setAttribute('position', new THREE.BufferAttribute(positions, 3));
        const color = (params && params.color) ? params.color : '#FFFFFF';
        const mat = new THREE.PointsMaterial({ color, size: 0.1 });
        const pts = new THREE.Points(geo, mat);
        pts.userData.hintType = 'particles';
        scene.add(pts);
        visualHintObjects[hintKey] = pts;
    }
    visualHintObjects[hintKey].position.set(tx, ty, tz);
}

function renderGridHint(hintKey, tx, ty, tz, params) {
    // GridHelper внутри зоны
    if (!visualHintObjects[hintKey]) {
        const size = (params && params.size) ? params.size : 4;
        const color = (params && params.color) ? params.color : '#44FF44';
        const grid = new THREE.GridHelper(size, 4, color, color);
        grid.userData.hintType = 'grid';
        scene.add(grid);
        visualHintObjects[hintKey] = grid;
    }
    visualHintObjects[hintKey].position.set(tx, ty, tz);
}
```

В блоке cleanup зон (там где `if (k.startsWith('zone_') && !currentZoneKeys.has(k)) removeMesh(k);`) также удалять связанные hints:
```javascript
Object.keys(meshes).forEach(k => {
    if (k.startsWith('zone_') && !currentZoneKeys.has(k)) {
        removeMesh(k);
        // Удалить visual hints для этой зоны
        Object.keys(visualHintObjects).forEach(hk => {
            if (hk.startsWith(k + '_hint_')) removeVisualHint(hk);
        });
    }
});
```
  </action>
  <verify>
    <automated>docker compose --project-directory /home/dmitriy/study/programming/s2-sim/docker up --build sim 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "renderVisualHints" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "renderGlowHint" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "renderArrowsHint" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "renderParticlesHint" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "renderGridHint" workspace/s2_visualizer/web/js/app.js` находит функцию
    - `grep "PointLight" workspace/s2_visualizer/web/js/app.js` находит в renderGlowHint
    - `grep "ArrowHelper" workspace/s2_visualizer/web/js/app.js` находит в renderArrowsHint
    - `grep "visual_hints" workspace/s2_visualizer/web/js/app.js` находит чтение в updateWorld
    - Docker build sim выходит с кодом 0
  </acceptance_criteria>
  <done>
    VisualHint рендеринг: glow (PointLight), arrows (ArrowHelper), particles (Points), grid (GridHelper) реализованы. Docker build sim проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → REST API | Zone форма отправляет SpawnZone/DespawnZone через POST /command |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering | Zone id_hint из формы | accept | id_hint — std::string, ядро использует как ключ в map; XSS не применим (локальный симулятор, нет auth) |
| T-01-15 | Denial of Service | VisualHint бесконечный рост visualHintObjects | mitigate | cleanup при удалении зоны (removeVisualHint вызывается в зональном cleanup) |
</threat_model>

<verification>
Docker build sim проходит. При открытии браузера видна вкладка "Зоны" в Edit Scene панели.
</verification>

<success_criteria>
- `grep "editor-tab-zones" workspace/s2_visualizer/web/index.html` — находит результат
- `grep "renderVisualHints" workspace/s2_visualizer/web/js/app.js` — находит результат
- `docker compose --project-directory docker up --build sim` выходит с кодом 0
</success_criteria>

<output>
После выполнения создать `.planning/phases/01-zone-visual-control-layer/01-06-SUMMARY.md`

ПРИМЕЧАНИЕ: Для верификации UI (вкладка Зоны видна в браузере, форма работает) — запустить `docker compose --project-directory docker up sim` и открыть http://localhost:1937. Это может потребоваться как checkpoint:human-verify в отдельном прогоне.
</output>
