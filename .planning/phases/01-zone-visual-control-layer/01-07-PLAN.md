---
phase: 01-zone-visual-control-layer
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/s2_visualizer/src/viz_server.hpp
  - workspace/s2_visualizer/src/viz_server.cpp
  - workspace/s2_visualizer/src/main.cpp
  - workspace/s2_visualizer/web/js/app.js
  - workspace/s2_visualizer/web/index.html
  - workspace/s2_core/include/s2/zone_system.hpp
  - workspace/s2_core/src/zone_system.cpp
autonomous: true
gap_closure: true
requirements:
  - ZONE-01
  - ZONE-05
  - ZONE-06

must_haves:
  truths:
    - "POST /command?cmd=spawn_zone с параметрами shape/effects/color/opacity/id_hint создаёт новую зону в SimEngine"
    - "POST /command?cmd=despawn_zone&id=<zone_id> удаляет зону из SimEngine"
    - "Кнопка 'Применить' в форме новой зоны отправляет POST spawn_zone с параметрами из формы"
    - "Кнопка 'Удалить' в форме редактирования отправляет POST despawn_zone с id зоны"
    - "При PER_LINK detection ctx.contact_link заполняется именем линка, через который произошёл контакт"
  artifacts:
    - path: "workspace/s2_visualizer/src/viz_server.cpp"
      provides: "REST routes spawn_zone и despawn_zone в handle_command()"
      contains: "spawn_zone"
    - path: "workspace/s2_visualizer/src/viz_server.hpp"
      provides: "VizCommandHandler::on_spawn_zone() и on_despawn_zone() virtual методы"
      contains: "on_spawn_zone"
    - path: "workspace/s2_visualizer/src/main.cpp"
      provides: "SimEngineCommandAdapter::on_spawn_zone() и on_despawn_zone() через push_command()"
      contains: "on_spawn_zone"
    - path: "workspace/s2_visualizer/web/js/app.js"
      provides: "confirmZoneForm() с реальным fetch POST spawn_zone, deleteCurrentZone() с fetch POST despawn_zone"
      contains: "cmd=spawn_zone"
    - path: "workspace/s2_core/src/zone_system.cpp"
      provides: "agent_in_zone_result() возвращает InZoneResult с contact_link, apply_active_effects() заполняет ctx.contact_link"
      contains: "contact_link"
  key_links:
    - from: "workspace/s2_visualizer/web/js/app.js"
      to: "/command?cmd=spawn_zone"
      via: "fetch POST в confirmZoneForm()"
      pattern: "cmd=spawn_zone"
    - from: "workspace/s2_visualizer/web/js/app.js"
      to: "/command?cmd=despawn_zone"
      via: "fetch POST в deleteCurrentZone()"
      pattern: "cmd=despawn_zone"
    - from: "workspace/s2_visualizer/src/viz_server.cpp"
      to: "handler->on_spawn_zone()"
      via: "handle_command() route cmd==spawn_zone"
      pattern: "on_spawn_zone"
    - from: "workspace/s2_visualizer/src/main.cpp"
      to: "engine_->push_command(cmd::SpawnZone{...})"
      via: "SimEngineCommandAdapter::on_spawn_zone()"
      pattern: "push_command.*SpawnZone"
    - from: "workspace/s2_core/src/zone_system.cpp"
      to: "ctx.contact_link"
      via: "apply_active_effects() использует результат agent_in_zone_result()"
      pattern: "ctx.contact_link"
---

<objective>
Gap closure: реализовать REST endpoints SpawnZone/DespawnZone + заменить UI заглушки на реальные fetch-вызовы + заполнить ctx.contact_link при PER_LINK detection.

Purpose: Закрыть 2 blocker-а из VERIFICATION.md (SC1 partial, SC3 partial) и 1 warning (SC4 contact_link). После этого плана все 6 success criteria Phase 1 будут полностью выполнены.

Output: Рабочий REST API для создания/удаления зон, работающие UI кнопки, заполненный contact_link в эффектах.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-zone-visual-control-layer/01-06-SUMMARY.md

<interfaces>
<!-- Существующий VizCommandHandler интерфейс (viz_server.hpp:20-39): -->

```cpp
struct VizCommandHandler {
    virtual ~VizCommandHandler() = default;
    virtual void on_pause() = 0;
    virtual void on_resume() = 0;
    virtual void on_reset() = 0;
    virtual void on_move_agent(AgentId id, double x, double y, double yaw) = 0;
    virtual void on_plugin_input(AgentId agent_id, const std::string& plugin_type, const std::string& json_input) = 0;
    virtual void on_update_geometry(const std::vector<WorldPrimitive>& prims) = 0;
    virtual bool on_move_zone(const std::string& zone_id, double x, double y) { ... }
    virtual bool on_toggle_zone(const std::string& zone_id, bool enabled) { ... }
    virtual bool on_update_zone_visual(const std::string& zone_id, const std::string& color, double opacity) { ... }
};
```

<!-- KernelCommand SpawnZone/DespawnZone (kernel_command.hpp:120-138): -->

```cpp
namespace cmd {
struct SpawnZone {
  ZoneShape                shape;
  std::vector<std::string> effects;         // ["ice", "fog"]
  std::optional<EntityId>  attached_to;
  std::string              id_hint;
  bool                     visible{true};
  std::string              color{"#FFFFFF"};
  double                   opacity{0.3};
  std::string              label;
};

struct DespawnZone {
  ZoneId id;
};
}
```

<!-- SimEngine::push_command() (sim_engine.hpp:172-176): -->

```cpp
void push_command(KernelCommand cmd) {
    std::lock_guard<std::mutex> lock(command_queue_mutex_);
    command_queue_.push_back(std::move(cmd));
}
```

<!-- SimEngineCommandAdapter в main.cpp (строки 94-155) -- наследует VizCommandHandler, вызывает engine_->push_command() / engine_->zone_system().* -->

<!-- ZoneSystem::agent_in_zone() (zone_system.hpp:112): -->

```cpp
static bool agent_in_zone(const Agent& agent, const Zone& zone);
```

<!-- EffectContext (effect_context.hpp:22-28): -->

```cpp
struct EffectContext {
    // ... другие поля ...
    double zone_strength{1.0};
    std::string contact_link;  // Имя линка при PER_LINK detection (пусто = CENTER/BOUNDING)
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REST routes spawn_zone/despawn_zone + VizCommandHandler + SimEngineCommandAdapter</name>
  <files>
    workspace/s2_visualizer/src/viz_server.hpp,
    workspace/s2_visualizer/src/viz_server.cpp,
    workspace/s2_visualizer/src/main.cpp
  </files>
  <read_first>
    workspace/s2_visualizer/src/viz_server.hpp (полный файл -- интерфейс VizCommandHandler, все виртуальные методы),
    workspace/s2_visualizer/src/viz_server.cpp (строки 375-465 -- handle_command() с существующими routes move_zone/toggle_zone/update_zone_visual как образец),
    workspace/s2_visualizer/src/main.cpp (строки 87-180 -- SimEngineCommandAdapter с on_move_zone/on_toggle_zone/on_update_zone_visual как образец),
    workspace/s2_core/include/s2/kernel_command.hpp (строки 114-149 -- cmd::SpawnZone и cmd::DespawnZone структуры)
  </read_first>
  <action>
    **viz_server.hpp** -- добавить два новых виртуальных метода в VizCommandHandler (после on_update_zone_visual, строка 39):

    ```cpp
    /** Создать новую зону (SpawnZone). */
    virtual bool on_spawn_zone(const std::string& shape_type, double cx, double cy,
                               double radius, double hx, double hy, double hz,
                               double cyl_r, double cyl_h,
                               const std::vector<std::string>& effects,
                               const std::string& color, double opacity,
                               const std::string& id_hint) {
        (void)shape_type; (void)cx; (void)cy; (void)radius;
        (void)hx; (void)hy; (void)hz; (void)cyl_r; (void)cyl_h;
        (void)effects; (void)color; (void)opacity; (void)id_hint;
        return false;
    }

    /** Удалить зону (DespawnZone). */
    virtual bool on_despawn_zone(const std::string& zone_id) {
        (void)zone_id; return false;
    }
    ```

    **viz_server.cpp** -- добавить два route в handle_command() ПЕРЕД закрывающим `if (cmd_handled && g_broadcast_server)` (после блока `update_zone_visual`, строка 461):

    Route `spawn_zone`: парсить параметры: shape (string "sphere"/"box"/"cylinder"), cx, cy (double), radius (double), hx, hy, hz (double -- half_size для box), cyl_r, cyl_h (double -- radius/height для cylinder), effects (comma-separated string -> vector<string> split по ','), color (string, url_decode), opacity (double), id_hint (string, url_decode). Вызвать `handler->on_spawn_zone(...)`. Паттерн парсинга -- по аналогии с toggle_zone/move_zone выше (params map + stod + url_decode).

    Route `despawn_zone`: парсить параметр id (string, url_decode). Вызвать `handler->on_despawn_zone(id)`.

    **main.cpp** -- добавить два override метода в SimEngineCommandAdapter (после on_update_zone_visual, строка 155):

    ```cpp
    bool on_spawn_zone(const std::string& shape_type, double cx, double cy,
                       double radius, double hx, double hy, double hz,
                       double cyl_r, double cyl_h,
                       const std::vector<std::string>& effects,
                       const std::string& color, double opacity,
                       const std::string& id_hint) override {
        if (!engine_) return false;
        s2::cmd::SpawnZone spawn;
        spawn.effects = effects;
        spawn.color = color;
        spawn.opacity = opacity;
        spawn.id_hint = id_hint;
        spawn.visible = true;

        // Заполнить shape по типу
        if (shape_type == "sphere") {
            spawn.shape.type = s2::ZoneShapeType::SPHERE;
            spawn.shape.center = {cx, cy, 0.0};
            spawn.shape.radius = radius;
        } else if (shape_type == "box") {
            spawn.shape.type = s2::ZoneShapeType::AABB;
            spawn.shape.center = {cx, cy, 0.0};
            spawn.shape.half_size = {hx, hy, hz};
        } else if (shape_type == "cylinder") {
            spawn.shape.type = s2::ZoneShapeType::CYLINDER;
            spawn.shape.center = {cx, cy, 0.0};
            spawn.shape.radius = cyl_r;
            spawn.shape.half_height = cyl_h / 2.0;
        } else {
            // Fallback на sphere
            spawn.shape.type = s2::ZoneShapeType::SPHERE;
            spawn.shape.center = {cx, cy, 0.0};
            spawn.shape.radius = radius;
        }

        engine_->push_command(s2::KernelCommand{spawn});
        broadcast_snapshot();
        return true;
    }

    bool on_despawn_zone(const std::string& zone_id) override {
        if (!engine_) return false;
        s2::cmd::DespawnZone despawn;
        despawn.id = zone_id;
        engine_->push_command(s2::KernelCommand{despawn});
        broadcast_snapshot();
        return true;
    }
    ```

    Не забыть: broadcast_snapshot() после push_command чтобы UI получил обновление. Паттерн аналогичен on_move_agent() (строка 123-132 main.cpp).
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "on_spawn_zone" workspace/s2_visualizer/src/viz_server.hpp возвращает определение virtual метода
    - grep "on_despawn_zone" workspace/s2_visualizer/src/viz_server.hpp возвращает определение virtual метода
    - grep "spawn_zone" workspace/s2_visualizer/src/viz_server.cpp возвращает route в handle_command()
    - grep "despawn_zone" workspace/s2_visualizer/src/viz_server.cpp возвращает route в handle_command()
    - grep "on_spawn_zone" workspace/s2_visualizer/src/main.cpp возвращает override метод в SimEngineCommandAdapter
    - grep "on_despawn_zone" workspace/s2_visualizer/src/main.cpp возвращает override метод в SimEngineCommandAdapter
    - grep "push_command.*SpawnZone\|SpawnZone.*push_command\|KernelCommand.*spawn" workspace/s2_visualizer/src/main.cpp возвращает строку с push_command
    - Docker build + tests проходят без ошибок
  </acceptance_criteria>
  <done>REST endpoints /command?cmd=spawn_zone и /command?cmd=despawn_zone работают, пробрасывают KernelCommand в SimEngine через command_queue_</done>
</task>

<task type="auto">
  <name>Task 2: UI confirmZoneForm() и deleteCurrentZone() -- замена заглушек на реальные fetch POST</name>
  <files>
    workspace/s2_visualizer/web/js/app.js,
    workspace/s2_visualizer/web/index.html
  </files>
  <read_first>
    workspace/s2_visualizer/web/js/app.js (строки 1747-1774 -- confirmZoneForm() и deleteCurrentZone() с заглушками),
    workspace/s2_visualizer/web/js/app.js (строки 1756-1761 -- существующий fetch для move_zone/update_zone_visual как образец),
    workspace/s2_visualizer/web/index.html (строки 742-796 -- форма зоны с id всех полей: zf-shape, zf-radius, zf-bx/by/bz, zf-cr/ch, zf-x, zf-y, zf-color, zf-opacity, zf-effects, zf-id)
  </read_first>
  <action>
    **app.js confirmZoneForm()** -- заменить строку 1763 (console.log заглушка) на реальный fetch POST:

    ```javascript
    } else {
        // Создание новой зоны -- собираем все параметры формы
        const shape = document.getElementById('zf-shape').value;
        const effects = Array.from(document.getElementById('zf-effects').selectedOptions).map(o => o.value).join(',');
        const idHint = idField || '';

        let params = `cmd=spawn_zone&shape=${shape}&cx=${x}&cy=${y}&color=${color}&opacity=${opacity}&id_hint=${encodeURIComponent(idHint)}&effects=${encodeURIComponent(effects)}`;

        if (shape === 'sphere') {
            const r = parseFloat(document.getElementById('zf-radius').value) || 2.0;
            params += `&radius=${r}`;
        } else if (shape === 'box') {
            const hx = parseFloat(document.getElementById('zf-bx').value) || 1.0;
            const hy = parseFloat(document.getElementById('zf-by').value) || 1.0;
            const hz = parseFloat(document.getElementById('zf-bz').value) || 1.0;
            params += `&hx=${hx / 2}&hy=${hy / 2}&hz=${hz / 2}`;
        } else if (shape === 'cylinder') {
            const cr = parseFloat(document.getElementById('zf-cr').value) || 2.0;
            const ch = parseFloat(document.getElementById('zf-ch').value) || 1.0;
            params += `&cyl_r=${cr}&cyl_h=${ch}`;
        }

        fetch(`http://${host}:${port}/command?${params}`, { method: 'POST' })
            .catch(e => console.error('[SpawnZone]', e));
    }
    ```

    Обратить внимание: box half_size -- форма ввода W/D/H (полные размеры), а SpawnZone ожидает half_size. Поэтому делим на 2.

    **app.js deleteCurrentZone()** -- заменить строку 1771 (console.log заглушка) на реальный fetch POST:

    ```javascript
    window.deleteCurrentZone = function() {
        if (!editingZoneId) return;
        const host = window.location.hostname || 'localhost';
        const port = window.location.port || '1937';
        fetch(`http://${host}:${port}/command?cmd=despawn_zone&id=${encodeURIComponent(editingZoneId)}`, { method: 'POST' })
            .catch(e => console.error('[DespawnZone]', e));
        cancelZoneForm();
    };
    ```

    Также:
    - Удалить текст hint из index.html (строка 747): заменить "Создание/удаление зон в рантайме -- Wave 2." на "Создание/удаление зон через SpawnZone/DespawnZone." (чтобы не вводить в заблуждение).
    - В app.js строка 1678: заменить 'Новая зона (добавление -- Wave 2)' на 'Новая зона' (убрать устаревший hint Wave 2).
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && grep -n "cmd=spawn_zone" workspace/s2_visualizer/web/js/app.js && grep -n "cmd=despawn_zone" workspace/s2_visualizer/web/js/app.js && ! grep "console.log.*Wave 2" workspace/s2_visualizer/web/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "cmd=spawn_zone" workspace/s2_visualizer/web/js/app.js возвращает строку с fetch POST запросом
    - grep "cmd=despawn_zone" workspace/s2_visualizer/web/js/app.js возвращает строку с fetch POST запросом
    - grep "console.log.*Wave 2" workspace/s2_visualizer/web/js/app.js НЕ возвращает ничего (console.log заглушки удалены)
    - grep "console.log.*SpawnZone\|console.log.*DespawnZone" workspace/s2_visualizer/web/js/app.js НЕ возвращает ничего
    - confirmZoneForm() содержит fetch с параметрами shape, cx, cy, radius/hx/hy/hz/cyl_r/cyl_h, effects, color, opacity, id_hint
    - deleteCurrentZone() содержит fetch с параметром id=editingZoneId
  </acceptance_criteria>
  <done>Кнопка "Применить" для новой зоны отправляет POST /command?cmd=spawn_zone, кнопка "Удалить" отправляет POST /command?cmd=despawn_zone</done>
</task>

<task type="auto">
  <name>Task 3: Заполнить ctx.contact_link при PER_LINK detection</name>
  <files>
    workspace/s2_core/include/s2/zone_system.hpp,
    workspace/s2_core/src/zone_system.cpp
  </files>
  <read_first>
    workspace/s2_core/include/s2/zone_system.hpp (строки 107-116 -- agent_in_zone() сигнатура и приватные методы),
    workspace/s2_core/src/zone_system.cpp (строки 61-112 -- agent_in_zone() реализация, особенно PER_LINK ветка),
    workspace/s2_core/src/zone_system.cpp (строки 148-237 -- tick() с вызовами agent_in_zone()),
    workspace/s2_core/src/zone_system.cpp (строки 452-488 -- apply_active_effects() где ctx.contact_link должен заполняться),
    workspace/s2_core/src/zone_system.cpp (строки 300-320 -- toggle_zone_with_events() с вызовом agent_in_zone()),
    workspace/s2_core/include/s2/effect_context.hpp (полный файл -- EffectContext с полем contact_link)
  </read_first>
  <action>
    **zone_system.hpp** -- добавить структуру результата и новый приватный метод:

    ```cpp
    /// Результат проверки вхождения агента в зону.
    struct InZoneResult {
        bool inside{false};
        std::string contact_link; ///< Имя первого линка внутри зоны (PER_LINK), пусто для CENTER/BOUNDING
    };
    ```

    Добавить объявление рядом с agent_in_zone (строка 112):
    ```cpp
    /// Расширенная проверка: возвращает InZoneResult с contact_link для PER_LINK.
    static InZoneResult agent_in_zone_result(const Agent& agent, const Zone& zone);
    ```

    Оставить `static bool agent_in_zone(const Agent&, const Zone&)` как есть (backward compat) -- он будет делегировать:
    ```cpp
    static bool agent_in_zone(const Agent& agent, const Zone& zone) {
        return agent_in_zone_result(agent, zone).inside;
    }
    ```

    **zone_system.cpp** -- рефакторинг agent_in_zone():

    1. Переименовать текущую `agent_in_zone()` в `agent_in_zone_result()`, изменив возвращаемый тип на `InZoneResult`.
    2. В ветке PER_LINK: вместо `return true` при нахождении линка в зоне -- вернуть `InZoneResult{true, link.name}`.
    3. В ветках CENTER и BOUNDING: вернуть `InZoneResult{inside_result, ""}`.
    4. Сделать `agent_in_zone()` inline делегатом (или определить в .cpp): `return agent_in_zone_result(agent, zone).inside;`

    **zone_system.cpp apply_active_effects()** (строки 452-488):
    - Перед циклом `for (auto& desc : zone.effects)` вызвать `auto result = agent_in_zone_result(agent, zone);`
    - В строке ~467 (после заполнения ctx.agent_position): добавить `ctx.contact_link = result.contact_link;`

    **zone_system.cpp tick()** (строка 175):
    - Вызов `agent_in_zone(agent, zone)` на строке 175 оставить без изменений -- tick() нужен только bool, contact_link не требуется на этом этапе.

    **zone_system.cpp on_agent_enter()** (строки 394-427):
    - Для ON_ENTER эффектов (mutation при входе) тоже нужен contact_link. Добавить `auto result = agent_in_zone_result(agent, zone);` и заполнить `ctx.contact_link = result.contact_link;` в EffectContext (строка ~414-419).

    **zone_system.cpp on_agent_exit()** (строки 430-450):
    - При exit contact_link уже не релевантен (агент покинул зону), оставить пустым.
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "InZoneResult" workspace/s2_core/include/s2/zone_system.hpp возвращает определение структуры
    - grep "agent_in_zone_result" workspace/s2_core/include/s2/zone_system.hpp возвращает объявление метода
    - grep "agent_in_zone_result" workspace/s2_core/src/zone_system.cpp возвращает реализацию метода
    - grep "ctx.contact_link" workspace/s2_core/src/zone_system.cpp возвращает присваивание в apply_active_effects()
    - grep "result.contact_link" workspace/s2_core/src/zone_system.cpp возвращает использование в apply_active_effects()
    - Все тесты проходят (test_zone_detection_mode, test_zone_lifecycle, test_zone_self_destruct, test_zone_owned, test_zone_commands, test_effect_fog_emi)
  </acceptance_criteria>
  <done>При PER_LINK detection поле EffectContext::contact_link заполняется именем линка, через который агент находится в зоне. Все существующие тесты проходят.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> viz_server HTTP | Пользовательский ввод из UI формы передаётся как query-параметры в viz_server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | T (Tampering) | viz_server.cpp spawn_zone params | accept | Локальный симулятор, не публичный сервис. Параметры парсятся через stod с try/catch (паттерн из существующих routes). Некорректные значения приведут к дефолтам, не к крашу. |
| T-01-07-02 | D (DoS) | viz_server.cpp spawn_zone | accept | Локальный инструмент. Массовый спавн зон ограничен SimEngine tick performance, не security concern. |
</threat_model>

<verification>
1. Docker build + все тесты проходят: `docker compose --project-directory docker up --build tests`
2. grep "spawn_zone" viz_server.cpp -- route существует
3. grep "despawn_zone" viz_server.cpp -- route существует
4. grep "cmd=spawn_zone" app.js -- UI отправляет POST
5. grep "cmd=despawn_zone" app.js -- UI отправляет POST
6. grep "InZoneResult" zone_system.hpp -- структура определена
7. grep "ctx.contact_link" zone_system.cpp -- заполняется в apply_active_effects
8. Нет console.log заглушек "Wave 2" в app.js
</verification>

<success_criteria>
- SC1 (VERIFICATION.md): confirmZoneForm() для новых зон отправляет POST /command?cmd=spawn_zone, deleteCurrentZone() отправляет POST /command?cmd=despawn_zone
- SC3 (VERIFICATION.md): viz_server.cpp handle_command() имеет routes для spawn_zone и despawn_zone, пробрасывающие команды через push_command() в SimEngine
- SC4 warning: ctx.contact_link заполняется именем линка при PER_LINK detection в apply_active_effects()
- Все существующие тесты проходят без регрессий
</success_criteria>

<output>
After completion, create `.planning/phases/01-zone-visual-control-layer/01-07-SUMMARY.md`
</output>
