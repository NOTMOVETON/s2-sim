---
phase: 02-actor-prop-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - workspace/s2_core/src/sim_engine.cpp
  - workspace/s2_core/src/scene_loader.cpp
  - workspace/s2_core/include/s2/world_snapshot.hpp
autonomous: true
requirements:
  - ACTR-01
  - PROP-01
  - PROP-02

must_haves:
  truths:
    - "Phase 2 тика вызывает behavior.update() для каждого актора с плагинами"
    - "Phase 6 тика обновляет world_pose attached пропов из позы parent entity + link"
    - "cmd::Interact маршрутизируется к actor.behavior.on_interact()"
    - "cmd::AttachObject записывает attached_to_agent/link/offset в Prop"
    - "cmd::DetachObject сбрасывает attached_to_agent в Prop + применяет drop_pose"
    - "SceneLoader парсит акторов с behavior_type + поле actors в YAML"
    - "SceneLoader парсит пропы с signals/capabilities/tags в YAML"
    - "WorldSnapshot содержит актуальные позы attached пропов"
  artifacts:
    - path: "workspace/s2_core/src/sim_engine.cpp"
      provides: "Phase 2 (actors), Phase 6 (attachments), обработчики Interact/AttachObject/DetachObject"
      contains: "phase2_actors"
    - path: "workspace/s2_core/src/scene_loader.cpp"
      provides: "Парсинг actors/props секций YAML"
      contains: "actors"
  key_links:
    - from: "sim_engine.cpp phase2_actors"
      to: "actor.behavior->update()"
      via: "WorldContext с bus_, null_world_query_, command_queue_"
      pattern: "behavior->update"
    - from: "sim_engine.cpp phase6_attachments"
      to: "prop.attached_to_agent"
      via: "world_.agents() + world_.props()"
      pattern: "attached_to_agent"
    - from: "sim_engine.cpp apply_kernel_command(cmd::Interact)"
      to: "actor.behavior->on_interact()"
      via: "world_.get_actor()"
      pattern: "on_interact"
---

<objective>
Подключить акторов и пропы к тиковому циклу SimEngine: Phase 2 (actors tick), Phase 6 (attachment update), обработчики KernelCommands Interact/AttachObject/DetachObject, SceneLoader парсинг.

Purpose: Без этого плана IActorBehavior и Prop — мёртвые типы: ничто их не вызывает и не загружает.

Output: Акторы тикают в Phase 2, пропы обновляют позиции в Phase 6, команды Interact/Attach/Detach работают, SceneLoader читает actors/props из YAML.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/PROJECT.md
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/02-actor-prop-foundation/02-CONTEXT.md

<interfaces>
<!-- Типы из Plan 02-01 — использовать напрямую -->

Из workspace/s2_core/include/s2/actor.hpp (расширенный, Plan 02-01):
```cpp
struct Actor {
  ActorId id{0};
  std::string name, type;
  Pose3D world_pose;
  ActorState current_state;
  CollisionShape collision;
  VisualDesc visual;
  bool collision_enabled{true};
  std::unique_ptr<IActorBehavior> behavior;
  std::vector<std::unique_ptr<plugins::IAgentPlugin>> plugins;
  SharedState state;
};
```

Из workspace/s2_core/include/s2/prop.hpp (расширенный, Plan 02-01):
```cpp
struct Prop {
  ObjectId id{0};
  std::string type;
  Pose3D world_pose;
  bool movable{false};
  bool has_collision{true};
  CollisionShape collision;
  VisualDesc visual;
  std::vector<Signal> signals;
  std::set<std::string> capabilities;
  std::map<std::string, std::string> tags;
  std::optional<EntityId> attached_to_agent;
  std::string attach_link;
  Pose3D attach_offset;
  std::unordered_map<std::string, std::string> properties;
};
```

Из workspace/s2_core/include/s2/actor_behavior.hpp (Plan 02-01):
```cpp
struct WorldContext {
  const WorldQuery& world;
  EventBus&         bus;
  KernelCommandQueue& commands;
  double            sim_time;
};
class IActorBehavior {
  virtual void update(double dt, Actor& actor, const WorldContext& ctx) = 0;
  virtual void on_interact(EntityId source, const std::string& action,
                           const nlohmann::json& params) = 0;
};
```

Из workspace/s2_core/include/s2/kernel_command.hpp:
```cpp
struct Interact  { EntityId source_id, target_id; std::string action;
                   nlohmann::json params; double max_distance; };
struct AttachObject { EntityId parent_id; std::string link; EntityId child_id;
                      Pose3D local_pose; };
struct DetachObject { EntityId child_id; std::optional<Pose3D> drop_pose; };
```

Существующий паттерн apply_kernel_command в sim_engine.hpp — шаблонная функция с if constexpr цепочкой. Новые обработчики добавлять в else-if блоки ДО финального else-блока.

Паттерн get_agent/get_actor/get_prop в world.hpp:
```cpp
Actor* world_.get_actor(ActorId id);    // nullptr если не найден
Agent* world_.get_agent(AgentId id);    // nullptr если не найден
Prop*  world_.get_prop(ObjectId id);    // nullptr если не найден
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 (actors tick) + Phase 6 (attachments) в SimEngine</name>
  <files>
    workspace/s2_core/src/sim_engine.cpp
    workspace/s2_core/include/s2/sim_engine.hpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/sim_engine.hpp (полный файл — phase2_actors stub, phase6_attachments, apply_kernel_command template)
    - workspace/s2_core/include/s2/actor_behavior.hpp (WorldContext struct)
    - workspace/s2_core/include/s2/actor.hpp (расширенный Actor struct)
    - workspace/s2_core/include/s2/prop.hpp (расширенный Prop struct)
    - .planning/phases/02-actor-prop-foundation/02-CONTEXT.md (D-06, D-07, D-19, D-20, D-21)
  </read_first>
  <action>
В sim_engine.hpp/cpp реализовать три части:

**1. phase2_actors() — тиковый цикл акторов (per D-06)**

Реализовать в теле SimEngine (заменить TODO-заглушку):
```cpp
void phase2_actors() {
  for (auto& actor : world_.actors()) {
    if (!actor.behavior) continue;

    // Плагины актора: pre_resolve (controller-плагины публикуют contributions)
    for (auto& plugin : actor.plugins)
      plugin->pre_resolve(dt_, /* нужен Agent& — см. ниже */);

    // Resolver для SharedState актора
    actor.state.resolve();

    // Behavior update (D-06: behavior читает effective из state и действует)
    {
      KernelCommandQueue tick_cmds;
      WorldContext ctx{null_world_query_, bus_, tick_cmds, sim_time_};
      actor.behavior->update(dt_, actor, ctx);

      for (auto& cmd : tick_cmds)
        std::visit([this](const auto& c) { apply_kernel_command(c); }, cmd);
    }

    // Плагины актора: update (controller-плагины после behavior)
    {
      KernelCommandQueue tick_cmds;
      PluginContext ctx{null_world_query_, bus_, tick_cmds};
      for (auto& plugin : actor.plugins) {
        plugin->update(dt_, /* Actor как Agent? — см. ниже */, ctx);
      }
      for (auto& cmd : tick_cmds)
        std::visit([this](const auto& c) { apply_kernel_command(c); }, cmd);
    }
  }
}
```

ПРОБЛЕМА: IAgentPlugin::update() принимает `Agent&`, но у нас `Actor`. В Phase 2 controller-плагины (DoorWireController и т.п.) — это IAgentPlugin плагины, работающие на акторе. Нужно решить.

РЕШЕНИЕ (по дизайну из CONTEXT.md): actor.plugins — это тот же `vector<unique_ptr<IAgentPlugin>>` что у агентов. Для Phase 2 плагины актора используют специальный adapter. Самый простой вариант для Phase 2 — создать временный `Agent` с минимальными полями:

Альтернативный подход (проще и правильнее): в Phase 2 плагины актора вызывать с ДРУГОЙ сигнатурой. Для controller-плагинов (DoorWireController, ConveyorWireController, EventReactor) в их реализации передаём данные через PluginContext, а не Agent. Они читают SharedState из ctx, не из agent напрямую.

Финальное решение для Phase 2: вызывать `plugin->update(dt_, /* fake agent */ ...)`. Создать minimальный Agent-заглушку в phase2_actors() только для передачи в plugin:
```cpp
// Для плагинов актора создаём минимальный агент-прокси чтобы использовать IAgentPlugin API
// В Phase 6 (Entity Model) это будет заменено на унифицированный EntityBase
Agent actor_agent_proxy;
actor_agent_proxy.id = actor.id;
actor_agent_proxy.world_pose = actor.world_pose;
// plugins вызываются с этим proxy — они читают world через ctx, не agent
```

Примечание: не добавляй комментарий "технический долг" или подобное — это намеренный паттерн Phase 2.

**2. Phase 6 (attachments) — обновление позиций attached пропов (per D-20)**

Добавить в существующий `phase6_attachments()` после `zone_system_.update_owned_zones_positions()`:
```cpp
// Обновить позиции пропов, привязанных к агентам
for (auto& prop : world_.props()) {
  if (!prop.attached_to_agent.has_value()) continue;

  EntityId parent_id = prop.attached_to_agent.value();

  // Поиск среди агентов
  for (const auto& agent : world_.agents()) {
    if (agent.id != parent_id) continue;

    // Если attach_link пуст — привязываем к base_link (world_pose агента)
    Pose3D link_world_pose = agent.world_pose;
    if (!prop.attach_link.empty() && agent.kinematic_tree) {
      link_world_pose = agent.kinematic_tree->compute_world_pose(
          prop.attach_link, agent.world_pose);
    }

    // world_pose пропа = link_pose + offset
    // Упрощённо: сложение позиций + yaw (Phase 6 ENTY унифицирует через Transform3D)
    prop.world_pose.x   = link_world_pose.x   + prop.attach_offset.x;
    prop.world_pose.y   = link_world_pose.y   + prop.attach_offset.y;
    prop.world_pose.z   = link_world_pose.z   + prop.attach_offset.z;
    prop.world_pose.yaw = link_world_pose.yaw + prop.attach_offset.yaw;
    break;
  }

  // Поиск среди акторов (для attach prop к актору)
  for (const auto& actor : world_.actors()) {
    if (actor.id != parent_id) continue;
    prop.world_pose.x   = actor.world_pose.x   + prop.attach_offset.x;
    prop.world_pose.y   = actor.world_pose.y   + prop.attach_offset.y;
    prop.world_pose.z   = actor.world_pose.z   + prop.attach_offset.z;
    prop.world_pose.yaw = actor.world_pose.yaw + prop.attach_offset.yaw;
    break;
  }
}
```

**3. Обработчики KernelCommand в apply_kernel_command() (per D-19)**

Заменить финальный `else { (void)cmd; }` на конкретные обработчики:

```cpp
else if constexpr (std::is_same_v<T, cmd::Interact>)
{
  // Проверить дистанцию если max_distance > 0
  // ... (опционально, найти source.world_pose и target.world_pose)

  // Маршрутизация к актору
  auto* actor = world_.get_actor(cmd.target_id);
  if (actor && actor->behavior) {
    actor->behavior->on_interact(cmd.source_id, cmd.action, cmd.params);
  }
  // Если target — агент с behavior (Phase 3): не реализуем в Phase 2
}
else if constexpr (std::is_same_v<T, cmd::AttachObject>)
{
  // Найти prop (child_id — это ObjectId/EntityId пропа)
  auto* prop = world_.get_prop(cmd.child_id);
  if (prop) {
    prop->attached_to_agent = cmd.parent_id;
    prop->attach_link       = cmd.link;
    prop->attach_offset     = cmd.local_pose;
    bus_.publish(event::ObjectAttached{
        .obj   = cmd.child_id,
        .agent = cmd.parent_id,
        .link  = cmd.link
    });
  }
}
else if constexpr (std::is_same_v<T, cmd::DetachObject>)
{
  auto* prop = world_.get_prop(cmd.child_id);
  if (prop) {
    if (cmd.drop_pose.has_value()) {
      prop->world_pose = cmd.drop_pose.value();
    }
    EntityId parent = prop->attached_to_agent.value_or(0);
    prop->attached_to_agent = std::nullopt;
    prop->attach_link.clear();
    prop->attach_offset = Pose3D{};
    bus_.publish(event::ObjectReleased{
        .obj   = cmd.child_id,
        .agent = parent
    });
  }
}
```

ВАЖНО: `get_prop()` в world.hpp принимает `ObjectId`. ObjectId — это uint32_t, EntityId — тоже uint32_t. Если нужно явное приведение типа — добавить static_cast<ObjectId>(cmd.child_id). Не изменять типы в world.hpp.

В Phase 8 cleanup добавить очистку SharedState акторов (аналогично агентам):
```cpp
for (auto& actor : world_.actors())
  actor.state.clear_contributions();
```
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "phase2_actors" без "TODO Phase 2"
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "behavior->update"
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "attached_to_agent"
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "cmd::Interact" с "on_interact"
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "cmd::AttachObject" с "ObjectAttached"
    - workspace/s2_core/include/s2/sim_engine.hpp содержит "cmd::DetachObject" с "ObjectReleased"
    - Docker build tests завершается без ошибок компиляции
  </acceptance_criteria>
  <done>Phase 2 и Phase 6 тика реализованы. Interact/AttachObject/DetachObject обрабатываются ядром.</done>
</task>

<task type="auto">
  <name>Task 2: SceneLoader парсинг actors и props + тест</name>
  <files>
    workspace/s2_core/src/scene_loader.cpp
    workspace/s2_core/include/s2/scene_loader.hpp
    workspace/s2_core/tests/test_actor_prop_loading.cpp
  </files>
  <read_first>
    - workspace/s2_core/src/scene_loader.cpp (полный файл — паттерн парсинга агентов)
    - workspace/s2_core/include/s2/scene_loader.hpp (SceneLoader интерфейс)
    - workspace/s2_core/include/s2/actor.hpp (расширенный Actor)
    - workspace/s2_core/include/s2/prop.hpp (расширенный Prop)
    - workspace/s2_core/CMakeLists.txt (добавить тест в список)
    - .planning/phases/02-actor-prop-foundation/02-CONTEXT.md (D-04, D-16, D-17)
  </read_first>
  <action>
Добавить парсинг секций `actors:` и `props:` в SceneLoader. Изучить существующий паттерн парсинга агентов и повторить его для акторов и пропов.

**scene_loader.cpp / scene_loader.hpp:**

В функции загрузки сцены (найти функцию parse/load в scene_loader.cpp) добавить после парсинга agents:

```cpp
// Парсинг акторов
if (yaml["actors"]) {
  for (const auto& actor_node : yaml["actors"]) {
    Actor actor;
    actor.id   = next_actor_id_++;  // или из node["id"] если задан
    actor.name = actor_node["name"].as<std::string>("");
    actor.type = actor_node["type"].as<std::string>("");

    if (actor_node["pose"]) {
      const auto& p = actor_node["pose"];
      actor.world_pose.x   = p["x"].as<double>(0.0);
      actor.world_pose.y   = p["y"].as<double>(0.0);
      actor.world_pose.z   = p["z"].as<double>(0.0);
      actor.world_pose.yaw = p["yaw"].as<double>(0.0);
    }

    // Visual описание
    if (actor_node["visual"]) {
      // Аналогично парсингу visual у агентов — скопировать паттерн
    }

    // Collision shape
    if (actor_node["collision"]) {
      // Аналогично парсингу collision у агентов
    }

    // behavior_type задаёт тип поведения — создаётся через behavior_factory
    // В Phase 2 behavior_factory регистрируется снаружи через set_behavior_factory()
    std::string behavior_type = actor_node["behavior"].as<std::string>("");
    if (!behavior_type.empty() && behavior_factory_) {
      actor.behavior = behavior_factory_(behavior_type, actor_node);
    }

    // Плагины актора (controller-плагины)
    if (actor_node["plugins"]) {
      for (const auto& plugin_node : actor_node["plugins"]) {
        std::string ptype = plugin_node["type"].as<std::string>("");
        auto plugin = create_plugin(ptype, plugin_node);  // из plugins_registry
        if (plugin) actor.plugins.push_back(std::move(plugin));
      }
    }

    world.add_actor(std::move(actor));
  }
}

// Парсинг пропов
if (yaml["props"]) {
  for (const auto& prop_node : yaml["props"]) {
    Prop prop;
    prop.id     = next_prop_id_++;
    prop.type   = prop_node["type"].as<std::string>("");
    prop.movable = prop_node["movable"].as<bool>(false);
    prop.has_collision = prop_node["has_collision"].as<bool>(true);

    if (prop_node["pose"]) {
      const auto& p = prop_node["pose"];
      prop.world_pose.x   = p["x"].as<double>(0.0);
      prop.world_pose.y   = p["y"].as<double>(0.0);
      prop.world_pose.z   = p["z"].as<double>(0.0);
      prop.world_pose.yaw = p["yaw"].as<double>(0.0);
    }

    // Visual и Collision — аналогично агентам
    if (prop_node["visual"]) { /* ... */ }
    if (prop_node["collision"]) { /* ... */ }

    // Capabilities
    if (prop_node["capabilities"]) {
      for (const auto& cap : prop_node["capabilities"])
        prop.capabilities.insert(cap.as<std::string>());
    }

    // Tags
    if (prop_node["tags"]) {
      for (const auto& tag : prop_node["tags"])
        prop.tags[tag.first.as<std::string>()] = tag.second.as<std::string>();
    }

    // Signals — аналогично парсингу signals у агентов (ARCH-03)
    if (prop_node["signals"]) {
      for (const auto& sig_node : prop_node["signals"]) {
        Signal sig;
        sig.signal_id   = sig_node["signal_id"].as<std::string>("");
        sig.type        = sig_node["type"].as<std::string>("wire");
        sig.range       = sig_node["range"].as<double>(0.0);
        sig.requires_los = sig_node["requires_los"].as<bool>(false);
        sig.enabled     = sig_node["enabled"].as<bool>(true);
        prop.signals.push_back(std::move(sig));
      }
    }

    world.add_prop(std::move(prop));
  }
}
```

В SceneLoader добавить метод `set_behavior_factory()`:
```cpp
using BehaviorFactory = std::function<
    std::unique_ptr<IActorBehavior>(const std::string& type, const YAML::Node& config)>;
void set_behavior_factory(BehaviorFactory factory) {
    behavior_factory_ = std::move(factory);
}
```
И поле `BehaviorFactory behavior_factory_;`

**test_actor_prop_loading.cpp** — тест загрузки:

```cpp
TEST(ActorPropLoading, LoadsActorFromYaml) {
    const std::string yaml = R"(
actors:
  - name: door_01
    type: door
    pose: {x: 1.0, y: 2.0, z: 0.0, yaw: 0.0}
props:
  - type: crate
    movable: true
    has_collision: true
    pose: {x: 3.0, y: 0.0, z: 0.0}
    capabilities: [fragile]
)";
    SceneLoader loader;
    auto scene = loader.load_from_string(yaml);  // или аналогичный API
    ASSERT_EQ(scene.actors.size(), 1u);
    EXPECT_EQ(scene.actors[0].name, "door_01");
    EXPECT_EQ(scene.actors[0].type, "door");
    ASSERT_EQ(scene.props.size(), 1u);
    EXPECT_TRUE(scene.props[0].movable);
    EXPECT_TRUE(scene.props[0].capabilities.count("fragile"));
}
```

Перед написанием теста: прочитать существующие тесты SceneLoader (test_scene_*.cpp) для понимания API.

Добавить `tests/test_actor_prop_loading.cpp` в CMakeLists.txt в список s2_core_tests (или s2_editor_tests — выбрать по аналогии с другими scene тестами).
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/src/scene_loader.cpp содержит "actors" YAML секцию парсинга
    - workspace/s2_core/src/scene_loader.cpp содержит "props" YAML секцию парсинга
    - workspace/s2_core/src/scene_loader.cpp содержит "behavior_factory_"
    - workspace/s2_core/src/scene_loader.cpp содержит "capabilities"
    - workspace/s2_core/tests/test_actor_prop_loading.cpp существует
    - Docker build tests завершается без ошибок; тест LoadsActorFromYaml PASSED
  </acceptance_criteria>
  <done>SceneLoader загружает акторов с behavior_type и пропы с capabilities/signals. Тест проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML scene file → SceneLoader | Внешний YAML файл сцены с actor/prop конфигурацией |
| HTTP/REST → cmd::Interact | REST запросы могут отправлять Interact команды с произвольным action |
| plugin → KernelCommandQueue (actor plugins) | Плагины актора кладут команды в локальную очередь |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | cmd::AttachObject.child_id | mitigate | `world_.get_prop(cmd.child_id)` возвращает nullptr для несуществующего id — команда игнорируется; логировать "AttachObject: prop not found" |
| T-02-05 | Spoofing | cmd::Interact.source_id | accept | В Phase 2 нет аутентификации entity; target behavior получает source_id и сам решает доверять ли |
| T-02-06 | Tampering | YAML actors[behavior] поле | mitigate | behavior_factory проверяет тип через реестр — неизвестный тип → nullptr, не краш |
| T-02-07 | Denial of Service | Бесконечный цикл в actor.behavior->update() | accept | В Phase 2 нет таймаута на тик; добавится в SCAL-01 |
</threat_model>

<verification>
После выполнения плана:

```bash
docker compose --project-directory docker up --build tests 2>&1 | tail -30
```

Проверочные grep:
```bash
grep -n "phase2_actors" workspace/s2_core/include/s2/sim_engine.hpp
grep -n "behavior->update" workspace/s2_core/include/s2/sim_engine.hpp
grep -n "AttachObject" workspace/s2_core/include/s2/sim_engine.hpp
grep -n "behavior_factory" workspace/s2_core/src/scene_loader.cpp
```
</verification>

<success_criteria>
- phase2_actors() вызывает behavior.update() для всех акторов с behavior
- phase6_attachments() обновляет world_pose attached пропов
- apply_kernel_command(cmd::Interact) маршрутизирует к actor.behavior.on_interact()
- apply_kernel_command(cmd::AttachObject) записывает attached_to_agent в Prop
- apply_kernel_command(cmd::DetachObject) сбрасывает attached_to_agent
- SceneLoader парсит actors секцию YAML (name, type, pose, behavior, plugins)
- SceneLoader парсит props секцию YAML (type, movable, capabilities, signals)
- Тест test_actor_prop_loading проходит
</success_criteria>

<output>
После завершения создать `.planning/phases/02-actor-prop-foundation/02-02-SUMMARY.md`
</output>
