---
phase: 02-actor-prop-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - workspace/s2_plugins/include/s2/plugins/grabber_plugin.hpp
  - workspace/s2_plugins/src/plugins/grabber_plugin.cpp
  - workspace/s2_plugins/src/plugins_registry.cpp
  - workspace/s2_core/tests/test_door_behavior.cpp
  - workspace/s2_core/tests/test_grab_attach.cpp
  - workspace/s2_core/CMakeLists.txt
autonomous: true
requirements:
  - PROP-03
  - ACTR-01
  - ACTR-02
  - ACTR-06
  - PROP-01
  - PROP-02

must_haves:
  truths:
    - "GrabberPlugin: grab action → ищет movable prop в interaction_distance, шлёт AttachObject"
    - "GrabberPlugin: release action → шлёт DetachObject с drop_pose"
    - "GrabberPlugin публикует GrabAttempt перед попыткой, GrabSucceeded/GrabFailed по результату"
    - "GrabberPlugin contribution: manipulation_locked=true в SharedState агента пока держит проп"
    - "Интеграционный тест: дверь открывается при on_interact('open'), FSM переходит в OPENING→OPEN"
    - "Интеграционный тест: проп прикрепляется через AttachObject, отсоединяется через DetachObject"
  artifacts:
    - path: "workspace/s2_plugins/include/s2/plugins/grabber_plugin.hpp"
      provides: "GrabberPlugin declaration"
      exports: ["GrabberPlugin"]
    - path: "workspace/s2_plugins/src/plugins/grabber_plugin.cpp"
      provides: "GrabberPlugin implementation"
      contains: "cmd::AttachObject"
    - path: "workspace/s2_core/tests/test_door_behavior.cpp"
      provides: "Тест DoorBehavior FSM"
      contains: "on_interact"
    - path: "workspace/s2_core/tests/test_grab_attach.cpp"
      provides: "Тест GrabberPlugin + attachment"
      contains: "AttachObject"
  key_links:
    - from: "grabber_plugin.cpp"
      to: "ctx.world.find_nearest_movable_prop()"
      via: "WorldQuery"
      pattern: "find_nearest_movable_prop"
    - from: "grabber_plugin.cpp"
      to: "cmd::AttachObject"
      via: "ctx.commands"
      pattern: "AttachObject"
    - from: "grabber_plugin.cpp"
      to: "event::GrabSucceeded"
      via: "ctx.bus.publish"
      pattern: "GrabSucceeded"
---

<objective>
Реализовать GrabberPlugin (захват пропов) и написать интеграционные тесты для DoorBehavior и grab/attach механики. Завершает Phase 2.

Purpose: GrabberPlugin — последний новый plugin Phase 2. Тесты верифицируют что вся цепочка (IActorBehavior, команды ядра, Phase 2 тик) работает end-to-end.

Output: GrabberPlugin + 2 теста + регистрация.
</objective>

<execution_context>
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmitriy/study/programming/s2-sim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/dmitriy/study/programming/s2-sim/.planning/PROJECT.md
@/home/dmitriy/study/programming/s2-sim/.planning/ROADMAP.md
@/home/dmitriy/study/programming/s2-sim/.planning/phases/02-actor-prop-foundation/02-CONTEXT.md

<interfaces>
<!-- Все контракты из Plans 02-01..02-04 -->

Из workspace/s2_core/include/s2/world_query.hpp (Plan 02-01):
```cpp
virtual std::optional<ObjectId> find_nearest_movable_prop(Vec3 pos, double radius) const;
// Возвращает nullopt (заглушка) — в тестах мокировать или использовать реальный SimEngine
```

Из workspace/s2_core/include/s2/kernel_command.hpp:
```cpp
struct AttachObject { EntityId parent_id; std::string link; EntityId child_id; Pose3D local_pose; };
struct DetachObject { EntityId child_id; std::optional<Pose3D> drop_pose; };
```

Из workspace/s2_core/include/s2/event_bus.hpp:
```cpp
namespace event {
  struct GrabAttempt   { EntityId agent; EntityId target; };
  struct GrabSucceeded { EntityId agent; EntityId target; };
  struct GrabFailed    { EntityId agent; EntityId target; std::string reason; };
}
```

Из workspace/s2_core/include/s2/shared_state.hpp (SharedState contribution паттерн):
- Посмотреть как BatteryPlugin или другой плагин добавляет contribution
- manipulation_locked — это новый тип contribution, нужен ли отдельный struct?

Из workspace/s2_core/include/s2/sim_engine.hpp (Plan 02-02):
- phase2_actors() вызывает behavior.update()
- apply_kernel_command(AttachObject) записывает attached_to_agent в Prop
- apply_kernel_command(Interact) маршрутизирует к actor.behavior.on_interact()

Из workspace/s2_plugins/include/s2/behaviors/door_behavior.hpp (Plan 02-03):
```cpp
class DoorBehavior : public IActorBehavior {
  void on_interact(EntityId source, const std::string& action,
                   const nlohmann::json& params) override;
  std::string current_state() const override;  // возвращает "CLOSED"/"OPENING"/"OPEN"/"CLOSING"
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GrabberPlugin — захват и отпускание пропов</name>
  <files>
    workspace/s2_plugins/include/s2/plugins/grabber_plugin.hpp
    workspace/s2_plugins/src/plugins/grabber_plugin.cpp
    workspace/s2_plugins/src/plugins_registry.cpp
  </files>
  <read_first>
    - workspace/s2_core/include/s2/plugin_base.hpp (IAgentPlugin, PluginContext, PluginRole)
    - workspace/s2_core/include/s2/world_query.hpp (find_nearest_movable_prop)
    - workspace/s2_core/include/s2/kernel_command.hpp (AttachObject, DetachObject, Interact)
    - workspace/s2_core/include/s2/event_bus.hpp (GrabAttempt, GrabSucceeded, GrabFailed)
    - workspace/s2_core/include/s2/shared_state.hpp (contribution паттерн для manipulation_locked)
    - workspace/s2_plugins/src/plugins_registry.cpp
    - .planning/phases/02-actor-prop-foundation/02-CONTEXT.md (D-22, D-23, D-24, D-25)
  </read_first>
  <behavior>
    - grab action (handle_input("grab") или via Interact): GrabAttempt → find_nearest_movable_prop → если нашли: AttachObject + GrabSucceeded; если нет: GrabFailed{reason:"no_prop_in_range"}
    - release action (handle_input("release") или via Interact): DetachObject{drop_pose=nullopt} + ObjectReleased через EventBus
    - Пока держит проп: each update добавляет contribution manipulation_locked=true в agent.state
    - Нет пропа в руке: нет contribution (manipulation_locked=false по умолчанию)
  </behavior>
  <action>
**grabber_plugin.hpp:**
```cpp
#pragma once
#include <s2/plugin_base.hpp>

namespace s2::plugins {

// GrabberPlugin — interaction плагин агента для захвата пропов.
// Grab: ищет ближайший movable prop в interaction_distance через WorldQuery,
//       шлёт AttachObject{parent=agent, link=grab_link, child=prop}
// Release: шлёт DetachObject{child=held_prop_id}
// Contribution: manipulation_locked=true когда prop захвачен
class GrabberPlugin : public IAgentPlugin {
public:
  std::string type() const override { return "grabber"; }
  PluginRole  role() const override { return PluginRole::INTERACTION; }

  void update(double dt, Agent& agent, const PluginContext& ctx) override;
  void from_config(const YAML::Node& node) override;
  void on_reset(Agent& agent) override;
  std::string to_json() const override;
  bool has_inputs() const override { return true; }
  std::string inputs_schema() const override;
  void handle_input(const std::string& json_input) override;

private:
  double      interaction_distance_{1.5}; // метры
  std::string grab_link_;                 // link агента для привязки ("gripper", "")

  ObjectId    held_prop_id_{0};  // 0 = ничего не держим
  bool        grab_requested_{false};
  bool        release_requested_{false};
};

} // namespace s2::plugins
```

**grabber_plugin.cpp:**

`from_config()`:
```cpp
interaction_distance_ = node["interaction_distance"].as<double>(1.5);
grab_link_            = node["grab_link"].as<std::string>("");
```

`handle_input(json_input)`:
```cpp
auto j = nlohmann::json::parse(json_input, nullptr, false);
if (j.is_discarded()) return;
std::string action = j.value("action", "");
if (action == "grab")    grab_requested_    = true;
if (action == "release") release_requested_ = true;
```

`update(dt, agent, ctx)`:
```cpp
// Grab
if (grab_requested_ && held_prop_id_ == 0) {
  grab_requested_ = false;

  ctx.bus.publish(event::GrabAttempt{.agent = agent.id, .target = 0});

  auto prop_id = ctx.world.find_nearest_movable_prop(
      agent.world_pose.position(), interaction_distance_);

  if (prop_id.has_value()) {
    held_prop_id_ = prop_id.value();
    ctx.commands.push_back(cmd::AttachObject{
        .parent_id  = agent.id,
        .link       = grab_link_,
        .child_id   = static_cast<EntityId>(held_prop_id_),
        .local_pose = Pose3D{}  // смещение = 0 (ядро может уточнить)
    });
    ctx.bus.publish(event::GrabSucceeded{
        .agent  = agent.id,
        .target = static_cast<EntityId>(held_prop_id_)
    });
  } else {
    ctx.bus.publish(event::GrabFailed{
        .agent  = agent.id,
        .target = 0,
        .reason = "no_prop_in_range"
    });
  }
}

// Release
if (release_requested_ && held_prop_id_ != 0) {
  release_requested_ = false;
  ctx.commands.push_back(cmd::DetachObject{
      .child_id  = static_cast<EntityId>(held_prop_id_),
      .drop_pose = std::nullopt
  });
  held_prop_id_ = 0;
}

// Contribution: manipulation_locked
if (held_prop_id_ != 0) {
  // Добавить LockContribution в SharedState агента
  // Найти как это делают другие плагины (прочитать shared_state.hpp)
  // Если нет LockContribution — использовать motion_lock_zone паттерн
  // Или просто: agent.state.add(LockData{.locked = true, .source = "grabber"});
  // Реализовать по аналогии с существующими contributions
}
```

`on_reset(agent)`:
```cpp
held_prop_id_      = 0;
grab_requested_    = false;
release_requested_ = false;
```

`inputs_schema()`:
```cpp
return R"({"type":"object","properties":{"action":{"type":"string","enum":["grab","release"]}}})";
```

`to_json()`:
```cpp
return nlohmann::json{
    {"type", "grabber"},
    {"held_prop_id", held_prop_id_},
    {"interaction_distance", interaction_distance_}
}.dump();
```

Регистрация в **plugins_registry.cpp**:
```cpp
#include <s2/plugins/grabber_plugin.hpp>

static const PluginRegistrar register_grabber("grabber",
    []() { return std::make_unique<GrabberPlugin>(); });
```
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_plugins/include/s2/plugins/grabber_plugin.hpp существует
    - grabber_plugin.hpp содержит "class GrabberPlugin : public IAgentPlugin"
    - grabber_plugin.hpp содержит "INTERACTION"
    - grabber_plugin.cpp содержит "cmd::AttachObject"
    - grabber_plugin.cpp содержит "GrabSucceeded"
    - grabber_plugin.cpp содержит "GrabFailed"
    - grabber_plugin.cpp содержит "find_nearest_movable_prop"
    - workspace/s2_plugins/src/plugins_registry.cpp содержит "grabber"
    - Docker build tests завершается без ошибок
  </acceptance_criteria>
  <done>GrabberPlugin реализован: grab/release через handle_input, GrabAttempt/GrabSucceeded/GrabFailed события, manipulation_locked contribution.</done>
</task>

<task type="auto">
  <name>Task 2: Интеграционные тесты — DoorBehavior + Grab/Attach</name>
  <files>
    workspace/s2_core/tests/test_door_behavior.cpp
    workspace/s2_core/tests/test_grab_attach.cpp
    workspace/s2_core/CMakeLists.txt
  </files>
  <read_first>
    - workspace/s2_core/tests/test_zone_system.cpp (образец интеграционного теста с SimEngine + EventBus)
    - workspace/s2_core/tests/test_tick_lifecycle.cpp (образец tick теста)
    - workspace/s2_core/tests/test_kernel_command.cpp (образец теста KernelCommand)
    - workspace/s2_core/include/s2/sim_engine.hpp (step(), push_command(), world())
    - workspace/s2_core/include/s2/actor.hpp (Actor struct)
    - workspace/s2_core/include/s2/prop.hpp (Prop struct)
    - workspace/s2_plugins/include/s2/behaviors/door_behavior.hpp (DoorBehavior)
    - workspace/s2_core/CMakeLists.txt (добавить тесты в s2_core_tests)
  </read_first>
  <action>
**test_door_behavior.cpp** — тест DoorBehavior FSM:

```cpp
#include <gtest/gtest.h>
#include <s2/sim_engine.hpp>
#include <s2/world.hpp>
#include <s2/actor.hpp>
#include <s2/actor_behavior.hpp>
#include <s2_plugins/behaviors/door_behavior.hpp>  // или s2/behaviors/door_behavior.hpp

// Вспомогательная функция: создать SimEngine с одним актором-дверью
static SimEngine make_engine_with_door(ActorId door_id) {
  SimEngine engine{{.update_rate = 100.0}};
  engine.pause();  // начинаем на паузе

  SimWorld world;
  Actor door;
  door.id = door_id;
  door.name = "test_door";
  door.type = "door";

  auto behavior = std::make_unique<DoorBehavior>();
  YAML::Node cfg;
  cfg["open_duration"]  = 0.1;  // быстро для тестов
  cfg["close_duration"] = 0.1;
  behavior->on_init(cfg);
  behavior->on_spawn(door_id);
  door.behavior = std::move(behavior);

  world.add_actor(std::move(door));
  engine.load_world(std::move(world));
  engine.resume();
  return engine;
}

TEST(DoorBehavior, OpenViaInteract) {
  auto engine = make_engine_with_door(10);
  auto* actor = engine.world().get_actor(10);
  ASSERT_NE(actor, nullptr);
  EXPECT_EQ(actor->behavior->current_state(), "CLOSED");

  // Отправить Interact{action:"open"}
  engine.push_command(cmd::Interact{
      .source_id = 1, .target_id = 10, .action = "open",
      .params = nlohmann::json::object(), .max_distance = 0.0
  });
  engine.step(1);

  EXPECT_EQ(actor->behavior->current_state(), "OPENING");
  EXPECT_TRUE(actor->collision_enabled);  // пока открывается, коллизия включена

  // Прождать open_duration (100 тиков по 0.01 сек = 1.0 сек, но нам 0.1/0.01=10 тиков)
  engine.step(12);  // 12 > 10 — гарантированно откроется

  EXPECT_EQ(actor->behavior->current_state(), "OPEN");
  EXPECT_FALSE(actor->collision_enabled);  // OPEN — коллизия отключена
}

TEST(DoorBehavior, ActorStateChangedPublished) {
  SimEngine engine{{.update_rate = 100.0}};
  engine.pause();
  SimWorld world;
  Actor door;
  door.id = 20;
  door.type = "door";
  auto behavior = std::make_unique<DoorBehavior>();
  YAML::Node cfg; cfg["open_duration"] = 0.01;
  behavior->on_init(cfg);
  behavior->on_spawn(20);
  door.behavior = std::move(behavior);
  world.add_actor(std::move(door));
  engine.load_world(std::move(world));

  std::vector<event::ActorStateChanged> events;
  engine.bus().subscribe<event::ActorStateChanged>([&](const event::ActorStateChanged& e) {
    events.push_back(e);
  });

  engine.resume();
  engine.push_command(cmd::Interact{
      .source_id=1, .target_id=20, .action="open",
      .params=nlohmann::json::object(), .max_distance=0.0
  });
  engine.step(5);

  EXPECT_FALSE(events.empty());
  // Первое событие: CLOSED→OPENING
  EXPECT_EQ(events[0].old_state, "CLOSED");
  EXPECT_EQ(events[0].new_state, "OPENING");
}
```

**test_grab_attach.cpp** — тест AttachObject/DetachObject через KernelCommand:

```cpp
#include <gtest/gtest.h>
#include <s2/sim_engine.hpp>
#include <s2/world.hpp>
#include <s2/prop.hpp>
#include <s2/agent.hpp>

TEST(GrabAttach, AttachObjectUpdatesPrope) {
  SimEngine engine{{.update_rate = 100.0}};
  SimWorld world;

  Agent agent;
  agent.id = 1;
  agent.world_pose = {1.0, 0.0, 0.0};
  world.add_agent(std::move(agent));

  Prop prop;
  prop.id = 100;
  prop.movable = true;
  prop.world_pose = {1.5, 0.0, 0.0};
  world.add_prop(std::move(prop));

  engine.load_world(std::move(world));

  // Отправить AttachObject
  engine.push_command(cmd::AttachObject{
      .parent_id = 1,
      .link      = "gripper",
      .child_id  = 100,
      .local_pose = Pose3D{0.5, 0.0, 0.0}
  });
  engine.step(1);

  const auto* p = engine.world().get_prop(100);
  ASSERT_NE(p, nullptr);
  EXPECT_TRUE(p->attached_to_agent.has_value());
  EXPECT_EQ(p->attached_to_agent.value(), 1u);
  EXPECT_EQ(p->attach_link, "gripper");
}

TEST(GrabAttach, DetachObjectClearsAttachment) {
  SimEngine engine{{.update_rate = 100.0}};
  SimWorld world;

  Agent agent; agent.id = 1; world.add_agent(std::move(agent));

  Prop prop;
  prop.id = 101;
  prop.movable = true;
  prop.attached_to_agent = 1;  // уже прикреплён
  prop.attach_link = "arm";
  world.add_prop(std::move(prop));

  engine.load_world(std::move(world));

  engine.push_command(cmd::DetachObject{.child_id = 101, .drop_pose = std::nullopt});
  engine.step(1);

  const auto* p = engine.world().get_prop(101);
  ASSERT_NE(p, nullptr);
  EXPECT_FALSE(p->attached_to_agent.has_value());
  EXPECT_TRUE(p->attach_link.empty());
}
```

**CMakeLists.txt** — добавить в s2_core_tests:
```cmake
tests/test_door_behavior.cpp
tests/test_grab_attach.cpp
```

ВАЖНО: перед написанием теста прочитать реальный scene_loader API и SimEngine тесты чтобы использовать правильный способ создания SimWorld и загрузки в движок. Файл `test_zone_system.cpp` — образцовый источник.
  </action>
  <verify>
    <automated>cd /home/dmitriy/study/programming/s2-sim && docker compose --project-directory docker up --build tests 2>&1 | grep -E "PASSED|FAILED|ERROR|test_door|test_grab"</automated>
  </verify>
  <acceptance_criteria>
    - workspace/s2_core/tests/test_door_behavior.cpp существует
    - workspace/s2_core/tests/test_grab_attach.cpp существует
    - test_door_behavior.cpp содержит "OpenViaInteract"
    - test_door_behavior.cpp содержит "ActorStateChangedPublished"
    - test_grab_attach.cpp содержит "AttachObjectUpdatesPrope"
    - test_grab_attach.cpp содержит "DetachObjectClearsAttachment"
    - Docker build tests: "DoorBehavior.OpenViaInteract PASSED"
    - Docker build tests: "GrabAttach.AttachObjectUpdatesPrope PASSED"
    - Docker build tests: "GrabAttach.DetachObjectClearsAttachment PASSED"
  </acceptance_criteria>
  <done>Интеграционные тесты проходят. Phase 2 полностью проверена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handle_input(json) → grab_requested_ | JSON-вход от HTTP/ROS2 устанавливает флаг захвата |
| find_nearest_movable_prop → AttachObject | WorldQuery результат используется как target для AttachObject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | GrabberPlugin::handle_input JSON | mitigate | nlohmann::json::parse с nullptr error_handler — invalid JSON игнорируется (is_discarded check) |
| T-02-15 | Spoofing | find_nearest_movable_prop → prop_id | accept | Заглушка возвращает nullopt; реальная реализация Phase 5 должна проверять movable flag |
| T-02-16 | Elevation of Privilege | manipulation_locked contribution | accept | Contribution только блокирует движение агента; не даёт доступа к другим агентам |
</threat_model>

<verification>
После выполнения:

```bash
docker compose --project-directory docker up --build tests 2>&1 | grep -E "PASSED|FAILED|test_door|test_grab|ALL TESTS"
```

Ожидаемый вывод (среди других):
- `DoorBehavior.OpenViaInteract PASSED`
- `DoorBehavior.ActorStateChangedPublished PASSED`
- `GrabAttach.AttachObjectUpdatesPrope PASSED`
- `GrabAttach.DetachObjectClearsAttachment PASSED`
</verification>

<success_criteria>
- GrabberPlugin: grab/release через handle_input + JSON schema
- GrabberPlugin: GrabAttempt→GrabSucceeded/GrabFailed через EventBus
- GrabberPlugin: manipulation_locked contribution
- test_door_behavior: FSM CLOSED→OPENING→OPEN через on_interact("open")
- test_door_behavior: ActorStateChanged событие публикуется при переходах
- test_grab_attach: AttachObject записывает attached_to_agent в Prop
- test_grab_attach: DetachObject очищает attached_to_agent
- Все 4 теста проходят в Docker
</success_criteria>

<output>
После завершения создать `.planning/phases/02-actor-prop-foundation/02-05-SUMMARY.md`
</output>
