# Задача 20.1 — Выравнивание ориентации агента по опорной поверхности

## Цель

При движении по наклонной поверхности агент должен визуально наклоняться (pitch и roll)
в соответствии с нормалью поверхности под ним. Сейчас pitch и roll всегда равны 0.

## Зависимости

- Задача 20 (CollisionSystem) — завершена. Использует данные из `check_sphere_all()`.

## Принятые решения

| Вопрос | Решение |
|--------|---------|
| Источник нормали | `CollisionContact.contact_normal` из уже вычисленных контактов (фаза 3h) |
| Нормаль какого контакта | Первый walkable-контакт (наибольшая penetration, т.к. contacts отсортированы) |
| При отсутствии опоры | Сброс pitch=roll=0 (агент в воздухе или только у стен) |
| Сглаживание | Нет (50Hz обновление даёт плавность при постепенном въезде на пандус) |
| Влияние на yaw | Нет — yaw определяется diff_drive, не поверхностью |

## Архитектура

### Текущее состояние

- `Pose3D` (types.hpp) имеет поля `roll`, `pitch`, `yaw`
- В `sim_engine.hpp` только `yaw` обновляется (`yaw += wz * dt_`)
- `roll` и `pitch` никогда не меняются — всегда 0
- Фронтенд (`app.js`) уже применяет все три угла: `rotation.set(pose.roll, pose.yaw, -pose.pitch, 'YZX')`
- `check_sphere_all()` возвращает `contact_normal` — нормаль поверхности в мировых координатах

### Математика

Нормаль поверхности `n = (nx, ny, nz)` дана в мировых координатах. Перед вычислением pitch/roll её нужно привести в тело робота (повернуть на `-yaw`), чтобы выравнивание было корректным при любом направлении движения:

```
nx_body =  cos(yaw) * nx + sin(yaw) * ny   // проекция на ось вперёд
ny_body = -sin(yaw) * nx + cos(yaw) * ny   // проекция на ось влево

pitch = atan2( nx_body, nz)   // наклон вперёд/назад
roll  = atan2(-ny_body, nz)   // крен влево/вправо
```

Проверка для пандуса (нормаль = (-0.316, 0, 0.949)) при yaw=0:
- `nx_body = -0.316, ny_body = 0`
- `pitch = atan2(-0.316, 0.949) ≈ -18.4°` ✓
- `roll  = 0` ✓

Тот же пандус при yaw=π/2 (робот повёрнут поперёк наклона):
- `nx_body = 0, ny_body = 0.316`
- `pitch = 0` ✓
- `roll  = atan2(-0.316, 0.949) ≈ -18.4°` ✓

Горизонтальный пол (нормаль = (0, 0, 1)):
- `roll = pitch = 0` при любом yaw ✓

## Реализация

### Единственный изменяемый файл: `workspace/s2_core/include/s2/sim_engine.hpp`

В фазе 3h, после цикла обработки коллизий, добавить секцию выравнивания ориентации
(внутри `if (agent.has_collision && agent.bounding.type == ShapeType::SPHERE)`):

```cpp
// === Выравнивание по опорной поверхности ===
bool surface_found = false;
for (const auto& contact : contacts) {
    bool walkable = contact.contact_normal.z() >=
                    std::cos(agent.max_slope_rad);
    if (walkable) {
        const Vec3& n = contact.contact_normal;
        // Приводим нормаль из мировой СК в тело робота (поворот на -yaw)
        const double yaw = agent.world_pose.yaw;
        const double nx_body =  std::cos(yaw) * n.x() + std::sin(yaw) * n.y();
        const double ny_body = -std::sin(yaw) * n.x() + std::cos(yaw) * n.y();
        agent.world_pose.pitch = std::atan2( nx_body, n.z());
        agent.world_pose.roll  = std::atan2(-ny_body, n.z());
        surface_found = true;
        break;
    }
}
if (!surface_found) {
    agent.world_pose.roll  = 0.0;
    agent.world_pose.pitch = 0.0;
}
```

## Проверка

```bash
# Тесты (не должно ничего сломаться)
docker compose --project-directory docker up --build tests

# Визуальная проверка
docker compose --project-directory docker up --build sim
# Открыть http://localhost:1937, загрузить test_collision.yaml
```

**Критерии:**
- [ ] robot_0 наклоняется при въезде на пологий пандус (roll=-15°)
- [ ] robot_0 возвращается к горизонтали при съезде с пандуса
- [ ] robot_1 (без коллизий) — ориентация не меняется (нет регрессии)
- [ ] Все существующие тесты проходят
